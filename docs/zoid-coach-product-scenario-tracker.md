# Zoid 666 End-User Scenario Tracker

This tracker covers the complete Zoid 666 product specification from the end user's point of view.

Every checkbox describes something the user can see, do, understand, configure, confirm, or recover from.

Technical implementation details are included only when they create an observable user outcome.

## Audit result

Updated on 2026-07-14 against branch `codex/full-system` through the independently verified review categories and hypothesis-learning signed-QA batch, the exact 666-scenario registry, and capped native macOS accessibility, pixel, persistence, privacy-deletion, and raw-database testing.

This update includes the implemented twelve-step onboarding flow, crash-safe onboarding persistence, permission deferral and repair paths, canonical Screenwatch folder selection, application discovery and classification, schedule and gaming-policy choices, rules-only coaching, Reminder-list inclusion policy, and durable first-daily-plan preparation.

Only scenarios proven completely usable end to end are checked.

- **Fully implemented:** 185
- **Touches remaining:** 304
- **Frontend only left:** 3
- **Partially implemented:** 102
- **Barely started:** 6
- **Not implemented:** 39
- **Blocked from verification:** 27
- **Total:** 666

### Status meanings

- **Fully implemented** means the complete user journey is available and credibly proven in the running product.
- **Touches remaining** means the core journey is usable but smaller usability or verification gaps remain.
- **Frontend only left** means supporting behavior exists but the user cannot complete the scenario through the interface.
- **Partially implemented** means meaningful pieces exist but the user cannot complete the whole scenario.
- **Barely started** means only a primitive, model, store, or generic shell exists.
- **Not implemented** means no meaningful implementation of the user scenario was found.
- **Blocked from verification** means the flow may exist but current conditions prevent credible end-to-end proof.

### Current verification note

The signed-QA application now opens a clean first launch without requiring a pre-existing policy database.

Visible accessibility click-through verified all 12 onboarding steps from welcome through the first daily plan and Today.

The test also verified leaving setup for Today, relaunching the app, resuming at the exact saved onboarding step, completing setup, and relaunching directly into Today.

The isolated QA and unit suites cover persistence conflicts, exact Reminder-list identities, all-lists-excluded local fallback, canonical Screenwatch bookmarks, gaming-policy storage, and first-plan restart durability.

The persistent signed-QA runtime registered its dedicated helper through SMAppService, ran the exact QA Mach service from the installed application, and passed real XPC mutation receipt, replay, and stale-write rejection checks.

The visible run saved exact Reminder-list choices, application classifications, schedule boundaries, gaming posture, rules-only coaching, bounded notification delivery, and onboarding completion through that installed runtime.

The first daily-plan handoff exposed and fixed two direct blockers: Today now refreshes on the onboarding route transition, and external Reminder reconciliation preserves incomplete local fallback tasks; the installed signed-QA background-agent journey additionally verified live health refresh, Login Items navigation, packaged repair, disabled and re-enabled launchd states, local-data preservation, and simultaneous production/QA identity isolation.

## 1. First launch

- [x] Open Zoid 666 for the first time and immediately understand that it helps connect planned work with actual computer activity. **Status: Fully implemented.** A clean signed-QA launch opens the Welcome step with explicit planned-versus-actual copy, and the visible accessibility run confirmed the complete first screen (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:102`; `Tests/ZoidCoachAppTests/OnboardingCoordinatorTests.swift`).
- [x] Understand that Apple Reminders represents intended work. **Status: Fully implemented.** The Welcome and Local Truth steps explicitly describe Reminders as intended work before the dedicated permission step, and both screens were verified in the signed-QA app (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:102`).
- [x] Understand that Screenwatch represents observed computer activity. **Status: Fully implemented.** The first-launch copy explains what actually happens on the Mac and the dedicated Screenwatch step explains behavior coaching before access or folder selection; the visible run reached and inspected this step (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:102`).
- [x] Understand that the Today dashboard provides persistent status and prompts. **Status: Fully implemented.** Welcome states that Today keeps the plan, source status, and unanswered coaching choices in one place, and Exit For Now visibly opened Today (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:104`; `Sources/ZoidCoachApp/Views/DashboardView.swift`).
- [x] Understand that Zoid 666 is a coach rather than a replacement task manager. **Status: Fully implemented.** A truly fresh signed-QA Welcome screen visibly and accessibly states that Zoid 666 is a coach, not a replacement task manager, names Apple Reminders as the system of record for connected tasks, tells users what remains in Reminders versus Today, and retains the non-punitive boundary. Native Continue advanced to Local Privacy, persisted the exact order, and signed relaunch restored step 2 without replaying Welcome (`Sources/ZoidCoachApp/OnboardingWelcomeCopy.swift`; `Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift`; `Tests/ZoidCoachAppTests/OnboardingWelcomeCopyTests.swift`; `.audit/runs/onboarding-system-of-record-copy/verifier/REPORT.md`).
- [x] Understand that the app does not punish, shame, or block the user by default. **Status: Fully implemented.** The Welcome step says recovery is without shame and nothing is blocked or punished by default; the gaming-policy step reinforces that no option locks apps or removes the user's final decision (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:104`).
- [x] Understand that important data stays on the Mac by default. **Status: Fully implemented.** The Local Truth step explicitly explains local Reminders, Screenwatch summaries, plans, and coaching history, and the signed-QA run visibly confirmed the screen (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:108`).
- [x] Continue onboarding without being forced to configure optional AI features. **Status: Fully implemented.** The installed signed-QA run selected Rules-only, saved it through the XPC policy boundary, continued without configuring a model provider, and completed setup (`OnboardingRootView.swift`; `OnboardingCoordinatorTests.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Leave onboarding and resume from the same point later. **Status: Fully implemented.** Exit For Now opened Today from step 3, and relaunching the signed-QA app returned to the exact same Reminders step (`Sources/ZoidCoachInfrastructure/OnboardingProgressStore.swift`; `Tests/ZoidCoachAppTests/OnboardingCoordinatorTests.swift`).
- [x] Restart the app during onboarding without losing completed setup steps. **Status: Fully implemented.** Killing and relaunching the signed-QA app at step 3 restored step 3 exactly, while crash-safe revision and migration tests cover durable progress recovery (`Tests/ZoidCoachAppTests/OnboardingProgressStoreHardeningTests.swift`; `Tests/ZoidCoachAppTests/OnboardingProgressTests.swift`).

## 2. Reminders permission during onboarding

- [x] See why Reminders access is useful before macOS requests permission. **Status: Fully implemented.** The dedicated Reminders onboarding step explains intended work and unavailable capabilities before the Request Access control, and the signed-QA run visibly inspected this state without invoking macOS permission (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift`; `Tests/ZoidCoachAppTests/OnboardingCoordinatorTests.swift`).
- [ ] Grant Reminders access and continue setup. **Status: Touches remaining.** The signed QA app restored the exact Reminders step after restart, consumed an isolated denied-to-granted fixture transition, displayed healthy permission state, disabled repeated Request Access, and entered list selection, while a real EventKit authorization prompt still requires a user-controlled macOS reset (`OnboardingCoordinator.swift`; `QAFixtureOSCompositionTests.swift`; `.audit/runs/reminders-permission-recovery/verifier/REPORT.md`).
- [ ] Deny Reminders access and understand which features will be unavailable. **Status: Touches remaining.** The installed signed app visibly showed denied fixture status, no production Reminders access, disabled repeated Request Access after the explicit request, enabled Continue, and then explained that Apple tasks stay unavailable while local tasks and setup remain usable (`OnboardingRootView.swift`; `.audit/runs/reminders-permission-recovery/verifier/REPORT.md`).
- [ ] Continue setup after denying Reminders access. **Status: Touches remaining.** The signed app enabled Continue after denial and visibly switched to an explicit local-only state without another prompt, while the actual Continue transition following a real macOS denial remains user-controlled (`OnboardingCoordinator.swift`; `.audit/runs/reminders-permission-recovery/verifier/REPORT.md`).
- [ ] Open System Settings directly when ready to grant previously denied access. **Status: Touches remaining.** Production opens the Reminders privacy destination and reports success or a manual fallback path, with focused injection proof, while the signed QA click was timeboxed before a stable System Settings capture (`RemindersConnectionController.swift`; `OnboardingDependencies.swift`; `.audit/runs/reminders-permission-recovery/verifier/REPORT.md`).
- [ ] Return from System Settings and see the updated permission state. **Status: Touches remaining.** Foreground activation now performs inspection without another authorization request, focused tests prove denied-to-healthy recovery, and the signed restart visibly restored Healthy with deterministic fixture evidence, while the foreground screenshot itself remained unstable (`OnboardingCoordinator.swift`; `RemindersConnectionController.swift`; `.audit/runs/reminders-permission-recovery/verifier/REPORT.md`).
- [ ] Avoid seeing the same permission dialog repeatedly after declining it. **Status: Touches remaining.** The installed denied state disabled Request Access, Recheck used inspection only, local-only deferral explicitly preserved a single opt-in Request Access path, and focused counters prove foreground checks never call the permission request again, while real EventKit denial remains user-controlled (`OnboardingRootView.swift`; `OnboardingCoordinatorTests.swift`; `.audit/runs/reminders-permission-recovery/verifier/REPORT.md`).
- [ ] Use manual local planning while Reminders access is unavailable. **Status: Touches remaining.** In an installed signed-QA app with Reminders unavailable, the Today inventory exposed New Local Task, kept Create disabled until a title was entered, created the task through the authenticated agent boundary, and immediately displayed it as today's main objective and in focus-capacity calculations. The signed lifecycle probe also covered exact-payload replay, two agent restarts, start, completion, durable history, and zero Apple Reminders outbox mutation, but this run did not produce the immutable scenario-bound evidence manifest required for a strict completion claim (`Sources/ZoidCoachApp/Views/LocalTaskCreationView.swift`; `Sources/ZoidCoachInfrastructure/AgentMutationRouter.swift`; `Scripts/verify-qa-manual-local-task-xpc.sh`; `.audit/runs/manual-local-task/eb0866b/REPORT.md`).

## 3. Screenwatch setup during onboarding

- [x] Let Zoid 666 find Screenwatch automatically at the expected location. **Status: Fully implemented.** The onboarding step automatically inspects the canonical default source before asking for a choice, and the visible signed-QA run displayed its current health plus Check Expected Folder and Choose Folder controls (`Sources/ZoidCoachInfrastructure/ScreenwatchSourceRepository.swift`; `Tests/ZoidCoachAppTests/ScreenwatchSetupServiceTests.swift`).
- [x] See whether the detected Screenwatch source is healthy. **Status: Fully implemented.** The signed installed Settings card visibly moved through Waiting, Stale, and Healthy against the isolated expected source, with current/schema-valid explanations and stable accessibility identifiers. Forty-nine focused Screenwatch tests cover the same health boundary without exposing captured content (`ScreenwatchConnectionView.swift`; `.audit/runs/screenwatch-recovery-ux/verifier/REPORT.md`).
- [ ] Select another Screenwatch folder when the default location is unavailable. **Status: Touches remaining.** Settings visibly opened the native days-folder picker with privacy-safe instructions, while focused controller, bookmark, app, agent, OCR, and restart tests pass. The capped accessibility run could not reliably enter the isolated alternate path, so native selection and persisted restart remain unclaimed (`ScreenwatchConnectionView.swift`; `ScreenwatchSourceRepositoryTests.swift`; `.audit/runs/screenwatch-recovery-ux/verifier/REPORT.md`).
- [ ] Understand why a selected folder is invalid without seeing sensitive captured content. **Status: Touches remaining.** Privacy-safe malformed, schema, bookmark, and unsafe-path diagnostics are implemented and tested without exposing captured records, but an invalid native folder selection was not visibly exercised (`Tests/ZoidCoachAppTests/ScreenwatchSetupServiceTests.swift`; `Tests/ZoidCoachAppTests/ScreenwatchSourceRepositoryTests.swift`).
- [x] Continue setup without Screenwatch and understand that behavior coaching will be unavailable. **Status: Fully implemented.** The signed-QA run clicked Not Now on the Screenwatch step, saw the consequence copy, and continued to Notifications (`Sources/ZoidCoachApp/Onboarding/OnboardingCoordinator.swift`; `Tests/ZoidCoachAppTests/OnboardingCoordinatorTests.swift`).
- [x] Open the repair path when folder access is denied. **Status: Fully implemented.** In the isolated signed-QA Settings journey, the user discovered and activated `CHOOSE FOLDER`, selected a deterministic alternate Screenwatch days folder through the native picker, rechecked to `CHOSEN FOLDER HEALTHY`, and relaunched the app with the bookmark still selected and `CHANGE FOLDER` plus `USE EXPECTED FOLDER` available. The fixture's private title and URL never appeared in accessibility output, and 11 focused repair-action tests passed (`ScreenwatchRepairActionPresentation.swift`; `ScreenwatchConnectionView.swift`; `ScreenwatchRepairActionPresentationTests.swift`; `.audit/runs/settings-safety-6038/REPORT.md`).
- [ ] Return after repairing access and see Screenwatch become connected. **Status: Touches remaining.** Recheck resolves the canonical bookmark afresh and updates onboarding health, with consumer and restart tests covering the transition; a visible repair round trip remains (`ScreenwatchSourceRepositoryTests.swift`; `OnboardingCoordinatorTests.swift`).
- [x] Understand that screenshots are not routinely inspected. **Status: Fully implemented.** The signed installed Screenwatch card and picker explicitly state that captured titles, URLs, screenshots, content, and file locations are never shown during routine source inspection. Seeded private fields remained absent across Waiting, Stale, and Healthy states, while screenshot-analysis policy remains a separate explicit choice (`ScreenwatchConnectionView.swift`; `.audit/runs/screenwatch-recovery-ux/verifier/REPORT.md`).
- [ ] Choose whether screenshot analysis may be used for genuinely ambiguous situations. **Status: Touches remaining.** Fresh signed onboarding proved the default-Off control, explicit-On state, ambiguous-only explanation, and fail-closed policy-before-advance behavior. The verifier fixed the startup bootstrap race and Screenwatch mutation allowlist with focused tests, but the final allowlist fix has not yet been repackaged to prove that Continue durably writes the choice, advances, and restores it after relaunch (`OnboardingRootView.swift`; `OnboardingCoordinatorTests.swift`; `PolicyStoreTests.swift`; `.audit/runs/screenshot-analysis-consent/candidate/REPORT.md`).

## 4. Notification setup

- [x] Understand that the Today dashboard remains available without notification permission. **Status: Fully implemented.** The Notifications onboarding step explicitly explains the Today fallback, and the visible signed-QA run clicked Not Now and continued successfully (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift:132`; `Tests/ZoidCoachAppTests/OnboardingCoordinatorTests.swift`).
- [ ] Grant notification permission for timely prompts. **Status: Touches remaining.** The fresh installed signed-QA run visibly reported healthy notification access, accepted Request Notification Access, scheduled the canonical prompt, and processed its action through the running agent. The recovery controller now refreshes authorization automatically after returning from System Settings, while a real macOS authorization reset and Notification Center click remain before full qualification (`NotificationService.swift`; `NotificationDeliveryHealthView.swift`; `PromptNotificationCoordinator.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`; `.audit/runs/notification-permission-recovery/0d6ec2d/REPORT.md`).
- [x] Resolve a test notification action. **Status: Fully implemented.** The installed signed-QA app created one canonical `ONBOARDING_TEST` prompt, the running agent accepted only its whitelisted `continue_intentionally` action from the delivered fixture notification, the response persisted with surface `notification`, onboarding changed to Prompt Resolved, and relaunch retained the result (`OnboardingTestPromptService.swift`; `PromptNotificationCoordinator.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`).
- [ ] Deny notification permission and continue with in-app prompts. **Status: Touches remaining.** The rebased installed signed-QA app visibly reported Attention under denied authorization, explained that every important action remains available in Today, disabled the repeated permission request, and enabled Continue without touching production Notification Center. A later background-agent timeout blocked this verifier before the complete fallback journey, and an actual macOS authorization-panel denial remains before full qualification (`OnboardingCoordinator.swift`; `OnboardingTestPromptService.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`; `.audit/runs/notification-permission-recovery/0d6ec2d/REPORT.md`).
- [x] Test the Today dashboard prompt inbox. **Status: Fully implemented.** With notification permission denied, step 11 visibly reported Today Fallback, Exit For Now exposed the same unresolved canonical prompt and actions under Today Decisions, Continue Setup resolved it with persisted surface `dashboard`, and Resume Setup returned to step 11 with Prompt Resolved and Continue enabled (`DashboardView.swift`; `TodayDashboardXPC.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`).

## 5. Initial preferences

- [x] Choose which Reminder lists Zoid 666 should use. **Status: Fully implemented.** The installed signed-QA run discovered Work and Personal with their exact stable identifiers, selected Include Work and Exclude Personal in the native UI, persisted both choices through XPC, and advanced successfully (`RemindersService.swift`; `OnboardingRootView.swift`; `SettingsView.swift`; `UserPolicy.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Exclude personal or irrelevant Reminder lists. **Status: Fully implemented.** Exclude Personal was selected in the visible run and saved through the same conflict-safe policy boundary. AppModel, agent synchronization, planning, rename, restart, unknown-list fail-closed, and all-excluded local fallback tests cover downstream enforcement (`AppModel.swift`; `AgentReminderPlanner.swift`; `OnboardingFirstDailyPlanService.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Preview detected applications before assigning categories. **Status: Fully implemented.** The installed signed-QA app scanned the actual Mac, displayed 116 detected applications, exposed Scan Again, and advanced only after the user reviewed the inventory (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift`; `Tests/ZoidCoachAppTests/OnboardingCoordinatorTests.swift`).
- [x] Mark known work applications. **Status: Fully implemented.** The visible installed run selected Work for Activity Monitor, saved the classification through the live QA XPC helper, advanced to schedule setup, and retained the winning policy version (`OnboardingCoordinator.swift`; `PolicyStoreTests.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Mark known gaming applications. **Status: Fully implemented.** The visible installed run selected Gaming for Atoll, saved it through the live QA XPC helper, and advanced successfully; policy persistence and behavior-consumer tests cover the downstream classification (`OnboardingCoordinator.swift`; `PolicyStoreTests.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Leave ambiguous applications unclassified for later review. **Status: Fully implemented.** Every detected app defaulted to Automatic, most remained unchanged, and the complete classification policy was durably saved when the run advanced to schedule setup (`Sources/ZoidCoachApp/Views/Onboarding/OnboardingRootView.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Configure a flexible work window. **Status: Fully implemented.** The installed signed onboarding step selected only Monday and Saturday, saved exact 08:30-18:15 work boundaries through the agent, advanced, and reloaded the identical weekday subset and times after relaunch. Removing the final selected weekday visibly changed the message to `Choose at least one work day.` and disabled Continue (`OnboardingCoordinator.swift`; `OnboardingRootView.swift`; `.audit/runs/onboarding-schedule-weekdays/1c30a680a844ba2f7974e4f210d4446ca35f9fb1/evidence.json`).
- [x] Configure quiet hours. **Status: Fully implemented.** The installed signed onboarding step saved and reloaded overnight quiet hours, and the signed delivery traversal then kept a prompt immediately usable in Today while scheduling its notification for the exact 08:15 quiet end. Same-day, overnight, exact-end, invalid-timezone, future-delivery, and latest-policy boundary tests also pass (`OnboardingCoordinator.swift`; `QuietHoursDeliveryBoundary.swift`; `.audit/runs/quiet-hours-delivery/7a216a88032234e5d7a442e46a8052a16b283b92/REPORT.md`).
- [x] Choose an initial gaming policy. **Status: Fully implemented.** The installed run selected Balanced, saved it through the app-agent XPC policy boundary, and advanced to coaching mode; restart and mutation tests cover durability (`OnboardingRootView.swift`; `PolicyStoreTests.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Choose rules-only coaching for Release 1. **Status: Fully implemented.** The installed run selected Rules-only, saved it without configuring AI, advanced through delivery proof, and completed onboarding (`OnboardingRootView.swift`; `OnboardingCoordinatorTests.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).
- [x] Finish onboarding by opening the first daily plan. **Status: Fully implemented.** The installed Zoid 666 QA run prepared a real 15-minute local starter objective, opened Today with one active commitment and one planned block immediately, then retained the same task, estimate, and action controls across package replacement and repeated quit/relaunch (`OnboardingFirstDailyPlanService.swift`; `AppModel.swift`; `OnboardingFirstDailyPlanServiceTests.swift`; `.audit/runs/onboarding-visible/cfe7fbd3480191177d0a59b568061a0236101147/evidence.json`).

## 6. Starting the day

- [ ] Receive a low-pressure planning invitation at the configured planning time. **Status: Partially implemented.** The agent schedules a PLAN_READY notification at morning confirmation time, but the wording was not assessed as low-pressure and no live delivery was verified (`AgentMain.swift:776-813`; `PromptNotificationCoordinator.swift:93-102`).
- [ ] Receive the invitation when first using the Mac if the configured time passed while inactive. **Status: Partially implemented.** Missed nightly planning after wake is unit-tested and immediate notification delivery is possible when the target time is past, but the complete wake-to-invitation flow was not exercised live.
- [ ] Open planning manually before the configured time. **Status: Touches remaining.** The installed signed app exposed enabled `PLAN NOW` and `DRAFT TODAY` controls with two real deterministic Reminder candidates, while activation of the manual draft itself remains unclaimed (`DashboardView.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [x] Snooze planning without causing coaching escalation. **Status: Fully implemented.** The installed signed app snoozed the canonical PLAN_READY decision, removed it without escalation, rendered `PLANNING SNOOZED` with the exact return time, and preserved that state through package replacement and relaunch (`PlanningInvitationService.swift`; `DashboardView.swift`; `PlanningInvitationServiceTests.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [x] Receive the planning invitation again when the snooze ends. **Status: Fully implemented.** A focused time-shift advanced the persisted snooze to its due time, the running signed helper presented it once, and relaunch visibly restored the full invitation with Plan Now, Snooze, Dismiss, and Work Unplanned actions (`AgentMain.swift`; `PlanningInvitationServiceTests.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [ ] Dismiss planning temporarily without being trapped in repeated prompts. **Status: Touches remaining.** Planning invitations now opt into dismissal, Today exposes Dismiss only for eligible prompts, and focused lifecycle tests prove mandatory decisions cannot be dismissed. The installed signed inbox could not refresh its seeded fixture rows, so the final click-through remains (`PromptInbox.swift`; `PromptInboxStore.swift`; `.audit/runs/today-prompt-inbox/verifier/REPORT.md`).
- [x] See clearly that the day is currently unplanned. **Status: Fully implemented.** The installed signed Today surface explicitly rendered `DAY STATE / UNPLANNED`, explained that drift coaching stays off until the user chooses, and kept two available Reminder tasks visible (`DashboardView.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [ ] Start work manually even when the day has not been planned. **Status: Touches remaining.** Each installed Reminder row exposed a stable Start Without Planning control and the XPC journey test proves durable unplanned task start and restart recovery, but this verifier did not click the signed mutation (`TodayDashboardAgent.swift`; `TodayDashboardXPC.swift`; `TodayDashboardAgentTests.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [ ] Avoid drift warnings before approving a plan or explicitly starting unplanned work. **Status: Touches remaining.** The signed surface truthfully stated that drift coaching stays off until a choice, and focused state tests gate interventions until unplanned work starts, while a live generated-drift episode remains unexercised (`PlanningInvitationService.swift`; `PlanningInvitationServiceTests.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).

## 7. Seeing today's available tasks

- [ ] See incomplete Reminders due today. **Status: Touches remaining.** Agent and app fallback now apply the policy timezone and include every incomplete Reminder before the next local midnight, including a focused 23:30 case, while the installed seeded queue remains unverified (`TodayReminderEligibility`; `TodayDashboardAgentTests.swift`; `.audit/runs/today-task-eligibility/candidate/REPORT.md`).
- [ ] See overdue incomplete Reminders. **Status: Touches remaining.** The canonical agent and fallback eligibility rule retain overdue tasks and focused coverage proves the exact queue alongside today, undated, and future cases, while installed seeded visibility remains (`TodayReminderEligibility`; `TodayDashboardAgentTests.swift`; `.audit/runs/today-task-eligibility/candidate/REPORT.md`).
- [ ] See tasks manually selected for today. **Status: Touches remaining.** Added reminders persist as daily-plan entries and render in the proposed plan, with store tests passing, but a complete live add-and-reopen flow was not performed (`AppModel.swift:200-218`; `EventStore.swift:88-155`).
- [ ] See tasks from included Today lists. **Status: Touches remaining.** Identifier-based list inclusion now controls the Reminder inventory used by the app, agent, and first-plan service, and signed-QA exact-ID tests prove included tasks survive filtering. A dedicated named Today-list policy is intentionally not inferred from display names, and the visible native inventory still needs unlocked-Mac proof.
- [ ] Carry a task without a due date into today. **Status: Touches remaining.** Undated tasks explicitly pass the shared eligibility rule and remain in the canonical and fallback available queues; the installed row remains unverified (`TodayReminderEligibility`; `TodayDashboardAgentTests.swift`; `.audit/runs/today-task-eligibility/candidate/REPORT.md`).
- [ ] Avoid seeing future tasks that were not selected for today. **Status: Touches remaining.** Unselected tasks at or after the next policy-local midnight are excluded from both the canonical agent queue and app fallback, while a selected future task remains in planned rows. Focused UTC and Africa/Cairo boundary proof passes; the installed five-row queue and restart traversal remain (`TodayReminderEligibility`; `TodayDashboardAgent.swift`; `DashboardView.swift`; `.audit/runs/today-task-eligibility/candidate/REPORT.md`).
- [ ] Avoid seeing tasks from excluded Reminder lists. **Status: Touches remaining.** Excluded identifiers are removed from the current AppModel session immediately after a durable Settings save and are also excluded before agent synchronization and first-plan planning. Policy-read failures suspend synchronization rather than failing open or deleting durable imports; signed-QA exact-ID and all-excluded tests pass, but native visual proof remains.
- [ ] Avoid seeing duplicate copies of the same Reminder. **Status: Touches remaining.** EventKit identifiers and plan reconciliation prevent one ID from being added twice, but no UI/E2E duplicate-source fixture was verified (`AppModel.swift:202-205`).
- [x] Hide completed tasks from the active task list. **Status: Fully implemented.** The installed signed QA journey completed a real local task through the authenticated helper, showed no copy in Today immediately or after helper and app restarts, and focused proof also enforces immediate exclusion for Reminder-backed tasks while their source write is still pending (`TodayDashboardAgent.swift`; `TodayDashboardAgentTests.swift`; `.audit/runs/completed-task-history/3578132/REPORT.md`).
- [x] Still find completed tasks in today's history. **Status: Fully implemented.** The installed signed QA Reviews screen showed the completed task's readable title, completion time, and local source after Today removed it, then showed the identical row after the app was killed and relaunched (`DailyReviewView.swift`; `TaskHistoryStore.swift`; `.audit/runs/completed-task-history/3578132/REPORT.md`).
- [ ] See recurring Reminder occurrences as separate tasks when appropriate. **Status: Blocked from verification.** EventKit supplies current Reminder entities, but there is no recurrence-specific UI logic or E2E recurring-reminder test demonstrating separate occurrences.
- [x] Understand when Reminders could not be refreshed. **Status: Fully implemented.** The installed signed-QA Settings card visibly changed from Connected to Refresh Failed when task retrieval failed, explained that the last successful sync remained available, retained its exact timestamp, and returned to Connected with a newer timestamp after one explicit retry (`RemindersConnectionController.swift`; `RemindersConnectionView.swift`; `.audit/runs/reminders-recovery/verifier/REPORT.md`).

## 8. Building the daily plan

- [ ] See a suggested main objective. **Status: Touches remaining.** The autonomous planner chooses a capacity-safe main objective and Today renders it prominently, with planner tests passing, but no live populated plan was verified (`AutonomousPlanner.swift`; `DashboardView.swift:918-932`).
- [ ] See three suggested priority tasks. **Status: Touches remaining.** The planner supports a bounded commitment set and the UI targets `3 PROPOSED`, but capacity may yield fewer and no live three-task draft was exercised (`DashboardView.swift:872-952`).
- [ ] Understand why each suggested task matters today. **Status: Touches remaining.** Suggested entries persist and display a selection reason; manually selected items truthfully say no ranking claim, but live reason quality was not verified (`DashboardView.swift:1022-1041`).
- [ ] See the proposed task order. **Status: Touches remaining.** Entries persist rank and render in rank order with numeric labels in the proposal ledger (`DashboardView.swift:868-870,981-1058`).
- [x] See the estimated focused-work total. **Status: Fully implemented.** The installed signed proposal showed 75 focused minutes for 45-minute and 30-minute committed tasks, then updated in place to 60 minutes after the verifier shortened one task to 15 minutes. The complete spoken value and stable accessibility identifier remained present (`PlanningCapacityState.swift`; `DashboardView.swift`; `.audit/runs/plan-preview-metrics/verifier/REPORT.md`).
- [x] See the planned buffer time. **Status: Fully implemented.** The installed signed capacity panel showed 303 buffer minutes from 378 available minus 75 focused minutes, then recalculated in place to 318 after the task update. Overloaded calculations separately expose exact overage rather than a misleading zero buffer (`PlanningCapacityState.swift`; `DashboardView.swift`; `.audit/runs/plan-preview-metrics/verifier/REPORT.md`).
- [ ] See the first recommended task. **Status: Touches remaining.** The populated Today overview has one `DO THIS NEXT` card and recommendation sentence, but no populated live plan was exercised (`TodayDashboardCommandOverview.swift:250-283`).
- [ ] See the proposed gaming unlock condition. **Status: Partially implemented.** The Today behavior panel shows current gaming budget and next unlock reason, but this is not a proposed-plan unlock condition users can review (`TodayDashboardCommandOverview.swift:172-191`).
- [ ] Add an available task to the plan. **Status: Touches remaining.** Inbox rows call `addToDailyPlan`, enforce uniqueness and maximum five, and persist through the agent, but the installed add flow was not exercised (`AppModel.swift:200-218`).
- [ ] Remove a task from the plan. **Status: Touches remaining.** `EXCLUDE` and `REMOVE` update and persist the plan, with fallback restoration on failure, but the live flow was not exercised (`DashboardView.swift:1044-1051`; `AppModel.swift:220-245`).
- [x] Reorder priority tasks. **Status: Fully implemented.** The installed signed Today surface moved the second task to rank 1 through the agent, immediately updated both move-control boundaries, stored the exact new rank order, and restored it after relaunch (`.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/evidence.json`; `.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/REPORT.md`).
- [ ] Choose a different main objective. **Status: Touches remaining.** `ADJUST: MAKE MAIN` updates exactly one main objective and persists it, but the installed interaction was not exercised (`DashboardView.swift:1044-1049`; `AppModel.swift:247-263`).
- [x] Mark a task optional. **Status: Fully implemented.** The installed signed Today surface marked a task optional, displayed that it was excluded from capacity and Calendar, reduced committed capacity immediately, persisted through the agent, and restored the exact state after relaunch (`.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/evidence.json`; `.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/REPORT.md`).
- [x] Mark a task blocked and explain why. **Status: Fully implemented.** The installed signed Today flow required a 3-to-240-character reason, atomically stored the reason with the blocked execution state, displayed the complete reason, and restored both after relaunch (`.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/evidence.json`; `.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/REPORT.md`).
- [x] Reduce the day's scope. **Status: Fully implemented.** The installed signed Today journey made one task optional and deferred another to tomorrow, visibly reduced committed capacity from 90 to 30 minutes, excluded both from Calendar scheduling, and restored the reduced scope after relaunch (`.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/evidence.json`; `.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/REPORT.md`).
- [x] Change the gaming unlock condition. **Status: Fully implemented.** The installed signed Today journey identified the exact task owning the locked 15-minute reward, exposed `MAKE MAIN + GAMING UNLOCK` with consequence-first help on the other task, kept Cancel mutation-free, moved the single main objective and unlock condition immediately after confirmation, and restored the same ownership after relaunch. Completing the former main produced no reward, completing the newly selected main produced exactly one 15-minute ledger row, and a final relaunch retained one reward with truthful Earned and Locked totals (`TodayDashboardCommandOverview.swift`; `GamingUnlockConditionPresentationTests.swift`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/REPORT.md`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/evidence/09-immediate-fixed.png`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/evidence/10-relaunch-fixed.png`).
- [x] Approve the proposed plan. **Status: Fully implemented.** The installed signed-QA journey reviewed the exact task and capacity, refused both a cancelled commitment and denied Calendar access with `NOTHING WAS WRITTEN`, preserved the plan with zero commands, refreshed to current availability, applied four exact Calendar and Reminder commands after repair, and restored the confirmed receipt after relaunch (`.audit/runs/calendar-approval-freshness/2f8b7156f6c5c22c80363c5eb611f433dc72bbbf/evidence.json`; `.audit/runs/calendar-approval-freshness/2f8b7156f6c5c22c80363c5eb611f433dc72bbbf/REPORT.md`).
- [ ] Restart the app and see the approved plan restored correctly. **Status: Touches remaining.** The receipt now durably stores exact reviewed tasks, order, estimates, capacity basis, approval time, command identities, and outcome without reopening or reissuing on restore, with focused persistence proof. The isolated signed run could not create a pending receipt because Calendar fixture access was disconnected, so pending and confirmed relaunch comparison remains (`CalendarPlanApprovalReceiptStore.swift`; `CalendarPlanApprovalStateTests.swift`; `.audit/runs/calendar-approval-receipt/verifier/REPORT.md`).

## 9. Planning an unrealistic workload

- [ ] See a clear warning when planned work exceeds available focus capacity. **Status: Touches remaining.** The live Today surface now renders a dedicated capacity panel in both snapshot and fallback states, and focused tests prove exact overage classification, but the signed seeded overload could not be completed after the helper snapshot failed to refresh seeded Reminders (`PlanningCapacityState.swift`; `DashboardView.swift`; `.audit/runs/planning-capacity/capacity-warning/REPORT.md`).
- [ ] See the selected workload and available capacity in the same warning. **Status: Touches remaining.** The signed app visibly showed `0 MIN PLANNED / 126 MIN AVAILABLE` after merging overlapping external commitments and ignoring a Zoid-owned block, while the populated-workload installed state remains unproven (`AppModel.swift`; `PlanningCapacityState.swift`; `.audit/runs/planning-capacity/capacity-warning/REPORT.md`).
- [ ] Receive a concrete suggestion about what to remove, defer, or make optional. **Status: Touches remaining.** The overload state names the lowest-ranked task and explains removal, estimate reduction, or exclusion, with focused rule coverage, but the signed helper refresh blocker prevented visible populated-plan proof (`PlanningCapacityState.swift`; `DashboardView.swift`; `.audit/runs/planning-capacity/capacity-warning/REPORT.md`).
- [ ] Reduce the plan directly from the warning. **Status: Touches remaining.** A stable accessible Remove Suggested Task action removes the exact suggested entry and immediately persists the revised plan, but the installed click-through remains outstanding (`AppModel.swift`; `DashboardView.swift`; `.audit/runs/planning-capacity/capacity-warning/REPORT.md`).
- [ ] Change an estimate and see capacity update immediately. **Status: Touches remaining.** Capacity derives directly from published plan state, the Calendar adjustment is now published, and focused tests prove overloaded-to-realistic recalculation after estimate changes; visible signed interaction remains outstanding (`AppModel.swift`; `PlanningCapacityStateTests.swift`).
- [ ] Add or remove a task and see capacity update immediately. **Status: Touches remaining.** Add, remove, and direct reduction mutate published plan state and therefore recalculate the live panel immediately, but stale helper snapshot inventory prevented the final installed add-remove sequence (`AppModel.swift`; `DashboardView.swift`; `.audit/runs/planning-capacity/capacity-warning/REPORT.md`).
- [x] Approve a realistic revised plan. **Status: Fully implemented.** The installed signed-QA journey created and reviewed a 30-minute plan against current Calendar capacity, refreshed the approval after availability changed, applied all four exact commands after Calendar repair, and restored the confirmed plan and receipt after relaunch (`.audit/runs/calendar-approval-freshness/2f8b7156f6c5c22c80363c5eb611f433dc72bbbf/evidence.json`; `.audit/runs/calendar-approval-freshness/2f8b7156f6c5c22c80363c5eb611f433dc72bbbf/REPORT.md`).
- [x] Avoid receiving a vague warning with no useful next action. **Status: Fully implemented.** The installed approval named the cancelled-commitment and unavailable-Calendar failures, guaranteed that nothing was written, offered direct availability refresh and Source Health actions, and reached visible Calendar impact and repair guidance while preserving the plan (`.audit/runs/calendar-approval-freshness/2f8b7156f6c5c22c80363c5eb611f433dc72bbbf/evidence.json`; `.audit/runs/calendar-approval-freshness/2f8b7156f6c5c22c80363c5eb611f433dc72bbbf/REPORT.md`).

## 10. Skipping planning

- [ ] Skip planning explicitly. **Status: Touches remaining.** The signed invitation exposed Work Unplanned, the XPC command persists the explicit choice, and focused tests prove restart-safe state, but the verifier did not click this second signed mutation (`AppModel.swift`; `TodayDashboardXPC.swift`; `TodayDashboardAgentTests.swift`).
- [ ] Understand that Zoid 666 is now in limited unplanned mode. **Status: Touches remaining.** The implemented limited-mode banner explains the reduced contract and focused persistence tests prove its state, while the exact post-click signed screen remains unclaimed (`DashboardView.swift`; `PlanningInvitationServiceTests.swift`).
- [x] See overdue and available tasks during an unplanned day. **Status: Fully implemented.** Before any plan existed, the installed signed Today screen labelled the day unplanned and rendered two available Work reminders with independent actions (`DashboardView.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [ ] Start any available task during an unplanned day. **Status: Touches remaining.** The installed signed app exposed Start Without Planning for both available reminders and the agent journey proves durable XPC mutation and restart recovery, while the final signed click remains unclaimed (`TodayDashboardAgent.swift`; `TodayDashboardXPC.swift`; `TodayDashboardAgentTests.swift`).
- [ ] See behavior totals without being told that behavior violated a nonexistent plan. **Status: Touches remaining.** The unplanned signed screen retained behavior totals and explicitly suppressed pre-choice drift coaching, while the limited-mode post-click wording remains covered by focused state tests rather than a completed signed mutation (`DashboardView.swift`; `PlanningInvitationServiceTests.swift`).
- [ ] Return to planning later in the same day. **Status: Touches remaining.** Plan Now remained enabled on both the initial unplanned and restart-safe snoozed screens, and backend planning remains available, while a signed skip-then-plan mutation sequence was not completed (`DashboardView.swift`; `.audit/runs/morning-planning/27ad4f34c23d4a77e36ae3f065e9c70cda7f6f4d/REPORT.md`).
- [ ] End and review an unplanned day without invented planned outcomes. **Status: Frontend only left.** Behavior and history stores can represent activity without a plan, but there is no end-day control or unplanned-day review UI.

## 11. Adding task estimates

- [ ] Choose a 15-minute estimate. **Status: Touches remaining.** Both proposal and Today estimate selectors offer 15 and persist it, but the live click-and-reload flow was not exercised (`DashboardView.swift:1230-1305`; `TodayDashboardCommandOverview.swift:750-779`).
- [ ] Choose a 30-minute estimate. **Status: Touches remaining.** Both selectors offer 30 and use the same persisted update path, without live E2E interaction.
- [ ] Choose a 45-minute estimate. **Status: Touches remaining.** Both selectors offer 45 and use the same persisted update path, without live E2E interaction.
- [ ] Choose a 60-minute estimate. **Status: Touches remaining.** Both selectors offer 60 and use the same persisted update path, without live E2E interaction.
- [ ] Choose a 90-minute estimate. **Status: Touches remaining.** Both selectors offer 90 and use the same persisted update path, without live E2E interaction.
- [ ] Enter a custom estimate. **Status: Touches remaining.** Every plan task estimate selector now offers a per-task `CUSTOM` editor, supports Return or Save, uses the existing durable estimate callback, and supports Cancel or Escape without changing the plan. Focused callback-dependent capacity tests pass, while signed installed save and restart proof remains (`TaskEstimateInput.swift`; `DashboardView.swift`; `.audit/runs/custom-task-estimate/verifier/REPORT.md`).
- [ ] Receive a clear error for an empty, zero, negative, or malformed estimate. **Status: Touches remaining.** Empty, zero, negative, decimal, text, and values above 480 minutes remain in the editor with specific corrective copy and per-task accessibility identifiers. Focused validation passes; installed invalid-input matrix proof remains (`TaskEstimateInput.swift`; `TaskEstimateInputTests.swift`; `.audit/runs/custom-task-estimate/verifier/REPORT.md`).
- [x] Select `Unknown` when an estimate cannot be made confidently. **Status: Fully implemented.** The installed signed app exposed accessible Unknown controls on both Today task representations, persisted the explicit choice across app and helper relaunch, and cleared it when 30 minutes was selected (`AppModel.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/explicit-unknown-estimates/verifier/REPORT.md`).
- [x] See the conservative placeholder assigned to an unknown estimate. **Status: Fully implemented.** Selecting Unknown changed capacity from zero to exactly 45 planned minutes, showed an approximately 45-minute placeholder in Today and Calendar approval, and retained the placeholder in the durable approval receipt after relaunch (`PlanningCapacityState.swift`; `CalendarPlanApprovalState.swift`; `.audit/runs/explicit-unknown-estimates/verifier/REPORT.md`).
- [x] Understand that the placeholder is uncertain. **Status: Fully implemented.** Today rendered `UNKNOWN`, `PLACEHOLDER`, and `UNCERTAIN`, while Calendar approval visibly labeled the same task `UNCERTAIN PLACEHOLDER` rather than presenting 45 minutes as a confident estimate (`CalendarPlanApprovalSheet.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/explicit-unknown-estimates/verifier/REPORT.md`).
- [ ] Change an estimate before starting the task. **Status: Touches remaining.** A selected estimate can be reopened with the pencil control or clicked in Today and persisted before task start (`DashboardView.swift:1246-1296`).
- [x] See that every priority task needs an estimate or explicit `Unknown` choice before approval. **Status: Fully implemented.** The untouched missing task showed `ESTIMATE NEEDED`, zero planned minutes, one remaining estimate, and disabled approval; explicit Unknown enabled approval only after contributing its transparent 45-minute placeholder (`PlanningCapacityState.swift`; `DashboardView.swift`; `.audit/runs/explicit-unknown-estimates/verifier/REPORT.md`).

## 12. Learning from previous estimates

- [x] See a suggested estimate based on similar completed tasks when enough history exists. **Status: Fully implemented.** The installed signed Today view showed no suggestion with three eligible matching samples, then displayed a 50-minute learned estimate after the fourth sample and one app/helper relaunch (`TodayDashboardAgent.swift`; `LearnedEstimateSuggestionView.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [x] See how many similar tasks support the suggestion. **Status: Fully implemented.** The signed learned-estimate card explicitly stated that four similar completed tasks supported the recommendation (`LearnedEstimateSuggestionView.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [x] See the historical duration range behind the suggestion. **Status: Fully implemented.** The signed card exposed the exact aligned-work evidence range of 40 to 55 minutes from the aggregate's immutable sample identifiers (`LearningAggregateStore.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [x] Understand when the evidence is uncertain. **Status: Fully implemented.** The signed four-sample card labeled the evidence `EARLY PATTERN`, named its sample count and range, and described the recommendation as advisory (`LearnedEstimateSuggestionView.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [ ] Accept the suggested estimate. **Status: Touches remaining.** Today now exposes an accessible Use action through the existing durable estimate mutation path, and focused tests prove explicit presentation semantics, but the capped signed run stopped before Use and relaunch durability (`LearnedEstimateSuggestionView.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [ ] Keep the original estimate instead. **Status: Touches remaining.** Today exposes an accessible Keep action with direct confirmation and no plan mutation, but the capped signed run stopped before Keep and relaunch verification (`LearnedEstimateSuggestionView.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [ ] Enter a different estimate. **Status: Touches remaining.** Every Today estimate strip now offers validated custom minutes with Save, Return, Cancel, and corrective copy, but invalid and valid signed input branches remain (`TaskEstimateInput.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [ ] Avoid receiving a confident suggestion when tracking coverage or sample size is insufficient. **Status: Touches remaining.** The installed app visibly suppressed advice at three eligible samples and surfaced it at four, while focused policy coverage also rejects low tracking coverage; the low-coverage signed branch remains (`EstimateLearningPolicy`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).
- [x] Avoid having an advisory estimate silently replace the user's estimate. **Status: Fully implemented.** The signed four-sample recommendation remained advisory while the task stayed visibly selected at its original 30 minutes before any explicit choice (`TodayDashboardAgent.swift`; `.audit/runs/learned-estimate-suggestions/verifier/REPORT.md`).

## 13. Understanding the Today dashboard

- [ ] See the current date and day state at a glance. **Status: Partially implemented.** A populated snapshot shows the full date, while the empty installed state omits it and neither surface names a formal day state (`TodayDashboardCommandOverview.swift:23-43`; runtime screenshot).
- [ ] See whether coaching is active or paused. **Status: Partially implemented.** Settings header clearly shows automation Running or Paused, but Today does not show coaching state and `AppModel.coachingState` is not rendered there (`SettingsView.swift:223-236`).
- [ ] See whether connected data sources are healthy. **Status: Blocked from verification.** Today and Source health contain the ledger and repair controls, but the independent `a068d27` verifier had no current main-window accessibility proof (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] See the main objective as the strongest task-level element. **Status: Blocked from verification.** Source styling makes the main objective visually prominent, but the independent `a068d27` verifier could not capture and assess the current packaged UI (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] See the active task or recommended next task prominently. **Status: Blocked from verification.** The focus and `DO THIS NEXT` cards exist in source, but the independent `a068d27` verifier had no isolated current UI state or visual proof (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] See the top three before remaining tasks. **Status: Touches remaining.** Planned rows precede the full unplanned inventory, but the product does not enforce exactly three and can hold up to five (`DashboardView.swift:864-979,215-274`; `AppModel.swift:202-204`).
- [x] See work, gaming, distraction, idle, and unknown totals. **Status: Fully implemented.** The signed Today sheet simultaneously exposed five separate accessible totals with the seeded exact values Work 5 minutes, Gaming 4 minutes, Distraction 3 minutes, Idle observed 2 minutes, and Unknown 1 minute. The sheet is available from a persistent native View All Activity button rather than a hover-only surface (`BehaviorEvidenceState.swift`; `BehaviorEvidenceSheet.swift`; `.audit/runs/today-behavior-evidence/verifier/REPORT.md`).
- [ ] See gaming budget status. **Status: Blocked from verification.** Budget calculation tests pass and the Today surface exists, but the independent `a068d27` verifier could not capture the current packaged UI or seed controlled budget states (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] See recent coach decisions. **Status: Touches remaining.** Today now has a bounded Recent decisions section for answered, dismissed, and expired prompts with state and resolution time. The installed signed inbox rendered but could not refresh seeded history, and behavior-coaching production remains incomplete (`PromptInboxTimeline.swift`; `DashboardView.swift`; `.audit/runs/today-prompt-inbox/verifier/REPORT.md`).
- [x] End the workday from the dashboard. **Status: Fully implemented.** The signed Today surface hid the action without an active task, then exposed a fixed accessible end-workday control for the exact active task with explicit preserved-time and non-completion copy. Cancel kept the task active, a deliberately stale confirmation performed no second mutation, and a stopped helper produced an honest failure while remaining on Today. After signed helper recovery, confirmation paused the task with the end-of-workday reason and opened Reviews only after the durable agent response; Reviews kept the task unfinished with no completion record, and app plus helper restart restored three tracked minutes, the end-of-workday pause reason, and Resume (`DashboardEndWorkdayFlow.swift`; `DashboardView.swift`; `DashboardEndWorkdayFlowTests.swift`; `.audit/runs/dashboard-end-workday/848dd365f1f2c751f64f8cecdb5fef748f2b8702/evidence.json`).
- [ ] Understand the dashboard even when some data is missing. **Status: Touches remaining.** Empty plan, missing source, limited coverage, and loading states have written explanations, though the installed dashboard stayed on `LOADING PLAN` and did not expose why it had not resolved.
- [ ] Distinguish missing information from a real zero value. **Status: Touches remaining.** Coverage explanations distinguish limited telemetry from observed values, but empty-plan/loading states still mix absence with zero-like counts (`TodayDashboard.swift:196-205`; `TodayDashboardCommandOverview.swift:160-163`).

## 14. Main objective and task list

- [ ] See the main objective's title, deadline, estimate, and status. **Status: Partially implemented.** The focus card shows title, estimate, deadline, and urgency, while status is implied by the command/heading rather than explicitly labelled (`TodayDashboardCommandOverview.swift:57-105`).
- [ ] Understand why it is the main objective. **Status: Touches remaining.** Suggested plan entries can show selection evidence and the next-reason text identifies the main objective, but manual plans only say no ranking claim (`DashboardView.swift:1022-1041`; `TodayDashboardCommandOverview.swift:344-347`).
- [ ] Start or resume it directly from its card. **Status: Touches remaining.** The primary focus card maps ready to Start and paused to Resume through XPC-backed task commands, but this installed-app action and persisted effect were not exercised end to end (`TodayDashboardCommandOverview.swift:95-98,368-374`).
- [ ] See each task's rank, title, list or project, estimate, deadline, urgency, and status. **Status: Partially implemented.** Proposal rows show numeric rank, title, estimate, and evidence, while active Today rows show deadline, urgency, and status; neither unified row includes all fields and planned rows omit list/project (`DashboardView.swift:981-1058`; `TodayDashboardCommandOverview.swift:662-748`).
- [ ] Complete a task from its row. **Status: Touches remaining.** Active rows expose Complete and the agent queues Reminder completion, with passing store tests, but a live Reminder completion was not exercised (`DashboardView.swift:650-659`; `TodayDashboardAgent.swift:120-127`).
- [ ] Start a task from its row. **Status: Touches remaining.** Ready rows expose Start and task execution tests pass, but the installed app had no runnable planned row during verification (`DashboardView.swift:656-659`).
- [ ] Identify blocked, deferred, paused, active, and completed tasks visually. **Status: Touches remaining.** Default Today now visibly and accessibly labels exact blocker reasons, future deferrals, optional exclusions, paused reasons, active work, and completed states; the signed revision journey directly proved blocked and deferred labels across relaunch, while one installed all-state sequence remains (`.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/evidence.json`; `.audit/runs/daily-plan-revision/c19e379c7beff03feda1d4655438ebc2988faf61/REPORT.md`).
- [ ] Read long task titles without losing access to controls. **Status: Touches remaining.** Task titles wrap to two lines with layout priority and controls remain separate, but longer-than-two-line truncation was not visually tested (`TodayDashboardCommandOverview.swift:681-701`).
- [ ] See task information clearly with larger text enabled. **Status: Blocked from verification.** SwiftUI semantic fonts and adaptive layouts are present, but no larger-text accessibility runtime pass or screenshot was completed.
- [ ] Find remaining non-priority tasks without letting them dominate the plan. **Status: Blocked from verification.** The inventory is positioned below priority surfaces in source, but the independent `a068d27` verifier had no current packaged UI hierarchy proof (`.audit/runs/baseline/a068d27/REPORT.md`).

## 15. Receiving a next-task recommendation

- [ ] See one recommended next task rather than a list of competing recommendations. **Status: Blocked from verification.** `TodaySnapshot` and the Today source model one card, but the independent `a068d27` verifier did not run the current packaged recommendation journey (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] See one concise reason for the recommendation. **Status: Blocked from verification.** The recommendation explanation exists in source, but the independent `a068d27` verifier had no current packaged visual or accessibility proof of its clarity (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] Receive recommendations that prefer today's selected tasks. **Status: Blocked from verification.** Recommender tests cover selected daily rows, but the independent `a068d27` verifier could not prove the complete packaged UI and persisted recommendation effect (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] Receive a bounded sprint recommendation when an important task is too large for the available time. **Status: Touches remaining.** The signed Today journey used an exact 20-minute capacity, rendered one truthful accessible 20-minute sprint action for a 90-minute task, started it, preserved its countdown through app and helper restart, left the task active and incomplete after expiry, exposed Continue Open-Ended, and suppressed the sprint at zero capacity. The fitting 15-minute task branch passed focused rule coverage but was not reseeded in the signed interface before the strict UI cap ended (`RecommendationSprintPresentation`; `.audit/runs/recommended-bounded-sprint/verifier/REPORT.md`).
- [ ] Start the recommendation immediately. **Status: Touches remaining.** A ready recommended row has a direct Begin/Start control routed to `.start`, but the installed-app action and persisted result were not exercised end to end (`TodayDashboardCommandOverview.swift:279-282,368-374`).
- [x] Choose `Not now`. **Status: Fully implemented.** The signed Today card visibly offered the accessible Not Now action beside the recommended task, saved it through the live agent, showed exact 30-minute confirmation copy, selected a different ready task, and retained that alternate through both app and helper restart. Focused tests prove the original task becomes eligible again after thirty minutes (`RecommendationFeedbackStore.swift`; `.audit/runs/next-task-recommendation-feedback/verifier/REPORT.md`).
- [ ] Say the recommendation has the wrong priority. **Status: Touches remaining.** Today now exposes a stable Wrong Priority action that durably suppresses the task for the policy-local day and selects an alternate across restart in focused tests. The signed app visibly exposed its label and hint, and a stopped-helper attempt truthfully retained the recommendation with Source Health recovery copy, while successful signed activation and next-day release remain (`RecommendationFeedbackStore.swift`; `.audit/runs/next-task-recommendation-feedback/verifier/REPORT.md`).
- [ ] Say the task is too large. **Status: Touches remaining.** Today now exposes a stable Too Large action that records the exact feedback without inventing a split or changing the task, estimate, plan, or completion state, and focused tests prove same-day alternate selection plus next-day release. The signed app visibly exposed the control and explanatory hint, while successful signed activation remains (`RecommendationFeedbackStore.swift`; `.audit/runs/next-task-recommendation-feedback/verifier/REPORT.md`).
- [ ] Mark the task blocked. **Status: Partially implemented.** The task can be blocked from its row's More menu, but not directly from the recommendation card and no reason can be entered (`DashboardView.swift:660-670`).
- [ ] Mark the task already done. **Status: Partially implemented.** An active task can be completed, but a ready recommendation cannot be marked already done directly and completion implies a tracked session.
- [ ] Hide the task for today. **Status: Partially implemented.** Removing a plan entry excludes it from Today, but there is no recommendation-specific Hide for today action or durable hidden state.
- [ ] See a new recommendation after giving feedback. **Status: Partially implemented.** Starting, completing, or blocking through task commands refreshes the snapshot and recommendation, but the requested feedback controls are absent (`AppModel.swift:136-145`; `TodayDashboardAgent.swift:112-168`).
- [ ] Avoid receiving completed, cancelled, deleted, or unusably blocked tasks as recommendations. **Status: Touches remaining.** Completed and blocked states are excluded by passing recommender tests and missing Reminder IDs disappear from rows, but cancelled/deleted live-source cases were not exercised (`NextTaskRecommender`; `TodayDashboardAgentTests`).

## 16. Starting a task

- [ ] Start a task from the Today dashboard. **Status: Touches remaining.** Ready task rows and the focus recommendation issue serialized `.start` commands through the agent, return an updated snapshot, and expose clear pending, success, and failure states. The signed-QA package and deterministic agent journey pass, but the Mac was locked during independent verification, so the installed click-through remains (`AppModel.swift`; `DashboardView.swift`; `TodayDashboardCommandOverview.swift`; `TodayDashboardAgentTests`).
- [ ] Start a task from the menu bar. **Status: Touches remaining.** The menu-bar recommendation now exposes a task-specific Start action that fetches the latest snapshot before mutation, rejects stale, active, or paused state, prevents overlapping activation, requires the exact task to return active, preserves the last confirmed state on failure, and refreshes Today after the attempt. Fourteen focused menu tests pass, including canonical-agent persistence, and the signed installed app created the exact ready recommendation used for acceptance. The separate SwiftUI MenuBarExtra remained outside the available Computer Use accessibility tree despite direct SystemUIServer and keyboard-focus attempts, so the current, stale, double-click, helper-failure, exact single-interval, and restart journeys remain unclicked in the signed UI (`MenuBarCoachView.swift`; `.audit/runs/menu-bar-start-task/verifier/REPORT.md`).
- [ ] Start a task from a dashboard prompt. **Status: Touches remaining.** The dashboard action routes the task start exactly once through the canonical prompt effect boundary, refreshes Today, and shows and announces `<task> is active in Today.` only when the refreshed active task matches the prompt payload. Focused tests prove dashboard and notification replay idempotency, one active interval, safe title fallback, and no confirmation for stale, mismatched, or unrelated actions. The signed ready-state fixture has no supported prompt seed seam, and a direct isolated seed was rejected by the decoder, so native prompt activation, replay, and relaunch persistence remain (`AppModel.swift`; `DashboardPromptActionOutcome.swift`; `TodayPromptInboxLedger.swift`; `.audit/runs/dashboard-prompt-task-start/verifier/REPORT.md`).
- [ ] Start a task from the recommendation card. **Status: Touches remaining.** The recommendation card routes a ready row through `.start`, but the installed-app action and persisted result were not exercised end to end during this audit (`TodayDashboardCommandOverview.swift:279-282,368-374`).
- [ ] Start a task with a keyboard shortcut. **Status: Touches remaining.** The signed Task menu named the exact Ready recommendation, displayed the Command-Option-S binding, started it through the same menu command exactly once, then disabled Start while the task was active. Focused tests cover the shortcut state and canonical command path, but the automation driver did not deliver the physical Command-Option-S chord, so direct keystroke activation remains (`TaskKeyboardCommands.swift`; `.audit/runs/task-keyboard-controls/verifier/REPORT.md`).
- [ ] See the task become the single active task everywhere. **Status: Partially implemented.** Backend execution enforces one open interval and Today updates to the active row, but the menu bar does not show task state and no cross-surface live test exists (`TaskExecutionStore.swift:35-46`; `TodayDashboardAgent.swift:60-61`).
- [ ] See the previous task pause automatically when starting another task. **Status: Touches remaining.** Both Today task surfaces now show an explicit switch confirmation explaining that the current task will pause and preserve its time. The transactional command records `switchingTasks` atomically, but the signed-QA switch click-through remains because the Mac was locked (`TodayDashboardCommandOverview.swift`; `DashboardView.swift`; `TaskExecutionStoreTests`).
- [ ] Avoid seeing two active tasks after clicking twice or using two surfaces. **Status: Touches remaining.** App-wide task-command serialization disables every task action while one command is pending, while SQLite permits only one open activity interval and atomically switches tasks. Deterministic coverage passes, but multi-surface rapid-click behavior still needs installed UI automation (`AppModel.swift`; `DashboardView.swift`; `TodayDashboardCommandOverview.swift`; `TaskExecutionStore.swift`).
- [ ] See active-task status appear in the dashboard and menu bar. **Status: Partially implemented.** Dashboard rows show Active and switch commands, while the menu bar remains voice-only (`DashboardView.swift:644-659`; `ZoidCoachApp.swift:51-55`).
- [ ] Restart Zoid 666 and continue the same active session without duplicated time. **Status: Touches remaining.** Open intervals, elapsed minutes, and pause episodes persist in migration 29. Store and agent reconstruction tests prove that elapsed time survives pause, resume, switch, and restart without double counting, but the installed start-restart-clock click-through remains (`TaskExecutionStore.swift`; `TaskExecutionStoreTests`; `TodayDashboardAgentTests`).

## 17. Choosing a work commitment

- [ ] Start an open-ended work session. **Status: Touches remaining.** Today and the menu bar now name the shared manual timing contract as `OPEN-ENDED SESSION`, explain that tracking continues until Pause or Complete, and focused canonical-agent tests prove restart-safe state. The signed package and exact helper passed, but the isolated Reminder fixture entered read-only safety mode before a task could be started visibly, so the installed start and relaunch journey remains (`ActiveCommitmentPresentation.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/active-commitment-visibility/verifier/REPORT.md`).
- [ ] Start a 10-minute recovery sprint. **Status: Touches remaining.** The signed QA Today surface visibly offered the preset, started it through the installed helper, changed the task to Active, and showed a live `9 minutes and 54 seconds remaining` countdown; the longer pause, expiry, and completion journey remains (`.audit/runs/bounded-sprints/verifier/REPORT.md`).
- [ ] Start a 20-minute work sprint. **Status: Touches remaining.** Today exposes the distinct preset and the same durable XPC/store lifecycle is covered by focused tests, but the installed 20-minute selection was not clicked (`TaskExecutionStoreTests`; `.audit/runs/bounded-sprints/verifier/REPORT.md`).
- [ ] Start a 25-minute focus sprint. **Status: Touches remaining.** Today exposes the distinct preset and the same durable XPC/store lifecycle is covered by focused tests, but the installed 25-minute selection was not clicked (`TaskExecutionStoreTests`; `.audit/runs/bounded-sprints/verifier/REPORT.md`).
- [ ] Enter a custom sprint duration. **Status: Touches remaining.** A labelled custom-duration sheet validates whole minutes from 1 through 240 and routes the chosen value through XPC to migration-32 storage; focused tests pass, but the installed custom sheet was not completed (`TodayDashboardCommandOverview.swift`; `TaskExecutionStoreTests`).
- [ ] Start a block matching the full task estimate. **Status: Partially implemented.** Starting work and selecting an estimate both exist, but start always opens an unbounded interval and does not attach the estimate as a timer boundary.
- [ ] See elapsed time for an open-ended session. **Status: Frontend only left.** The snapshot carries `elapsedMinutes`, but current Today task rows and focus card do not render it (`TodayDashboard.swift:37-58`; `TodayDashboardCommandOverview.swift:57-105,662-748`).
- [ ] See remaining time for a bounded sprint. **Status: Touches remaining.** The signed installed Today surface visibly counted down once per second and exposed an accessibility remaining-time announcement; relaunch and boundary acceptance remain (`.audit/runs/bounded-sprints/verifier/REPORT.md`).
- [ ] Reach the end of a sprint without having the task completed automatically. **Status: Touches remaining.** Store and agent restart tests prove expiry leaves the task Active and incomplete, and the UI explains this before start and during countdown; installed expiry was not awaited (`TaskExecutionStoreTests`; `TodayDashboardAgentTests`).
- [ ] Continue working after a sprint ends. **Status: Touches remaining.** The expired panel exposes `CONTINUE OPEN-ENDED`, while the backend now rejects early or missing-sprint continuation and preserves duplicate idempotence; the installed expiry action was not clicked (`TaskExecutionStore.swift`; `TaskExecutionStoreTests`).
- [ ] Pause and later resume a bounded sprint with understandable timing. **Status: Touches remaining.** Migration-32 storage preserves accumulated active seconds across pause, restart, and resume, and Today exposes the normal labelled pause and resume controls; the installed pause-relaunch-resume sequence remains (`TaskExecutionStoreTests`; `.audit/runs/bounded-sprints/verifier/REPORT.md`).

## 18. Active-task controls

- [x] See the active task title and elapsed time. **Status: Fully implemented.** The signed installed Today journey visibly started `Verify estimate progress`, showed its title with tracked minutes and a stable accessibility summary, paused it for a break, relaunched the app, and recovered the same canonical paused state. Active rows advance minute by minute while paused rows remain stable (`TaskEstimateProgressView.swift`; `TaskEstimateProgressTests.swift`; `.audit/runs/task-estimate-progress/verifier/REPORT.md`).
- [ ] See sprint time remaining when applicable. **Status: Touches remaining.** The signed installed active-task card visibly showed a live remaining-time countdown with a dedicated accessibility status container; relaunch and expiry states remain to be walked (`.audit/runs/bounded-sprints/verifier/REPORT.md`).
- [x] See progress against the estimate when meaningful. **Status: Fully implemented.** Active and paused focus and task-row surfaces show tracked minutes, the estimate, bounded progress, remaining or over-estimate copy, and one complete accessibility summary. The signed installed journey visibly moved from `0 minutes tracked of 5 estimated, not started` to the persisted paused state `7 minutes tracked of 5 estimated, 2 min over estimate`, while seven focused tests cover every boundary and restart recovery (`TaskEstimateProgress.swift`; `TaskEstimateProgressView.swift`; `.audit/runs/task-estimate-progress/verifier/REPORT.md`).
- [x] See whether the current computer context appears aligned, uncertain, or mismatched. **Status: Fully implemented.** The signed active focus card visibly moved from missing-evidence uncertain, to fresh-work aligned, to fresh-gaming mismatched, and back to stale-evidence uncertain. Relaunch preserved the same active task and reconstructed its assessment from durable observations, while focused rules cover inactive suppression, idle, and unclassified evidence (`ActiveTaskContextAssessor`; `TodayDashboardCommandOverview.swift`; `.audit/runs/active-task-context/47527ac4840a67bbb3b618bc6e09f27942404f85/evidence.json`).
- [x] Read neutral alignment language rather than judgmental productivity labels. **Status: Fully implemented.** The signed aligned state explicitly said the evidence was `a signal, not proof`, the mismatched state asked the user to check context before changing the plan, and missing or stale evidence stated that Zoid 666 would not guess. Focused tests reject judgmental productivity wording and preserve neutral uncertain states (`ActiveTaskContextAssessor`; `.audit/runs/active-task-context/47527ac4840a67bbb3b618bc6e09f27942404f85/evidence.json`).
- [x] Pause the task. **Status: Fully implemented.** The signed Task menu changed from Pause Current Task: Keyboard Focus Task to a paused Today state with the neutral Done for now reason. The durable pause closed the first 21-second activity interval, opened one pause event, preserved elapsed work, and kept Start disabled (`TaskKeyboardCommands.swift`; `TaskExecutionStore.swift`; `.audit/runs/task-keyboard-controls/verifier/REPORT.md`).
- [x] Resume the task. **Status: Fully implemented.** The signed paused Task menu exposed Resume Paused Task: Keyboard Focus Task, resumed the same task, closed the pause event, opened exactly one new active interval, and restored the same single active task after application relaunch. Focused agent-restart and atomic single-active tests also passed (`TaskKeyboardCommands.swift`; `TaskExecutionStore.swift`; `.audit/runs/task-keyboard-controls/verifier/REPORT.md`).
- [ ] Complete the task. **Status: Touches remaining.** The live active card exposes `COMPLETE FOCUS`, and the agent records completion and enqueues an Apple Reminders completion action. The UI does not wait for or display source confirmation, and this destructive action was not performed on the user's real Reminder.
- [ ] Mark the task blocked. **Status: Touches remaining.** The focus-card pause menu and task-row overflow expose `Task is blocked`, persist an exact blocked pause reason and blocked task state, and return an updated snapshot. Contextual replanning and installed click-through proof remain (`TodayDashboardCommandOverview.swift`; `DashboardView.swift`; `TaskExecutionStore.swift`).
- [ ] Switch to another task. **Status: Touches remaining.** Both Today task surfaces present an explicit confirmation naming the selected task and explaining that tracked time will be preserved. One transaction pauses the current task with `switchingTasks` and starts the next, with store and agent restart proof; installed UI acceptance remains (`TodayDashboardCommandOverview.swift`; `DashboardView.swift`; `TaskExecutionStoreTests`; `TodayDashboardAgentTests`).
- [ ] See every active-task surface update promptly after an action. **Status: Partially implemented.** Every Today task command returns and installs a fresh snapshot, while global command serialization keeps the focus card and task rows from racing and errors preserve the last confirmed state. The voice-only menu bar still lacks task parity, and installed cross-surface proof remains (`AppModel.swift`; `DashboardView.swift`; `TodayDashboardCommandOverview.swift`).

## 19. Pausing and switching

- [ ] Pause for a break. **Status: Touches remaining.** Both active-task surfaces expose `Take a break`, persist the exact break reason in an append-only pause episode, and render the reason afterward. This is a task pause rather than a timed accepted-break session, and installed click-through remains (`TaskPauseReason`; `TaskExecutionStore.swift`; `TodayDashboardCommandOverview.swift`; `DashboardView.swift`).
- [ ] Pause because the task is blocked. **Status: Touches remaining.** The user-facing `Task is blocked` action persists a blocked reason and blocked state while retaining tracked time. Contextual replanning and installed click-through proof remain (`TaskExecutionStore.swift`; `TodayDashboardCommandOverview.swift`; `DashboardView.swift`).
- [ ] Pause while switching tasks. **Status: Touches remaining.** Starting another task now requires explicit confirmation and atomically records `switchingTasks` on the previous task while preserving time. Store and agent journeys pass; installed UI acceptance remains (`TaskExecutionStoreTests`; `TodayDashboardAgentTests`; `TodayDashboardCommandOverview.swift`).
- [ ] Pause for an external interruption. **Status: Touches remaining.** Both active-task surfaces expose `External interruption`, persist the exact reason, and render it after refresh or restart. The installed menu click-through remains because the Mac was locked (`TaskPauseReason`; `TaskExecutionStoreTests`; `DashboardView.swift`).
- [ ] Pause because the user is done for now. **Status: Touches remaining.** Both active-task surfaces expose `Done for now`, persist the exact reason, retain elapsed time, and allow later Resume or explicit Complete. Deterministic persistence passes; installed UI acceptance remains (`TaskExecutionStoreTests`; `TodayDashboardCommandOverview.swift`; `DashboardView.swift`).
- [ ] Pause when ending the workday. **Status: Touches remaining.** Both active-task surfaces expose `End the workday` and persist that exact pause reason across restart. A broader close-workday workflow and installed click-through proof remain (`TaskPauseReason`; `TaskExecutionStore.swift`; `TodayDashboardCommandOverview.swift`).
- [x] See the selected pause reason in task history or review where relevant. **Status: Fully implemented.** Reviews now renders the last factual pause reason beside a completed task, while focused restart, deduplication, and day-boundary tests prove that the preserved reason belongs to the completed history record rather than an active Today row (`TaskHistoryStore.swift`; `DailyReviewView.swift`; `TaskHistoryStoreTests.swift`; `.audit/runs/completed-task-history/3578132/REPORT.md`).
- [ ] Resume the paused task later. **Status: Touches remaining.** Paused tasks expose Resume, close the open pause episode, preserve accumulated time, and survive both store and agent reconstruction. The installed UI click-through remains (`TaskExecutionStoreTests`; `TodayDashboardAgentTests`; `DashboardView.swift`).
- [ ] Switch tasks without losing the earlier task's tracked time. **Status: Touches remaining.** Switch confirmation explains the effect before acting, and deterministic store plus agent restart tests prove the prior interval closes, its minutes remain, and only the selected task becomes active. Installed UI acceptance remains (`TaskExecutionStoreTests`; `TodayDashboardAgentTests`; `TodayDashboardCommandOverview.swift`).
- [ ] Replan after an important task becomes blocked. **Status: Touches remaining.** Blocking the main objective now transactionally saves an undo revision, promotes the next usable planned task in plan order, refreshes the main objective and recommendation, persists across agent reconstruction, and keeps the plan honest when no replacement exists. Focused promotion, restart, undo, and no-candidate tests plus release signing passed, and the signed app visibly reached an active main task with a named ready replacement and both blocker entry points. Computer Use lost the app window when activating the blocker control, so blocker submission, visible promotion, undo, restart, and no-candidate installed acceptance remain (`AutonomousPlanStore.swift`; `TodayDashboardAgent.swift`; `AppModel.swift`; `TodayDashboardAgentTests.swift`; `.audit/runs/blocked-task-replan/verifier/REPORT.md`).

## 20. Completing and rescheduling tasks

- [ ] Complete an active task even when observed aligned time is low. **Status: Partially implemented.** Completion is not gated by behavior totals, and the live active card has a completion button. Alignment is not calculated, so the exact low-alignment condition cannot be seen or verified.
- [ ] Complete a paused task explicitly. **Status: Touches remaining.** A paused focus card exposes a separate Complete button and a paused task row exposes `Complete paused task`, preserving elapsed minutes and closing the pause episode. The deterministic agent restart journey passes, but installed click-through and Reminder source confirmation remain (`TodayDashboardCommandOverview.swift`; `DashboardView.swift`; `TaskExecutionStoreTests`; `TodayDashboardAgentTests`).
- [x] See the task disappear from active work and appear in today's completed history. **Status: Fully implemented.** The signed QA XPC journey completed a real task, the installed Today surface visibly showed zero active or available copies, and Reviews visibly retained the readable completed row across app relaunch (`TodayDashboardAgent.swift`; `DailyReviewView.swift`; `.audit/runs/completed-task-history/3578132/REPORT.md`).
- [ ] See the corresponding Apple Reminder become completed. **Status: Blocked from verification.** Completion enqueues `.completeReminder` and an action executor exists, but this destructive source mutation was not performed on the user's real Reminder.
- [x] See a clear pending-sync warning if Apple Reminders rejects completion. **Status: Fully implemented.** The signed QA package revoked deterministic Reminders access, induced the real agent-side source-write failure, and visibly retained the completed task in a task-specific sync ledger with an honest failed state, local-history safety message, and retry action (`ReminderCompletionSyncState.swift`; `DashboardView.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).
- [x] Retry a failed completion sync. **Status: Fully implemented.** After access returned, the signed QA package retried the exact durable command through XPC, prevented duplicate activation, confirmed Apple Reminders completion on attempt two, and preserved that same command identity after restart (`ActionOutboxStore.swift`; `TodayDashboardAgent.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).
- [x] Avoid seeing a false success before Apple Reminders confirms completion. **Status: Fully implemented.** Today now distinguishes pending, retrying, failed, unavailable, unknown, and confirmed source states, and the signed denial journey proved that only a source-confirmed succeeded command is presented as synchronized (`ReminderCompletionSyncState.swift`; `DashboardView.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).
- [ ] Reschedule a task only after confirming the new date. **Status: Touches remaining.** The Today task menu now opens a reviewed, task-named sheet, Cancel leaves the plan untouched, future dates normalize to local midnight, and Confirm durably defers the task out of today's capacity without changing its Apple Reminder due date. This does not automatically add the task to a future plan, and the signed installed cancel/confirm/restart journey remains (`TaskRescheduleState.swift`; `DashboardView.swift`; `.audit/runs/task-reschedule-date/verifier/REPORT.md`).
- [ ] See a clear pending-sync warning if rescheduling fails. **Status: Touches remaining.** Confirmed rescheduling now durably saves the local future date before it queues one explicit Apple Reminders due-date command, supersedes obsolete pending dates, skips source mutation for local-only tasks, and exposes task-specific Pending, Retrying, Rejected, Unavailable, and Confirmed ledger states without claiming success before the write. Three focused sync tests and 29 affected regressions pass, the release QA package passed signing and helper coherence, and the verifier-fixed ordering prevents an external write when local plan persistence fails. The capped signed run stopped because the handed-off deterministic fixture used unsupported control encoding and the installed app truthfully showed QA fixture startup failure before any cancel, confirm, rejection, repair, or restart journey could be exercised (`ReminderRescheduleSyncState.swift`; `AppModel.swift`; `.audit/runs/reminder-reschedule-sync/verifier/REPORT.md`).
- [x] Keep the task and its local history when a source write fails. **Status: Fully implemented.** The signed denial, failure, repair, retry, confirmation, and restart journey retained the readable task title and exactly one local completion-history entry while the task stayed absent from active Today work (`TaskHistoryStore.swift`; `TodayDashboardAgent.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).

## 21. Changes made in Apple Reminders

- [ ] See an externally completed Reminder update in Zoid 666. **Status: Partially implemented.** The agent periodically refreshes Reminder snapshots and Today rows are derived from incomplete snapshots, so the task should disappear after refresh. No live external-change E2E test or completed-history presentation was found.
- [ ] See an active externally completed task end with an understandable reason. **Status: Partially implemented.** The signed installed journey already proved that the unplanned completed row persists across helper refresh, app relaunch, and later source deletion while another Reminder stays available, with one ended interval, zero open intervals, one Reminder completion history row, and zero duplicate completion commands. The narrow reason-UI follow-up now presents `Ended because the Apple Reminder was completed` for externally completed rows while ordinary local completion remains `Completed`, and five focused UI, lifecycle, persistence, and deletion-ordering tests plus one QA release package pass. The capped independent signed follow-up consumed the isolated fixture completion but canonical source synchronization did not propagate before relaunch, so no externally ended row existed on which to verify the new copy, local completion copy, or the full tombstone journey in one run. The end-user acceptance bar therefore remains incomplete (`TodayDashboard.swift`; `TodayDashboardAgent.swift`; `.audit/runs/external-reminder-completion/unplanned-verifier/REPORT.md`; `.audit/runs/external-reminder-completion/reason-ui-verifier/REPORT.md`).
- [ ] See title, notes, list, due date, or priority changes made in Reminders appear after sync. **Status: Partially implemented.** Reminder snapshots carry these source fields and refresh periodically, but Today rows expose only title, due date, and priority-derived urgency. Notes are not displayed, and no live sync test was performed.
- [ ] Keep local estimates and coaching history when source-owned fields change. **Status: Partially implemented.** Estimates live in the local plan by Reminder ID and task history is local, which should survive source field changes, but a live external-field change and restart sequence was not verified.
- [ ] See an active task pause when its Reminder is deleted externally. **Status: Touches remaining.** The canonical agent now detects a previously visible active Apple Reminder that disappears, closes its interval, pauses its bounded sprint exactly once, retains the row with `Paused because the Apple Reminder was deleted`, preserves stopped elapsed time across refresh and restart, keeps the next row usable, and removes the orphan after completion. Focused end-to-end agent tests and release packaging pass, while signed QA visibly proved a Healthy connected fixture with both target and remaining Reminders but reached the runtime cap during onboarding before the external deletion and Today-state journey (`TodayDashboardAgent.swift`; `TaskExecutionStore.swift`; `.audit/runs/deleted-reminder-active-task/verifier/REPORT.md`).
- [ ] Choose whether to keep a deleted Reminder as a local historical task. **Status: Touches remaining.** Settings now presents privacy-safe deleted-Reminder choices with distinct Keep Local History and Remove Local Copy controls. Twelve focused decision, rollback, retry, and migration tests passed; the signed app created exactly two pending choices after real fixture deletion, exposed no private notes or URLs, preserved both choices after Cancel, and changed only the selected row to Kept. Signed confirmation of Remove, matching reappearance cleanup, the final app-and-helper relaunch, and controlled signed failure/retry remain (`DeletedReminderDecisionController.swift`; `RemindersConnectionView.swift`; `ReminderSnapshotStore.swift`; `.audit/runs/deleted-reminder-choice/verifier/REPORT.md`).
- [ ] Avoid having Zoid 666 automatically rewrite Reminder titles, notes, lists, or priorities. **Status: Touches remaining.** The action system only creates/completes/reschedules supported entities and no automatic metadata rewrite path was found. This was verified statically, not through a complete external-change E2E test.
- [ ] Complete one recurring occurrence without modifying future occurrences. **Status: Blocked from verification.** Completion targets an EventKit Reminder identifier, but no recurring-occurrence test or safe live proof establishes future-instance behavior.

## 22. Work away from the Mac

- [ ] Mark a task session as work completed away from the Mac. **Status: Touches remaining.** The signed QA Today surface exposed `ADD AWAY-FROM-MAC WORK FOR RESEARCH MIGRATION RISKS` only after that exact local task became active, bound the sheet to the readable task title, explained the Screenwatch boundary, saved one non-default 10-minute interval and note, and retained the active task plus entry through foreground-app relaunch. A verifier-fixed Reviews title resolver passed focused and signed-package validation, but its readable-title presentation still needs one installed visual recheck (`ActiveOfflineWorkEntry.swift`; `ActiveOfflineWorkSheet.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/active-offline-work/verifier/REPORT.md`).
- [ ] Add offline work during the task session. **Status: Touches remaining.** The installed active-task sheet showed a bounded duration stepper, completed-time default, optional note, exact success feedback, Return/Escape semantics, and disabled saving when the selected interval would extend into the future. The database contained exactly one 10-minute row after save, cancel, and relaunch; Reviews showed 10 Actual, 0 Screenwatch-observed, and 10 Away from Mac with the persisted note. A final installed recheck of the post-verifier readable task-title fix remains (`ActiveOfflineWorkEntry.swift`; `DailyReviewView.swift`; `.audit/runs/active-offline-work/verifier/REPORT.md`).
- [x] Add offline work during end-of-day review. **Status: Fully implemented.** The signed-QA Reviews screen accepted a labeled 30-minute away-from-Mac entry and immediately displayed it in the selected day's review (`.audit/runs/offline-work/9cb54454/REPORT.md`).
- [x] See offline work included in actual task time. **Status: Fully implemented.** The signed-QA review changed Actual Time from 0 to 30 minutes and then to 45 minutes after correction, with restart preserving the corrected total (`.audit/runs/offline-work/9cb54454/REPORT.md`).
- [x] See offline work kept separate from Screenwatch-aligned time. **Status: Fully implemented.** The signed-QA coverage summary kept Screenwatch-observed at 0 minutes while Away from Mac and Actual Time changed independently, and the persisted entry retained that separation after relaunch (`.audit/runs/offline-work/9cb54454/REPORT.md`).
- [x] Correct the duration of an offline work session. **Status: Fully implemented.** The signed-QA Edit flow changed one entry from 30 to 45 minutes without duplication, and the corrected duration, task, note, and totals survived process termination and relaunch (`.audit/runs/offline-work/9cb54454/REPORT.md`).
- [x] Distinguish intentional offline work from missing telemetry. **Status: Fully implemented.** Reviews explicitly explain that missing telemetry is never invented as work, require a bounded task or note before enabling Add, preserve Screenwatch observations during scoped removal, and expose the same distinction to accessibility (`.audit/runs/offline-work/9cb54454/REPORT.md`).

## 23. Menu bar use

- [ ] See a neutral menu bar state when healthy with no active task. **Status: Touches remaining.** The menu extra now derives a neutral `circle.dotted` icon and accessible `Zoid 666 is ready` state from the canonical Today snapshot, focused tests pass, and the signed QA product installed successfully, but the macOS status item itself could not be addressed by the available accessibility harness for a visible final assertion (`MenuBarCoachState.swift`; `MenuBarCoachTests.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] See an active state while a task or sprint is running. **Status: Touches remaining.** The menu icon and compact status now distinguish active work, tracked minutes, and a running sprint's remaining minutes from the canonical snapshot, with focused and full-suite proof, while visible status-item inspection remains (`MenuBarCoachState.swift`; `MenuBarCoachView.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] See a warning state when attention or source repair is needed. **Status: Touches remaining.** The first unhealthy source now produces the attention icon, truthful source detail, and a direct Source Health route without exposing private activity, but the status item was not visibly opened in signed QA (`MenuBarCoachState.swift`; `MenuBarCoachView.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] See a paused state while coaching is paused. **Status: Touches remaining.** The menu-bar controller now gives a durably paused coaching policy precedence through a distinct paused icon and label while retaining the active task, with focused state tests and signed pause/restart semantics passing. The macOS status item remained absent from the available accessibility tree, so visible popover acceptance remains (`MenuBarCoachState.swift`; `.audit/runs/menu-bar-coaching-pause/verifier/REPORT.md`).
- [ ] Open Today. **Status: Touches remaining.** The menu command selects Today, reopens the identified main SwiftUI window even after closure, and activates it, with the reopen defect fixed during verification, but the status-item click could not be captured visibly (`ZoidCoachApp.swift`; `MenuBarCoachView.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] Start the recommended task. **Status: Touches remaining.** The popover selects the canonical explicit recommendation before a non-optional ready fallback and sends Start through the authenticated Today XPC client while preserving confirmed state on failure; signed QA visibly held a real recommended local task and the package/helper passed, but the final menu click remains unverified (`MenuBarCoachState.swift`; `MenuBarCoachView.swift`; `MenuBarCoachTests.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] Pause or resume the active task. **Status: Touches remaining.** Pause and Resume now use the canonical authenticated task commands, install the returned persisted snapshot, refresh Today, and surface truthful failure copy without optimistic state, with focused/full proof but no visible status-item click sequence (`MenuBarCoachView.swift`; `MenuBarCoachTests.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] Start a break. **Status: Touches remaining.** The menu now exposes `BREAK 15` only for the active task, sends the canonical durable break pause, shows the same live accepted-break countdown, changes Resume to End Break, and preserves task identity and tracked time. Focused controller, agent, persistence, restart, and accepted-break suites pass, while the capped signed harness could not open the macOS status-item popover for the final click journey (`MenuBarCoachView.swift`; `MenuBarCoachState.swift`; `.audit/runs/menu-bar-break-end-day/verifier/REPORT.md`).
- [ ] Pause coaching. **Status: Touches remaining.** The menu bar now exposes receipt-validated Pause and Resume controls that reload the newest durable policy, preserve unrelated fields, and publish truthful progress, success, and failure copy. The signed app proved policy v1 to paused v2, intervention suppression, continued task tracking, app/helper restart persistence, and resumed v3 with an eligible prompt appearing, while the status-item control itself remained inaccessible to the UI harness (`MenuBarCoachingPauseController.swift`; `.audit/runs/menu-bar-coaching-pause/verifier/REPORT.md`).
- [ ] End the workday. **Status: Touches remaining.** The active menu task now exposes a destructive confirmation that explains tracked-time preservation, supports cancellation without mutation, records the durable end-of-day pause reason, and remains resumable. Confirmation rechecks that the same task is still active and truthfully refuses stale confirmation. Focused and canonical-agent restart proof pass, while the status-item click and signed restart remain (`MenuBarCoachView.swift`; `MenuBarCoachTests.swift`; `.audit/runs/menu-bar-break-end-day/verifier/REPORT.md`).
- [ ] Open source health. **Status: Touches remaining.** The menu popover now selects the complete multi-source Source Health section, reopens the identified main window when needed, and keeps the earlier Background Agent route available, but the final signed status-item click remains (`MenuBarCoachView.swift`; `.audit/runs/agent-lifecycle/candidate/REPORT.md`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).
- [ ] Open settings. **Status: Touches remaining.** The menu popover now selects Settings, reopens the identified main window when needed, and activates the application, with compilation and full-suite proof but no visible status-item click assertion (`ZoidCoachApp.swift`; `MenuBarCoachView.swift`; `.audit/runs/menu-bar-task-controls/verifier/REPORT.md`).

## 24. Understanding behavior totals

- [x] See deep work, creative work, research, communication, and administration represented as work categories. **Status: Fully implemented.** The installed signed-QA app exposed six distinct ordered accessibility elements labeled Deep work, Creative work, Research, Communication, Administration, and Uncategorized work, each with exactly five deterministic current-day minutes and the expected user-facing hint. The exact identifiers, labels, order, values, and separate element identities survived hypothesis acceptance, app relaunch, review-and-learning privacy deletion, deterministic re-seeding, and another relaunch. Private fixture titles and URLs never appeared in the accessibility subtree, while the focused post-integration category, behavior-state, accessibility, migration, and privacy suites passed (`Sources/ZoidCoachCore/WorkCategory.swift`; `Sources/ZoidCoachApp/BehaviorEvidenceState.swift`; `Sources/ZoidCoachApp/Views/BehaviorEvidenceSheet.swift`; `.audit/runs/review-hypothesis-learning/84623d934a4eff76ef56eb4787de4ac6fc4640f5/REPORT.md`).
- [ ] See gaming, entertainment, passive consumption, distraction, system-neutral, idle, and unknown time represented distinctly. **Status: Partially implemented.** Gaming, distraction, idle, and unknown are distinct. Entertainment, passive consumption, and system-neutral are not modeled.
- [ ] See a useful summary without needing to inspect raw five-second records. **Status: Touches remaining.** The live dashboard exposes an observed-use popover with application percentages and category totals. It does not present a complete, task-aligned behavioral summary.
- [ ] See totals update as new activity is observed. **Status: Partially implemented.** The background agent regenerates snapshots while ingesting, but `AppModel` does not poll Today snapshots while the window stays open; refresh is tied mainly to launch, foreground activation, or commands.
- [ ] Avoid seeing false minute-level precision when coverage is incomplete. **Status: Touches remaining.** The installed signed Daily Review visibly labelled limited coverage, rendered category and task totals as rounded `about` observations, explained that they were not a complete account of the day, and retained missing time outside productive or distracting categories (`DailySourceCoverage.swift`; `DailySourceCoverageView.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [x] See unknown time separately from distraction. **Status: Fully implemented.** The signed Today sheet rendered Unknown 1 minute and Distraction 3 minutes as separate accessible totals and explicitly explained that unknown time is not counted as distraction or used as strong drift evidence. Focused state and dashboard tests passed (`BehaviorEvidenceState.swift`; `BehaviorEvidenceSheet.swift`; `.audit/runs/today-behavior-evidence/verifier/REPORT.md`).
- [ ] See idle time only when it can be observed reliably. **Status: Partially implemented.** Idle is classified only for known apps such as `screensaver` and `loginwindow`, but reliability and lock/wake transitions are not validated end to end.
- [ ] See active-task elapsed time separately from aligned time. **Status: Touches remaining.** The installed signed MenuBarExtra exposed separate `menu-bar.task.elapsed-time`, `menu-bar.task.aligned-time`, and `menu-bar.task.alignment-evidence` elements with truthful timer and evidence-boundary hints; the baseline showed 14 elapsed minutes beside 5 observed-aligned minutes, and an app and helper relaunch restored the same non-regressing values. The corrected permission-independent local-task fixture resolved through the real Today task-row path, and a second signed state showed elapsed advancing to 15 minutes while explicit non-work producer observations yielded 0 aligned minutes. Full qualification still requires a signed missing-producer state that is visibly unavailable rather than conflated with explicit zero, plus a full compact-card accessibility privacy scan proving fixture application names, task identifiers, window titles, URLs, and private observation values remain absent (`Sources/ZoidCoachApp/MenuBarActiveTimeComparisonView.swift`; `Sources/ZoidCoachApp/MenuBarCoachState.swift`; `Scripts/qa-active-time-comparison-fixture.sh`; `.audit/runs/zc-024-008-active-vs-aligned-time/initial-verifier/REPORT.md`; `.audit/runs/zc-024-008-active-vs-aligned-time/fixture-correction/REPORT.md`; `.audit/runs/zc-024-008-active-vs-aligned-time/signed-rerun/REPORT.md`).
- [ ] See a low-coverage warning when too much of a task session is unknown. **Status: Touches remaining.** The signed review visibly rendered `LIMITED COVERAGE`, the observed-task proportion, rounded categories, an unreliable-idle result, and a direct explanation when the latest Screenwatch checkpoint was unavailable (`DailySourceCoverage.swift`; `DailySourceCoverageView.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] Understand which source problem caused missing totals. **Status: Touches remaining.** The Today evidence state now attributes behavior coverage only to Screenwatch and focused tests prove that unrelated Reminders or Calendar outages are never blamed. The signed current-coverage sheet remained correct while Reminders was unavailable, but a fresh signed stale-Screenwatch click-through and Source Health repair-route assertion remain (`BehaviorEvidenceState.swift`; `BehaviorEvidenceSheet.swift`; `.audit/runs/today-behavior-evidence/verifier/REPORT.md`).

## 25. Ambiguous applications and activity

- [x] See known work applications classified according to configured rules. **Status: Fully implemented.** The signed-QA Settings ledger discovered 131 installed, observed, and saved applications, exposed distinct Auto, Work, Communication, and Gaming choices, persisted an explicit rule through the agent, and restored that exact normalized rule after relaunch. Runtime tests prove that Work and Communication rules count as work while Communication remains visibly distinct (`AppClassificationLedger.swift`; `UserPolicyTests.swift`; `.audit/runs/app-classification-management/candidate/REPORT.md`).
- [x] See known games classified as gaming. **Status: Fully implemented.** The signed-QA ledger exposes Gaming for every discovered application, the visible verifier applied Gaming individually and through a confirmed filtered bulk action, and runtime tests prove configured Gaming overrides classification and budget accounting (`AppClassificationLedger.swift`; `TodayDashboardTests.swift`; `.audit/runs/app-classification-management/candidate/REPORT.md`).
- [ ] See browsers, Discord, Slack, Notion, YouTube, and Preview treated according to context rather than permanently judged. **Status: Touches remaining.** Screenwatch now classifies context-sensitive apps from local title and URL signals after durable correction rules and explicit application policy, while ambiguous context remains Unknown. Focused ingestion tests cover Safari documentation, YouTube tutorials and Shorts, Discord project and gaming contexts, Notion roadmaps, Preview proposals, ambiguous Slack, policy precedence, and SQLite reopen persistence. In the installed signed-QA app, the exact mixed fixture produced visible Work, Gaming, Distraction, and Unknown totals, preserved them after app and helper relaunch, and exposed the privacy-safe Review path without titles or URLs. One finishing touch remains: visibly create an explicit rule, ingest later contradictory context, relaunch the helper and app, and confirm that the user rule still wins (`ContextualAppClassification.swift`; `ScreenwatchArchive.swift`; `ScreenwatchArchiveTests.swift`; `.audit/runs/contextual-app-classification/verification/REPORT.md`).
- [ ] See uncertain activity remain unknown. **Status: Touches remaining.** Unmatched applications are classified `unknown` and shown as `Unclassified`; this behavior is implemented and tested at the classifier/sessionizer level.
- [ ] Avoid a strong drift warning based only on uncertain activity. **Status: Partially implemented.** The ambiguity-aware behavior service now suppresses coaching when evidence is uncertain, and 23 focused tests plus the deterministic fixture prove idempotent baseline, ambiguity, withdrawal, recovery, privacy, and cleanup transitions. Full status is not yet justified because the installed helper has no bounded QA command that invokes `GamingDriftPromptService.produce` with every runtime-owned gate, so the live prompt withdrawal and exactly-once recovery prompt remain unproven in the signed UI (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `Scripts/qa-ambiguous-drift-fixture.sh`; `.audit/runs/settings-safety-6038/REPORT.md`).
- [ ] Be asked for confirmation when ambiguity materially affects coaching. **Status: Not implemented.** No ambiguity-confirmation prompt generator exists.
- [x] See a technical tutorial related to the active task treated as research or left uncertain. **Status: Fully implemented.** In the installed signed-QA app, the user started the related Swift concurrency task and the real helper classified ten minutes of a technical YouTube documentation tutorial as Work while the Behavior Evidence sheet kept Research at zero, left all ten minutes Uncategorized, and explicitly explained that application evidence could not prove support for the active task or safely label it Research. Two insufficient-context YouTube observations remained Unknown, stable privacy-safe identifiers, labels, and hints were present, private fixture titles and URLs never appeared in the sheet accessibility tree, and the outcome survived app and helper relaunch. The 14 focused state, accessibility, and contextual-classifier tests passed together with release packaging, deep signing, LaunchAgent, Mach/XPC, heartbeat, and cleanup gates (`Sources/ZoidCoachCore/ContextualAppClassification.swift`; `Sources/ZoidCoachApp/BehaviorEvidenceState.swift`; `Sources/ZoidCoachApp/Views/BehaviorEvidenceSheet.swift`; `.audit/runs/zc-025-007-context-sensitive-work/verifier/REPORT.md`).
- [ ] Understand when Zoid 666 may be wrong. **Status: Touches remaining.** The signed Today sheet explains that Unknown is not distraction or strong drift evidence, that missing time is never idle, and that idle is shown only when reliably observed. Review and Correct Activity and Open Source Health actions are adjacent to that explanation, but the verifier did not complete both signed destination assertions before the UI time cap (`BehaviorEvidenceState.swift`; `BehaviorEvidenceSheet.swift`; `.audit/runs/today-behavior-evidence/verifier/REPORT.md`).

## 26. Correcting observed activity

- [x] Reclassify an incorrectly categorized session. **Status: Fully implemented.** The installed signed QA review changed a specific Steam session from Unknown to Work, recalculated the visible category immediately, persisted the correction across relaunch, and retained source observations unchanged (`DailyReviewStore.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [ ] Split a session that contains two different activities. **Status: Touches remaining.** Daily Review exposes a midpoint-only correction control and focused tests prove only the second half changes, but that split path has not received a fresh signed-QA click-through (`DailyReviewView.swift`; `DailyReviewTests.swift`).
- [ ] Merge adjacent sessions that are really one activity. **Status: Touches remaining.** Migration 49, the durable merge store, Daily Review merge candidate and confirmation controls, stale and non-adjacent rejection, deterministic left-session classification and task truth, split-after-merge restoration, privacy deletion, reversible owned fixture, and affected migration and review tests are integrated in the 108-test passing combined candidate. Full qualification still requires a signed installed journey that visibly presents two adjacent sessions, activates `MERGE WITH NEXT`, confirms one combined activity with the chosen left-session truth, proves raw private observations are absent and unchanged, and restores the merged result after app and helper relaunch (`Sources/ZoidCoachInfrastructure/DailyReviewStore.swift`; `Sources/ZoidCoachApp/Views/DailyReviewView.swift`; `Scripts/qa-daily-review-session-merge-fixture.sh`; `Tests/ZoidCoachAppTests/DailyReviewTests.swift`; `docs/implementation/GAMING-MANUAL-ADJUSTMENT-INTEGRATION.md`).
- [ ] Attach a session to the correct task. **Status: Touches remaining.** Daily Review exposes an accessible task attachment field and restart tests preserve the attachment, but the signed correction acceptance did not exercise a real task association (`DailyReviewView.swift`; `DailyReviewTests.swift`).
- [x] See affected totals update after correction. **Status: Fully implemented.** The installed signed QA review changed its visible activity summary from Unknown to Work immediately after correction while keeping the two-minute factual duration intact, and focused tests cover recalculation after whole and midpoint corrections (`DailyReviewStore.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [ ] See affected alignment and review statements update after correction. **Status: Not implemented.** Alignment and correction-aware review statements are absent.
- [ ] Apply a correction only once. **Status: Partially implemented.** Corrections are append-only and later reads deterministically resolve their effective result, but the correction command does not yet carry an idempotency key that prevents a repeated activation from recording another equivalent event.
- [x] Create a reusable rule from a correction. **Status: Fully implemented.** The installed signed QA review offered an explicit future-rule toggle, previewed the Steam-to-Work scope, committed the correction and rule atomically, and restored the active rule after app relaunch (`DailyReviewStore.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [ ] Apply a one-time correction without creating a rule. **Status: Touches remaining.** The future-rule toggle defaults off and focused restart tests prove a correction can remain historical only, but the signed acceptance journey deliberately enabled adaptation and did not separately click the one-time path (`DailyReviewView.swift`; `DailyReviewTests.swift`).
- [x] Preview exactly how broadly a proposed rule will apply. **Status: Fully implemented.** Before committing, the installed signed QA app visibly stated that new Steam observations would become Work while historical observations stayed unchanged, and replacement previews name the prior and proposed classifications (`DailyReviewView.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [ ] Edit or remove a learned rule later. **Status: Touches remaining.** The installed app restores an accessible active-rule ledger after relaunch and opens a scoped removal confirmation explaining that history remains; append-only replacement and tombstone removal pass focused tests, but the destructive UI confirmation was conservatively not activated in signed QA (`DailyReviewView.swift`; `DailyReviewTests.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [x] Ensure a user correction continues to outrank future automatic classification. **Status: Fully implemented.** After the signed review created a Steam-to-Work rule, the isolated signed agent ingested a later Steam Screenwatch observation as Work even though the normal policy would leave it Unknown; the original historical session remained a separate durable correction (`ScreenwatchArchive.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).

## 27. Grace periods and neutral activity

- [x] Switch applications during the first three minutes after starting a task without receiving a normal drift warning. **Status: Fully implemented.** The installed signed helper evaluated a durable active priority task started 150 seconds earlier plus a new two-minute gaming session, created no prompt, and left the task and its open activity interval unchanged. Deterministic integration asserts the explicit `taskStartGrace` reason before the normal threshold (`GamingDriftPromptService.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/evidence.json`).
- [ ] Receive protection during the first minute after waking, unlocking, or returning from idle. **Status: Touches remaining.** Deterministic integration returns `returnFromIdleGrace` for both a real idle classification and a telemetry gap, and the signed idle-to-gaming seed created no prompt or task mutation within one minute. Explicit macOS wake and unlock signals are not yet modeled (`GamingDriftPromptService.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/REPORT.md`).
- [ ] Allow high-confidence gaming to bypass the task-start grace period when other trigger conditions are met. **Status: Touches remaining.** Deterministic integration queues accountability for a certain ten-minute session that began before the active task, and the signed seed truthfully reached ten used minutes with zero allowance. Its installed prompt did not appear before the capped run ended, so signed bypass delivery remains (`GamingDriftPromptServiceTests.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/REPORT.md`).
- [x] Use System Settings briefly without having the task marked misaligned. **Status: Fully implemented.** The installed signed helper evaluated current System Settings activity after sustained gaming, created no prompt, and preserved one active task plus one open activity interval. Focused coverage asserts the explicit neutral-supporting result (`GamingDriftPromptService.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/evidence.json`).
- [ ] Use a password manager briefly without having the task marked misaligned. **Status: Touches remaining.** Bounded 1Password, Bitwarden, and Keychain Access matching suppresses coaching without mutating the active task in deterministic integration, while the signed timebox completed only the System Settings representative (`GamingDriftPromptService.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/REPORT.md`).
- [ ] Use file dialogs, downloads, or short communication checks as neutral supporting activity. **Status: Touches remaining.** Deterministic integration covers Finder/downloads, explicit file-dialog titles, local file URLs, Slack, Messages, Mail, Teams, and Zoom with no prompt or task mutation. Negative coverage keeps OpenTTD, Open World, and Gmail Notifier non-neutral; signed representatives beyond System Settings remain (`GamingDriftPromptService.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/REPORT.md`).
- [x] Avoid having neutral activity automatically pause the task. **Status: Fully implemented.** Neutral evaluation is read-only. The installed System Settings journey left the task active with its activity interval open, while deterministic representatives assert zero prompts and unchanged active-task count (`GamingDriftPromptService.swift`; `.audit/runs/drift-grace-neutral/17ac1e521d3469582715dfb030524ac8322bab05/evidence.json`).

## 28. Taking breaks

- [ ] Start an accepted break from the dashboard. **Status: Partially implemented.** Today now exposes Take a break for the active task and the signed app persisted a durable `break` pause, while the installed countdown state did not appear reliably after the QA helper replacement and remains unproven (`DashboardView.swift`; `TaskExecutionStore.swift`; `.audit/runs/accepted-break-lifecycle/verifier/REPORT.md`).
- [ ] Start an accepted break from the menu bar. **Status: Partially implemented.** The menu bar now exposes `BREAK 15` and routes the same durable pause command, but the capped signed run could not reach the menu-bar branch after the QA helper disappeared (`MenuBarCoachView.swift`; `.audit/runs/accepted-break-lifecycle/verifier/REPORT.md`).
- [x] Start an accepted break from a coach prompt. **Status: Fully implemented.** The installed signed gaming prompt exposed Take a break only while a task was actively tracking. Choosing it closed the active interval, visibly changed Today to `PAUSED FOR A BREAK` with neutral copy, recorded one durable break pause and one response, survived app/helper restart, and resumed cleanly (`PromptResponseEffectRouter.swift`; `GamingDriftPromptService.swift`; `.audit/runs/prompt-accepted-break/0f1eb596ab7df9d8cdbc4bb4b7147ca93c2c1846/evidence.json`).
- [ ] Choose or understand the expected break duration. **Status: Partially implemented.** The accepted-break snapshot models a fixed 15-minute duration and Today and the menu bar contain explicit countdown copy, but the countdown was not visible in the capped signed run (`TodayDashboard.swift`; `DashboardView.swift`; `MenuBarCoachState.swift`; `.audit/runs/accepted-break-lifecycle/verifier/REPORT.md`).
- [ ] Avoid receiving drift prompts during an accepted break. **Status: Touches remaining.** The gaming-drift producer consumes the same durable open break pause and deterministically returns `acceptedBreak`; the installed signed app preserved that pause across app/helper restart and closed it on Resume. A live eligible gaming session during the break was not generated, so installed no-prompt timing remains (`GamingDriftPromptService.swift`; `.audit/runs/prompt-accepted-break/verifier/REPORT.md`).
- [ ] Receive a break-end reminder. **Status: Partially implemented.** The running agent now reconciles durable accepted breaks into one replacement-safe `BREAK_END` reminder with stable identity, exact 15-minute boundary, immediate missed-boundary scheduling, named neutral copy, durable one-shot ledger gating, restart-safe replay, and cancellation on resume. Focused lifecycle, coordinator, and reminder suites plus release and signed packaging pass, while installed scheduling and cancellation remain unproven because the signed QA app did not consume its fixture-permission control request within the capped run (`AcceptedBreakReminderService.swift`; `PromptNotificationCoordinator.swift`; `.audit/runs/agent-break-end-reminders/verifier/REPORT.md`).
- [ ] End the break early. **Status: Partially implemented.** Today and the menu bar route End Break Early through the durable resume command, but the installed control was not reached after the countdown state failed to appear (`DashboardView.swift`; `MenuBarCoachView.swift`; `.audit/runs/accepted-break-lifecycle/verifier/REPORT.md`).
- [ ] Resume work after the break. **Status: Touches remaining.** Store, agent, and focused restart tests prove the same task resumes with its break pause closed, and the signed app preserved and resumed the task across app and helper restart; the installed countdown branch remains incomplete (`TaskExecutionStore.swift`; `TodayDashboardAgent.swift`; `.audit/runs/accepted-break-lifecycle/verifier/REPORT.md`).
- [ ] See break activity without having it described as failure or drift. **Status: Partially implemented.** Today and menu copy explicitly call the interval an accepted break rather than drift or failure, but the installed accepted-break presentation did not appear reliably in the capped run (`DashboardView.swift`; `MenuBarCoachState.swift`; `.audit/runs/accepted-break-lifecycle/verifier/REPORT.md`).

## 29. Gaming policy setup

- [x] Observe gaming without applying a budget. **Status: Fully implemented.** In the installed signed app, the user disabled the gaming budget and prompts, saved through the agent, relaunched with all configured values retained in disabled controls, seeded exactly 42 gaming minutes, and saw `GAMING OBSERVED 42m` with no allowance, unlock, debt, waiting decision, or durable prompt before and after a helper restart (`GamingPolicy`; `SettingsView.swift`; `GamingDriftPromptService.swift`; `.audit/runs/gaming-observation-mode/fd24a048eaec782532a2401f6fd422a3861307fc/evidence.json`).
- [ ] Set a daily gaming budget. **Status: Touches remaining.** Settings now exposes a 0-1,440 minute base allowance in five-minute steps, reconstructs it from the active policy, and saves it through the conflict-safe policy mutation boundary. Focused persistence and runtime allowance tests pass, while the signed installed save and relaunch journey remains (`SettingsPolicyDraft.swift`; `SettingsView.swift`; `.audit/runs/gaming-policy-settings/verifier/REPORT.md`).
- [ ] Unlock gaming after selected priority tasks are completed. **Status: Partially implemented.** Settings now configures the additional minutes unlocked once by completing the existing priority objective, and runtime tests prove the reward changes remaining allowance. The user still cannot select arbitrary unlock tasks and the installed Today effect remains unverified (`SettingsView.swift`; `SettingsPolicyDraftTests.swift`; `.audit/runs/gaming-policy-settings/verifier/REPORT.md`).
- [ ] Earn gaming time from aligned focus time. **Status: Not implemented.** Rewards are completion-based only; aligned focus time is not calculated.
- [ ] Combine a base budget, task unlock, and focus-time rewards. **Status: Not implemented.** Only base budget plus a fixed one-time priority reward exists.
- [ ] Choose which applications count as gaming. **Status: Touches remaining.** Settings provides per-app Gaming classification and persists it in `BehaviorPolicy`. There is no context-sensitive rule or current-session preview.
- [x] Decide whether Discord or Twitch count only in a gaming context. **Status: Fully implemented.** The signed-QA Settings ledger exposed Discord and Twitch with `AUTO BY CONTEXT` guidance, let the user explicitly set Discord to Work and Twitch to Gaming, persisted Discord after app termination and relaunch, and then restored both applications to Automatic. The final active policy contained no explicit override, proving that contextual behavior was restored, while four presentation tests and the 42-test batch passed (`ContextualGamingAppRulePresentation.swift`; `AppClassificationLedger.swift`; `ContextualGamingAppRulePresentationTests.swift`; `.audit/runs/settings-safety-6038/REPORT.md`).
- [ ] Set the base available minutes. **Status: Touches remaining.** Settings now exposes the stored base minutes with bounded five-minute edits, conflict-safe merging, and factual zero-budget copy. The signed installed edit, save, and relaunch persistence sequence remains (`SettingsView.swift`; `SettingsPolicyConflict.swift`; `.audit/runs/gaming-policy-settings/verifier/REPORT.md`).
- [ ] Set the unlock tasks or focus-time rule. **Status: Partially implemented.** The installed signed Today journey exposes the current main objective as the one-time gaming reward condition, lets the user choose another planned task through `MAKE MAIN + GAMING UNLOCK`, requires an explicit Cancel-or-confirm decision, moves the single unlock owner immediately, restores it after relaunch, withholds the reward from the former task, and awards the configured minutes exactly once after the selected task is completed (`Sources/ZoidCoachApp/GamingUnlockConditionPresentation.swift`; `Sources/ZoidCoachApp/Views/TodayDashboardCommandOverview.swift`; `Tests/ZoidCoachAppTests/GamingUnlockConditionPresentationTests.swift`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/REPORT.md`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/evidence/03-confirmation.png`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/evidence/09-immediate-fixed.png`; `.audit/runs/plan-gaming-unlock-condition/acceptance-27f034a/evidence/10-relaunch-fixed.png`; `.audit/runs/plan-gaming-unlock-condition/2b9f106e63bebc6a8a3479ce68296d80e2fc952d/evidence.json`). The policy still cannot choose task-based, focus-time, or hybrid mode; configure an arbitrary task set or completion count; configure a focus threshold or reward ratio; or validate invalid values, Cancel, disabled-policy, and accessibility branches across those missing modes. A `ZC-029-009`-bound immutable evidence manifest remains required.
- [ ] Set the daily maximum during work hours. **Status: Not implemented.** No separate work-hours maximum exists.
- [ ] Choose whether early gaming creates same-day debt. **Status: Not implemented.** Debt is not modeled.
- [ ] Choose whether debt carries into another day. **Status: Not implemented.** Debt is not modeled.
- [ ] Set the intentional-override cooldown. **Status: Touches remaining.** Settings now exposes a conflict-safe 5-to-1,440-minute `Continue intentionally` duration, persists it in Gaming Policy, and the restart-safe coaching service consumes the saved value. Focused 25-minute round-trip, conflict, suppression, expiry, legacy decode, and range proof passes; signed Settings save and relaunch remains (`SettingsPolicyDraft.swift`; `GamingDriftPromptService.swift`; `.audit/runs/intentional-override-policy/verifier/REPORT.md`).
- [ ] Configure different weekend or after-work behavior. **Status: Not implemented.** Work windows exist, but gaming policy has no weekday or after-work variants.

## 30. Gaming budget use

- [x] See available, earned, used, locked, and debt minutes. **Status: Fully implemented.** Signed installed Today shows the complete base, earned, used, locked, remaining, and same-day overage ledger after app and helper restart; focused proof covers non-zero earned and overage states (`TodayDashboard.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/gaming-budget-transparency/verifier/REPORT.md`).
- [ ] See the next unlock condition. **Status: Touches remaining.** Signed installed Today shows `Finish one priority task to unlock a one-time reward.` beside the complete allowance ledger after restart, but the condition remains fixed rather than user configured (`.audit/runs/gaming-budget-transparency/verifier/REPORT.md`).
- [ ] See gaming time accumulate from confidently detected gaming sessions. **Status: Touches remaining.** A signed isolated journey shows Steam at four raw observed minutes while three meaningful minutes consume the allowance and persist after app and helper restart; confidence policy remains limited to app classification and source coverage (`TodayDashboard.swift`; `.audit/runs/gaming-budget-transparency/verifier/signed-ui-observed-vs-meaningful.jpeg`).
- [x] Avoid counting a brief game-launcher transition as meaningful gaming. **Status: Fully implemented.** A signed installed journey separates a one-minute launcher visit from a later three-minute session, keeps all four minutes visible as observed use, and charges only three minutes to the allowance after app and helper restart (`TodayDashboard.swift`; `.audit/runs/gaming-budget-transparency/verifier/REPORT.md`).
- [ ] See gaming unlock after the configured task condition is satisfied. **Status: Partially implemented.** Completing the main objective still records the fixed priority reward and focused proof exposes earned versus locked minutes, but configurable conditions and a signed source-completion journey remain (`.audit/runs/gaming-budget-transparency/verifier/REPORT.md`).
- [ ] See gaming time earned after the required aligned work. **Status: Not implemented.** There is no aligned-work reward calculation.
- [ ] Stop earning automatic time at the configured daily maximum. **Status: Not implemented.** No focus-time earning or configurable maximum exists.
- [ ] See same-day debt after gaming before unlock when debt is enabled. **Status: Not implemented.** Debt is absent.
- [ ] Start the next day without carried debt by default. **Status: Not implemented.** Debt is absent, so the intended policy cannot be configured or reviewed.
- [ ] Carry debt only when explicitly configured. **Status: Not implemented.** Debt is absent.
- [x] Add or remove gaming time manually. **Status: Fully implemented.** The installed signed Today surface exposed the manual adjustment action, blocked an invalid removal before any grant, added 15 minutes with note `qa-grant`, displayed the positive append-only history row and 75-minute allowance, restored it after app and helper relaunch, removed 15 minutes with note `qa-remove`, displayed the negative row, and restored both timestamped rows with net zero after a second relaunch. Signed stale-day and changed-time-zone submissions visibly rejected and refreshed without writes, an unavailable ledger visibly disabled adjustment with zero writes, and the actual signed helper later returned `suppressed:gamingIsUnlocked` with 65 minutes remaining, no prompt, no delivery event, no QA notification, and unchanged raw observations and grant (`Sources/ZoidCoachInfrastructure/GamingManualAdjustmentStore.swift`; `Sources/ZoidCoachApp/Views/TodayDashboardCommandOverview.swift`; `.audit/runs/zc-030-011-manual-gaming-adjustments/core-signed/REPORT.md`; `.audit/runs/zc-030-011-manual-gaming-adjustments/negative-paths/REPORT.md`; `.audit/runs/zc-030-011-manual-gaming-adjustments/helper-suppression/REPORT.md`).
- [x] See manual adjustments separately from automatically earned rewards. **Status: Fully implemented.** The signed allowance ledger visibly exposed `Manual +15m` separately from Base and Earned minutes, showed distinct positive `qa-grant` and negative `qa-remove` history rows with timestamps, preserved exactly one then two append-only rows across two app and helper relaunches, and returned the manual component to net zero without changing the automatic reward fields (`Sources/ZoidCoachCore/TodayDashboard.swift`; `Sources/ZoidCoachApp/Views/TodayDashboardCommandOverview.swift`; `.audit/runs/zc-030-011-manual-gaming-adjustments/core-signed/REPORT.md`).

## 31. Gaming drift detection

- [ ] Game for less than ten minutes without receiving the default drift prompt. **Status: Touches remaining.** The production agent now uses a correction-aware fresh-session detector with an exact ten-minute threshold, and focused tests prove nine minutes stays quiet. Installed signed-QA threshold timing remains to be exercised (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Game for at least ten minutes with incomplete priority work and become eligible for a prompt. **Status: Touches remaining.** The agent now combines a fresh ten-minute gaming session, exhausted allowance, and the canonical incomplete priority task into one durable prompt. Deterministic integration passes; the installed post-baseline active leg remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving a prompt when all applicable unlock conditions are satisfied. **Status: Touches remaining.** The producer suppresses coaching while any gaming allowance remains, with focused proof against the canonical gaming snapshot. Installed signed-QA unlock suppression remains (`GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving a prompt while on an accepted break. **Status: Touches remaining.** An open canonical break pause now suppresses the gaming prompt producer, and focused persistence tests pass. Installed break-state proof remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving a prompt while coaching is paused. **Status: Touches remaining.** The agent gates the producer on the durable automation-pause policy before reading session evidence. Focused proof passes; installed paused-runtime proof remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving a prompt outside the configured work window. **Status: Touches remaining.** The producer evaluates the policy timezone, weekday, normal windows, and overnight windows before prompting. A signed-runtime diagnostic truthfully returned `outsideWorkWindow`; full installed boundary traversal remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving a prompt after the workday is closed. **Status: Touches remaining.** An open `endingWorkday` pause now closes the coaching path and focused proof passes. Installed end-day verification remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving a prompt when the gaming classification is uncertain. **Status: Touches remaining.** Limited telemetry now suppresses prompt production and correction-aware rows can remove a false gaming session before eligibility. Installed limited-coverage traversal remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Avoid receiving repeated prompts for the same continuing gaming session. **Status: Touches remaining.** Session decision keys now survive unresolved, resolved, expired, and reopened stores, while coaching level enforces cooldown and a three-per-day maximum. Focused restart proof passes; installed active-session repetition remains (`GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Correct an application rule and stop future false gaming alerts for that context. **Status: Touches remaining.** The session detector consumes correction-aware classifications, while Settings app-rule changes continue governing future ingestion. The combined visible correction-to-silence journey remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).

## 32. First-week observation mode

- [ ] Use the app for the first seven complete days without behavior-triggered interruptions. **Status: Touches remaining.** The production behavior-prompt producer now consumes the durable seven-complete-day baseline gate, and an installed signed-QA agent produced zero gaming prompts and zero notifications with six complete days plus eligible gaming evidence. A longer installed seven-day lifecycle remains (`BaselineObservationStore.swift`; `GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] See that eligible drift is being observed during the baseline. **Status: Touches remaining.** Today now shows an accessible first-week ledger with the count of qualifying gaming or distracting episodes observed while planned work was unfinished at the ten-minute threshold. The threshold result freezes truthfully even if the task completes later, correction-aware aggregates and restart-safe daily reconciliation pass focused tests, and installed signed-QA visibility remains before full qualification (`BaselineObservationStore.swift`; `BaselineObservationView.swift`; `.audit/runs/baseline-observation/candidate/REPORT.md`).
- [ ] Understand why accountability prompts are not active yet. **Status: Touches remaining.** The new Today baseline surface explicitly explains that Zoid 666 observes seven adequately covered completed days before behavior coaching gets louder, that the current day never counts, and that planning and manual task controls remain available. The product still lacks the behavior-prompt producer whose real suppression must be proven end to end (`BaselineObservationView.swift`; `.audit/runs/baseline-observation/candidate/REPORT.md`).
- [ ] Complete the baseline week and see coaching progress to the configured level. **Status: Touches remaining.** Settings now persists Gentle or Accountability through the versioned policy store, and the agent applies that level only after the baseline status stops suppressing behavior prompts. Installed active-prompt visibility after canonical policy save remains (`SettingsView.swift`; `GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Keep stronger coaching disabled if the baseline is incomplete. **Status: Touches remaining.** Both coaching levels share the baseline suppression gate, and the installed signed-QA agent stayed silent with six complete days despite otherwise eligible evidence. The complete installed transition remains (`GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Use baseline results to review work capacity, gaming patterns, and alert sensitivity. **Status: Touches remaining.** After seven qualifying days, Today presents a frozen local report with average correction-aware work minutes, gaming days and minutes, eligible drift, unknown coverage, and conservative alert-sensitivity guidance without grading effort or inferring intent. Focused restart and correction tests pass; installed signed-QA report visibility remains (`BaselineObservation.swift`; `BaselineObservationStore.swift`; `BaselineObservationView.swift`; `.audit/runs/baseline-observation/candidate/REPORT.md`).

## 33. Receiving a coaching prompt

- [ ] Receive a gentle nudge offering an easy return to work. **Status: Touches remaining.** Gentle coaching now produces one daily dismissible `Ready for an easy return?` episode after eligible drift, shared by notification and Today. Installed post-baseline delivery remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] Receive an accountability prompt asking whether gaming is intentional. **Status: Touches remaining.** Accountability coaching now produces `Is this gaming intentional?`, permits at most three daily episodes, and enforces sixty minutes between separate sessions across restart. Installed post-baseline delivery remains (`GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] See the observed fact before the coach's interpretation. **Status: Touches remaining.** The prompt summary leads with observed minutes and application, then states the unfinished task and explicitly says the observation is not a judgment. Installed visual copy inspection remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] See the relevant unfinished task named. **Status: Touches remaining.** The producer selects the highest canonical incomplete non-optional priority task and names it in both summary and primary action. Installed Today rendering remains (`GamingDriftPromptService.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [ ] See one clear primary action. **Status: Touches remaining.** Every gaming prompt assigns `Return to <task>` the sole primary role, and the response router starts that exact task once through canonical execution state. Installed click-through remains (`PromptResponseEffectRouter.swift`; `.audit/runs/behavior-coaching/candidate/REPORT.md`).
- [x] See no more than three secondary actions. **Status: Fully implemented.** The prompt store rejects every draft with more than three secondary actions before persistence, focused tests cover the rejection, and the installed signed Today prompt visibly presented one primary action and exactly three secondary actions (`BehaviorPromptPresentationPolicy`; `.audit/runs/behavior-prompt-clarity/verifier/REPORT.md`).
- [x] See reliable elapsed time when included. **Status: Fully implemented.** Gaming prompts derive their displayed minutes from the first and latest canonical observations, reject mismatched evidence before persistence, and the installed signed Today prompt visibly reported the same exact ten observed Steam minutes after restart (`GamingDriftPromptService.swift`; `.audit/runs/behavior-prompt-clarity/verifier/REPORT.md`).
- [x] See uncertainty acknowledged when context is ambiguous. **Status: Fully implemented.** Contracted prompts must state that observed activity does not establish why it happened or what the user intended, limited coverage suppresses generation, and the installed signed Source Health and Today surfaces visibly explained the missing Screenwatch evidence and safe manual fallback (`BehaviorPromptPresentationPolicy`; `.audit/runs/behavior-prompt-clarity/verifier/REPORT.md`).
- [x] Avoid guilt, insults, moral labels, disappointment, or exaggerated claims. **Status: Fully implemented.** Contracted prompts containing the defined coercive phrase classes are rejected before persistence, while the signed prompt used factual activity, application, elapsed-time, and unfinished-task copy and remained neutral through response history (`BehaviorPromptPresentationPolicy`; `.audit/runs/behavior-prompt-clarity/verifier/REPORT.md`).
- [x] Avoid being told what the user's intent must be. **Status: Fully implemented.** Contracted prompts reject asserted-intent phrases and require an explicit uncertainty boundary, while the signed prompt asked whether gaming was intentional and explicitly said the observations did not establish intent (`BehaviorPromptPresentationPolicy`; `.audit/runs/behavior-prompt-clarity/verifier/REPORT.md`).
- [ ] Find the same unresolved prompt in the dashboard if the initial surface disappears. **Status: Touches remaining.** The signed fixture retained superseded plan and gaming decisions in Today while exposing only the latest notification per relevance group, allowed one older decision to resolve exactly once, and preserved four waiting plus one answered decision through app and helper relaunch. Real Notification Center disappearance remains unverified because signed QA mode intentionally uses its isolated notification fixture (`PromptNotificationRelevance`; `.audit/runs/latest-relevant-notification/verifier/REPORT.md`).

## 34. Responding to coaching

- [x] Start the recommended task. **Status: Fully implemented.** The signed Today decision started its named task, exposed it as the active commitment, and retained one closed interval after the later end-workday response and helper restart (`PromptResponseEffectRouter.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [ ] Start a 10-minute recovery sprint. **Status: Touches remaining.** The shared response router starts an exact restart-safe 10-minute bounded sprint, and the focused cross-surface journey proves its duration and exactly-once mutation. Selecting the 10-minute action in the signed app remains (`PromptResponseEffectRouter.swift`; `PromptResponseEffectRouterTests.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [x] Start a 20-minute work sprint. **Status: Fully implemented.** The signed Today decision started the named task with an exact visible 20-minute countdown, persisted one bounded sprint row, and retained its paused state across app and helper restart (`PromptResponseEffectRouter.swift`; `TaskExecutionStore.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [ ] Return to the current active task. **Status: Touches remaining.** The focused response journey resumes a paused 20-minute sprint without replacing its duration and rejects notification-surface replay after the dashboard response. Selecting Resume in the signed app remains (`PromptResponseEffectRouter.swift`; `PromptResponseEffectRouterTests.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [x] Choose `Five more minutes`. **Status: Fully implemented.** The signed Today prompt visibly offered `Five more minutes`, the end user selected it, the answered choice persisted across a helper restart, and no follow-up appeared before the promised boundary (`GamingDriftPromptService.swift`; `.audit/runs/five-minute-coaching-followup/verifier/REPORT.md`).
- [x] Receive one follow-up when the five-minute snooze ends. **Status: Fully implemented.** The restart-safe response timestamp produced exactly one fixture-delivered `Your five minutes are up` card after the boundary, the card omitted a second snooze, and helper replay kept prompt and delivery counts unchanged (`GamingDriftPromptService.swift`; `.audit/runs/five-minute-coaching-followup/verifier/REPORT.md`).
- [ ] Start an accepted break. **Status: Barely started.** `startBreak` exists as an enum, but no break state or effect is wired.
- [ ] Choose `Continue intentionally`. **Status: Barely started.** `continueIntentionally` exists as an enum, but no gaming prompt or override effect exists.
- [x] Pause the task. **Status: Fully implemented.** The signed Today response carried deliberately stale prompt context yet paused the actual live task, closed its only interval, preserved its 20-minute sprint as paused, and remained singular after restart (`PromptResponseEffectRouter.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [ ] Reschedule the task. **Status: Touches remaining.** A generated signed coaching prompt opens an accessible future-date sheet, Cancel safely preserves the decision, and Confirm saves the local date before queuing exactly one matching Apple Reminder mutation and resolving the prompt. Signed acceptance found and fixed both the fully deferred-plan validator rejection and the automatic planner overwrite; one fresh signed relaunch on the final retention fix remains (`AppModel.swift`; `AgentOwnedStateStore.swift`; `AutonomousPlanStore.swift`; `.audit/runs/prompt-task-reschedule/verifier/REPORT.md`).
- [ ] Mark the task blocked. **Status: Touches remaining.** The builder's signed flow exposed all six prompt actions as direct stable controls, rejected a two-character reason without mutation, preserved the prompt on Cancel, saved `Waiting for approval.` exactly once, closed the active interval, blocked the original task, promoted `Prepare launch notes`, restored the reason in answered history after app and helper relaunch, and kept the database unchanged with the specific recovery message when the helper was unavailable. A fresh independent run first exposed a degraded helper that returned an empty XPC timeline despite the canonical presented prompt; the signed registration-continuity repair now requires a writable XPC prompt timeline and heartbeat, preserved the same helper PID across foreground relaunch, and independently restored all six controls plus invalid and Cancel behavior. One fresh independent activation of valid Save Blocker, followed by exactly-once SQLite, answered-history, app/helper relaunch, and helper-unavailable checks, remains before completion (`TodayPromptInboxLedger.swift`; `PromptTaskBlockState.swift`; `AppModel.swift`; `AgentLaunchService.swift`; `.audit/runs/prompt-blocked-finish/verifier/REPORT.md`; `.audit/runs/prompt-blocked-finish/independent-verifier/REPORT.md`).
- [x] End the workday. **Status: Fully implemented.** The signed Today response paused the actual live task despite a different stale payload, recorded the durable `endingWorkday` reason, closed the interval, and restored with no active commitment after restart (`PromptResponseEffectRouter.swift`; `TaskExecutionStore.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [ ] Ignore or dismiss the prompt. **Status: Partially implemented.** Today renders prompt actions and conditionally exposes Dismiss, the store persists eligible dismissal, and mandatory prompts are protected from accidental dismissal. Behavior-coaching prompts and the installed click-through remain incomplete (`DashboardView.swift`; `PromptInboxStore.swift`; `.audit/runs/today-prompt-inbox/verifier/REPORT.md`).
- [x] Avoid having the same action happen twice after clicking more than once. **Status: Fully implemented.** The focused coaching journey replays every response token from the notification surface after a dashboard response and produces no second mutation. The signed run then retained exactly four responses, four applied effects, two intervals, one sprint, and two pauses across app and helper restart, with all four decisions shown once in history (`PromptInboxStore.swift`; `PromptResponseEffectRouterTests.swift`; `.audit/runs/coaching-response-actions/e33558dbb2bcc4a6acbff2b5ffa992ad3f2ffc9e/REPORT.md`).
- [ ] See a refreshed state instead of applying an action from an outdated prompt. **Status: Partially implemented.** Today refreshes the timeline after action success and after stale or failed action responses, while the store rejects already-resolved tokens. Coaching-specific stale validation and signed UI proof remain (`AppModel.swift`; `PromptInboxStore.swift`; `.audit/runs/today-prompt-inbox/verifier/REPORT.md`).

## 35. Prompt frequency and escalation

- [ ] Receive no more than six behavior interventions during a default workday. **Status: Touches remaining.** The behavior producer now counts only same-local-day gaming-drift decisions and enforces the configured default cap of six across restart; focused cap-boundary tests pass, while the signed capped run proved real delivery under cap one rather than a full six-intervention day (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-intervention-guardrails/verifier/REPORT.md`).
- [ ] Avoid having estimate requests and source warnings counted against the behavior cap. **Status: Touches remaining.** Daily cap accounting now scopes exclusively to `GAMING_DRIFT` decision keys, and focused tests prove plan-ready and source-warning episodes do not consume it. The signed run proved a real behavior decision under cap one, but did not seed both unrelated prompt types before delivery (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-intervention-guardrails/verifier/REPORT.md`).
- [ ] Receive no duplicate prompt during its cooldown. **Status: Touches remaining.** The producer now enforces decision-key deduplication, the configured cross-session cooldown, and response-specific pause windows across restart. The signed run rendered one prompt and applied its response exactly once, while a full installed expiry-and-reprompt boundary remains (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-intervention-guardrails/verifier/REPORT.md`).
- [ ] Receive a 15-minute pause after a gentle nudge. **Status: Touches remaining.** A gentle response now suppresses generic behavior interventions for exactly fifteen minutes, with deterministic 14-minute and 15-minute boundary tests passing. The installed signed journey produced and answered the gentle prompt, but the real-time boundary was outside the capped UI window (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-intervention-guardrails/verifier/REPORT.md`).
- [ ] Receive a 20-minute pause after answering an accountability prompt. **Status: Touches remaining.** An accountability response now suppresses generic behavior interventions for exactly twenty minutes, with deterministic 19-minute and 20-minute boundary tests passing. Installed accountability response and real-time boundary capture remain (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-intervention-guardrails/verifier/REPORT.md`).
- [ ] Receive the selected snooze duration after choosing a snooze. **Status: Touches remaining.** The fixed five-minute choice now persists its response timestamp, survives restart, and produces one follow-up with `snoozeDurationMinutes: 5`, but arbitrary end-user duration selection is not implemented (`GamingDriftPromptService.swift`; `.audit/runs/five-minute-coaching-followup/verifier/REPORT.md`).
- [ ] Receive a 45-minute same-behavior pause after an intentional override. **Status: Not implemented.** `continueIntentionally` is only an unused action enum; no override window is stored or enforced.
- [ ] Receive no more prompts that day after choosing `I am done today`. **Status: Touches remaining.** The five-more follow-up now surfaces `I am done today`, the response router pauses the active task with the durable end-workday reason, and the producer suppresses later same-local-day behavior prompts while resetting eligibility on the next local day. Focused same-day, restart, and next-day tests pass; the installed end-day restart journey remains (`GamingDriftPromptService.swift`; `PromptResponseEffectRouter.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/behavior-intervention-guardrails/verifier/REPORT.md`).
- [ ] See later drift recorded quietly after reaching the prompt cap. **Status: Touches remaining.** The behavior producer now upserts one privacy-safe `quiet_drift_episodes` row per same-day gaming session after the configured prompt cap, grows the same session idempotently, and creates no extra prompt. Focused cap-one, distinct-session, same-session-growth, migration, day-scope, and restart tests pass. The signed verifier proved the one-row growth and zero-prompt database invariants, but its helper-triggered session used an unreadable baseline seed and therefore did not independently exercise the live cap transition (`GamingDriftPromptService.swift`; `AutonomousDatabaseMigrator.swift`; `.audit/runs/post-cap-drift-review/verifier/REPORT.md`).
- [ ] See quietly recorded drift summarized only during review. **Status: Touches remaining.** The signed Daily Review showed one factual `PROMPT CAP REACHED` card with one later episode, 71 observed minutes, the largest episode, and Steam, while Today showed zero waiting decisions. The card and one-row ledger survived app and helper restart, and focused tests prove matching-day-only aggregation. A complete naturally triggered signed cap transition remains before full acceptance (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/post-cap-drift-review/verifier/REPORT.md`).
- [ ] See coaching return to observation after aligned work resumes. **Status: Not implemented.** `CoachingState` is display-only and no behavior episode state machine returns it to observation.
- [ ] See coaching de-escalate after starting a break, pausing work, rescheduling, or ending the day. **Status: Not implemented.** Task commands exist, but they are not connected to behavior-coaching escalation state.

## 36. Intentional gaming

- [ ] Choose to continue gaming intentionally. **Status: Touches remaining.** Accountability gaming prompts now expose `Continue intentionally`, persist the factual response, and resolve the prompt atomically. Focused tests and the signed QA package pass, but the installed notification interaction was not completed in the capped acceptance run (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/intentional-gaming-override/verifier/REPORT.md`).
- [ ] See the current prompt close immediately. **Status: Touches remaining.** Responding with `continue_intentionally` resolves the episode and removes it from the unresolved inbox in the persisted store. The installed visible close transition still needs UI automation proof (`GamingDriftPromptServiceTests.swift`; `.audit/runs/intentional-gaming-override/verifier/REPORT.md`).
- [ ] Avoid another equivalent prompt during the override window. **Status: Touches remaining.** A persisted intentional response suppresses equivalent gaming coaching for the user-configured duration across service restart while leaving daily caps active. Focused proof passes at 25 minutes, but installed runtime suppression remains (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/intentional-override-policy/verifier/REPORT.md`).
- [ ] Continue seeing gaming time tracked. **Status: Partially implemented.** Screenwatch observations are classified and the Today dashboard shows gaming usage, but this was not exercised through an intentional-override journey.
- [ ] See incomplete priority work remain visible without moral judgment. **Status: Partially implemented.** Planned and incomplete tasks remain visible in Today with neutral state labels, but there is no intentional-gaming context tying the behavior to that presentation.
- [ ] See the intentional choice recorded factually in the review. **Status: Not implemented.** There is no daily review UI or intentional-choice review record.
- [ ] Return to work before the override ends. **Status: Touches remaining.** Two continuous minutes of correction-aware aligned work now end the intentional override early and permit coaching to resume if a later qualifying gaming session develops. Installed end-to-end transition proof remains (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/intentional-gaming-override/verifier/REPORT.md`).
- [ ] Receive normal coaching again after the override expires if conditions still apply. **Status: Touches remaining.** After the configured duration, a still-qualifying gaming session receives a new persisted decision key and can prompt again without being blocked by the generic coaching cooldown. Focused restart proof passes with a 25-minute duration and reprompt after 26 minutes; installed expiry proof remains (`GamingDriftPromptService.swift`; `GamingDriftPromptServiceTests.swift`; `.audit/runs/intentional-override-policy/verifier/REPORT.md`).

## 37. Dashboard active-task status

- [ ] See the active task in the Today dashboard. **Status: Touches remaining.** Today now gives the canonical active row a named timing mode, tracked duration, factual manual-session explanation, Pause menu, and Complete control with stable accessibility evidence. Focused tests prove the canonical state and restart reconstruction, but the signed Reminder seed failed closed before a controlled active task could be rendered and relaunched (`ActiveCommitmentPresentation.swift`; `TodayDashboardCommandOverview.swift`; `.audit/runs/active-commitment-visibility/verifier/REPORT.md`).
- [ ] See elapsed time for an open-ended task. **Status: Frontend only left.** `TodayTaskRow.elapsedMinutes` and `TaskExecutionStore.elapsedMinutes` are implemented and tested, but the current Today UI never renders elapsed minutes.
- [ ] See remaining time for a sprint. **Status: Touches remaining.** The signed installed Today dashboard visibly rendered the active bounded-sprint countdown and the active task stayed available with pause and complete controls; the full persisted lifecycle remains (`.audit/runs/bounded-sprints/verifier/REPORT.md`).
- [x] See estimate or meaningful progress where appropriate. **Status: Fully implemented.** The signed installed Today dashboard visibly presented the same canonical 5-minute estimate and factual tracked progress in active, paused, relaunched, and over-estimate states, with stable accessibility identifiers on the primary focus and detailed-row surfaces (`TodayDashboardCommandOverview.swift`; `DashboardView.swift`; `.audit/runs/task-estimate-progress/verifier/REPORT.md`).
- [ ] See the dashboard update when pausing, resuming, switching, or completing a task. **Status: Touches remaining.** All commands are wired through `AppModel.applyTaskCommand`, snapshot refreshes, and passing tests cover transitions and switching, but a complete installed-app lifecycle journey was not performed.
- [ ] Use the compact active-task view without losing essential task state. **Status: Touches remaining.** The compact menu now shares the exact runtime boundary used by Today, rejects delayed fallback snapshots that would overwrite newer controller truth, and retains its previously tested deduplicated active, paused, break, recommended, and blocked action model. All 27 focused tests passed, and exact-PID signed QA acceptance proved that Today could start the fixture task, compact Refresh displayed the same active task with its full native action set, and compact Pause persisted one closed interval plus canonical paused state. Reopening the compact popover then incorrectly displayed no task instead of the paused task and Resume action, so native Resume, Complete, Blocked reason, helper-failure stale preservation, and relaunch restoration remain incomplete (`MenuBarCoachState.swift`; `MenuBarCoachView.swift`; `ZoidCoachApp.swift`; `.audit/runs/compact-active-task/helper-repair-c8c478/REPORT.md`).
- [ ] Open the full dashboard for totals, gaming budget, coaching state, and primary actions. **Status: Partially implemented.** The full Today dashboard shows behavior totals, gaming budget, and task actions, but behavior-coaching state and intervention controls are absent.
- [ ] See the task activity disappear when no useful active state remains. **Status: Touches remaining.** Snapshot state removes `activeTask` after completion and tests verify completion refresh; installed UI cleanup was not verified end to end.
- [ ] Avoid distracting updates more often than necessary. **Status: Partially implemented.** The UI refreshes after explicit commands rather than on a rapid visual timer, but there is no usability test or explicit update-throttling contract.

## 38. Prompt fallback surfaces

- [ ] Receive a macOS notification when notification permission is available. **Status: Partially implemented.** `PromptNotificationCoordinator` schedules authorized plan, meeting, and plan-change notifications, but behavior prompts are not produced and live delivery was not exercised.
- [ ] See a menu bar badge if notifications are unavailable. **Status: Touches remaining.** `MenuBarExtra` now switches to `exclamationmark.bubble.fill`, exposes the exact waiting-decision count, and offers direct fallback copy plus Today navigation when notification health is unavailable and unresolved prompts exist. Focused state tests passed and the signed app proved durable zero, one, and two-decision Today states, resolution clearing, notification repair, and restart recovery. macOS did not expose the actual status item through app accessibility, and SystemUIServer inspection timed out, so the signed badge, label, copy, and Today button still require one addressable menu-bar acceptance run (`MenuBarCoachState.swift`; `MenuBarCoachView.swift`; `ZoidCoachApp.swift`; `.audit/runs/menu-bar-prompt-fallback/verifier/REPORT.md`).
- [ ] Find every unresolved prompt in the dashboard. **Status: Touches remaining.** Today now groups every durable due prompt under Awaiting your response and separates future snoozes from recent resolved history. Focused persistence coverage passes, but the installed helper could not refresh the seeded fixture journey (`PromptInboxTimeline.swift`; `DashboardView.swift`; `.audit/runs/today-prompt-inbox/verifier/REPORT.md`).
- [ ] Respond from any available surface. **Status: Touches remaining.** The signed canonical prompt was independently resolved from both the notification action and Today dashboard through the same durable XPC store, but unrelated behavior action kinds and the absent menu-bar prompt surface still need completion (`PromptNotificationCoordinator.swift`; `TodayDashboardXPC.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`).
- [ ] See all other surfaces update after responding once. **Status: Partially implemented.** Today now shows prompt-specific Applying feedback and refreshes from the durable response, while an older notification consumes the same stored decision if used. Delivered-notification withdrawal and menu-bar synchronization are still absent, so the UI copy explicitly avoids promising immediate cross-surface refresh (`PromptActionPresentation.swift`; `TodayPromptInboxLedger.swift`; `.audit/runs/prompt-action-feedback/verifier/REPORT.md`).
- [ ] Avoid having two surfaces start duplicate sprints or apply the same choice twice. **Status: Touches remaining.** Today disables every prompt action during one in-flight write and shows Applying only on the selected row. Deterministic tokens, response idempotency, conflicting-token rejection, and exactly-once effects persist across restart in focused tests; the simultaneous installed Today-notification race remains (`PromptInboxStore`; `PromptActionPresentation.swift`; `.audit/runs/prompt-action-feedback/verifier/REPORT.md`).
- [ ] See only the latest relevant notification when delivery is throttled. **Status: Touches remaining.** Plan-ready and plan-changed now share one replacement group, while meeting, gaming, wake, and onboarding groups remain independent and the previous accepted notification survives a refused replacement. Focused coordinator tests and the signed fixture proved latest plan, meeting, and gaming records alongside all durable Today decisions and restart persistence. Real Notification Center remains the final proof gap because signed QA mode intentionally routes notifications to the isolated fixture (`PromptNotificationRelevance`; `PromptNotificationCoordinator`; `.audit/runs/latest-relevant-notification/verifier/REPORT.md`).
- [ ] Continue using all task controls without notifications. **Status: Touches remaining.** The signed isolated app retained local task creation, estimate selection, start, bounded sprint, begin focus, defer, block, remove, and recommendation controls while notification delivery was unavailable. Focused menu state coverage also keeps active-task controls in the fallback menu. The actual status-item controls remain unclickable through macOS accessibility, so one signed menu-bar acceptance run is still required (`MenuBarCoachView.swift`; `MenuBarCoachStateTests`; `.audit/runs/menu-bar-prompt-fallback/verifier/REPORT.md`).

## 39. Pausing coaching

- [x] Pause coaching for one hour. **Status: Fully implemented.** The signed Settings action persisted an exact boundary sixty minutes later, displayed the Cairo resume time, and restored the same active pause and Resume Now action after relaunch (`AutomationPause`; `SettingsView.swift`; `.audit/runs/timed-coaching-pause/b9f655b20d62d2455f5b78c6380da53fb4b72471/REPORT.md`).
- [x] Pause coaching until tomorrow. **Status: Fully implemented.** The signed Settings action displayed the next local midnight and persisted `2026-07-13T21:00:00Z`, exactly 14 July at 12:00 AM in Africa/Cairo. Focused rules cover timezone-aware calendar day transitions (`AutomationPause`; `SettingsPolicyController.swift`; `.audit/runs/timed-coaching-pause/b9f655b20d62d2455f5b78c6380da53fb4b72471/REPORT.md`).
- [x] Resume coaching before the pause expires. **Status: Fully implemented.** Resume Now immediately persisted Running as the next agent-saved policy version, restored all pause choices, and preserved unrelated unsaved Settings draft values in focused persistence proof (`SettingsPolicyController.swift`; `SettingsPolicyDraftTests.swift`; `.audit/runs/timed-coaching-pause/b9f655b20d62d2455f5b78c6380da53fb4b72471/REPORT.md`).
- [x] See clearly that coaching is paused. **Status: Fully implemented.** The signed card visibly distinguished Running, one-hour, next-midnight, and indefinite states, explained that tracking continues, exposed stable accessibility identifiers, and changed automatically from a shortened paused boundary to Running on its thirty-second refresh after expiry (`SettingsView.swift`; `.audit/runs/timed-coaching-pause/b9f655b20d62d2455f5b78c6380da53fb4b72471/REPORT.md`).
- [ ] Continue tracking tasks and behavior while coaching prompts are paused. **Status: Partially implemented.** Task and Screenwatch pipelines are separate from the saved automation-pause flag, but there is no end-to-end test proving ingestion continues throughout a pause.
- [ ] Avoid receiving behavior interventions during the pause. **Status: Partially implemented.** Gaming drift production and every shared autonomous-action gate now read the effective timed pause dynamically, so an active boundary suppresses new work and expiry restores eligibility without restart. A newly eligible signed behavior intervention was not produced and suppressed during this acceptance run (`GamingDriftPromptService.swift`; `AgentMain.swift`; `.audit/runs/timed-coaching-pause/b9f655b20d62d2455f5b78c6380da53fb4b72471/REPORT.md`).
- [x] End the workday from the pause controls. **Status: Fully implemented.** The signed Settings card exposed one accessible active-task-specific action, required destructive confirmation, preserved the active task after cancellation and helper failure, and applied exactly one durable `endingWorkday` pause after success (`EndWorkdayReviewController.swift`; `.audit/runs/pause-end-workday-review/verifier/REPORT.md`).
- [ ] Disable notification prompts while retaining dashboard access. **Status: Touches remaining.** The installed signed QA app saved the dedicated Settings switch off through the live helper, removed only the queued coaching notification after one agent loop, kept the same unresolved decision visibly available in Today, preserved an unrelated break-end reminder, wrote no false delivery-ledger attempt, and restored the disabled policy plus Today decision after app and helper restart. Focused proof also passes legacy defaulting, concurrent Settings conflict handling, and re-enable without coordinator restart, but the capped signed run stopped before visibly completing the final re-enable and resumed-delivery leg (`.audit/runs/notification-prompt-preference/verifier/REPORT.md`).
- [ ] Stop Screenwatch ingestion without deleting existing data. **Status: Partially implemented.** Signed Settings now exposes an `INGEST NEW SCREENWATCH ACTIVITY` toggle with stable accessibility semantics and explicit copy that pausing keeps existing Zoid 666 history and every source-owned Screenwatch file. The versioned policy defaults legacy documents to enabled, persists a pause, merges unrelated concurrent Settings edits, gates live and maintenance ingestion before the source is opened, and re-reads policy each watch loop so resume needs no agent restart. Focused tests and one signed release package passed, but the capped signed journey stopped because the isolated baseline observation had not imported or created a canonical-source checkpoint before the bounded fixture deadline; pause across two loops, relaunch persistence, exactly-once resume, and concurrent signed save remain unproven (`ScreenwatchIngestionControl.swift`; `UserPolicy.swift`; `AgentMain.swift`; `SettingsView.swift`; `.audit/runs/screenwatch-ingestion-control/verifier/REPORT.md`).

## 40. End-of-day review trigger

- [x] Open the review at the configured review time. **Status: Fully implemented.** The installed signed app exposed and persisted an accessible Daily review time, replaced its scheduled daily notification without duplicates after two time changes, deferred a 23:30 choice to the 07:00 quiet-hours boundary, and preserved policy version 4 plus exactly one pending reminder after app and helper restart (`ReviewReminderService.swift`; `SettingsView.swift`; `.audit/runs/configurable-review-time/verifier/REPORT.md`).
- [x] Open the review immediately after ending the workday manually. **Status: Fully implemented.** The signed success path refreshed Today and opened the current-day Review with visible work evidence only after the durable command succeeded, while failure remained in Settings; helper and app relaunch restored the end-workday pause with no active timer (`SettingsView.swift`; `.audit/runs/pause-end-workday-review/verifier/REPORT.md`).
- [ ] Find an unfinished previous-day review on the next launch. **Status: Not implemented.** No review draft/persistence lifecycle exists.
- [x] Delay the review without losing the day's evidence. **Status: Fully implemented.** In the signed isolated QA app, Review Tomorrow persisted an exact future time and preservation copy, stayed quiet before the boundary across relaunch, resumed early through Resume Review Now, and returned automatically after the due boundary with the saved correction, task attachment, and personal note unchanged (`DailyReviewView.swift`; `DailyReviewStore.swift`; `.audit/runs/daily-review-deferral/verifier/REPORT.md`).
- [x] Resume an unfinished review after restarting the app. **Status: Fully implemented.** The installed signed app persisted a Daily Review correction across two pre-confirmation relaunches, exposed an accessible cross-day Resume action, restored the exact day and corrected classification, then cleared the unfinished notice only after confirmation and kept it cleared after a final relaunch (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/unfinished-review-resume/verifier/REPORT.md`).
- [x] Skip the review explicitly and close the day. **Status: Fully implemented.** The signed installed Review exposed an accessible consequence-first Skip action, preserved the open review after Cancel, closed it into one dated skipped state after confirmation, returned to Today without an unfinished prompt, preserved the state across app and helper relaunch, reopened after a note edit, and safely skipped again without losing the note. Focused persistence proof also preserved corrected classification, task attachment, and cleared deferral across repeated Skip and store reconstruction (`DailyReviewView.swift`; `DailyReviewStore.swift`; `DailyReviewTests.swift`; `.audit/runs/skip-daily-review/verifier-69fd38f/REPORT.md`).

## 41. Understanding the daily review

- [x] See whether the main objective was completed. **Status: Fully implemented.** The signed installed Daily Review showed completed, unfinished, and not-designated main-objective states for three selected policy-local days, named the exact objective, excluded another day's completion, and reproduced the completed state after app relaunch (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/daily-review-plan-outcomes/verifier/REPORT.md`).
- [x] See how many priority tasks were completed. **Status: Fully implemented.** The signed review showed one of two non-optional priorities completed, omitted a completed optional task, kept a priority completed the next day open for the selected day, exposed explicit zero-count and no-plan states, and reproduced the same count after relaunch (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/daily-review-plan-outcomes/verifier/REPORT.md`).
- [ ] See total active-task time. **Status: Touches remaining.** The installed signed Daily Review visibly rendered the selected day's 60-minute active-task total from persisted task intervals and restored the same local evidence after application relaunch (`DailySourceCoverageStore.swift`; `DailySourceCoverageView.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] See aligned work time separately. **Status: Touches remaining.** The signed review visibly separated active-task, Screenwatch-observed, and work-aligned minutes, while focused tests prove interval merging and correction-aware alignment without rewriting source evidence (`DailySourceCoverageStore.swift`; `DailySourceCoverageTests.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] See work broken down by category. **Status: Barely started.** Today can group application observations by classification, but no review categories or historical review presentation exist.
- [ ] See gaming and distraction time. **Status: Touches remaining.** The installed signed review visibly rendered separate gaming and distraction totals and distinct source sessions, while missing time remained outside both categories (`DailySourceCoverageStore.swift`; `DailySourceCoverageView.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] See reliable idle time. **Status: Touches remaining.** The signed limited-coverage review visibly rendered idle as `not reliable`, and focused tests prove that healthy adequate coverage exposes observed idle minutes, but the reliable-idle branch was not captured visibly in the installed app (`DailySourceCoverage.swift`; `DailySourceCoverageTests.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] See unknown or missing coverage. **Status: Touches remaining.** The signed review visibly exposed unknown, missing, observed-task, and source-cause fields, explicitly refused to infer unobserved minutes into a behavioral category, and retained the underlying sessions separately (`DailySourceCoverage.swift`; `DailySourceCoverageView.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [x] See the best work block. **Status: Fully implemented.** The signed review showed the longest corrected observed work session with app, duration, classification, and policy-local time; a corrected earlier YouTube block won a true five-minute tie over later Xcode, the empty state was explicit, and relaunch preserved the result (`DailyReview.swift`; `DailyReviewStore.swift`; `.audit/runs/daily-review-highlights/verifier/REPORT.md`).
- [x] See the largest drift episode. **Status: Fully implemented.** The signed review selected a three-minute Steam gaming episode over a two-minute Safari distraction, exposed factual corrected-observation copy and local time, showed an explicit not-observed state on an empty day, and preserved the result after relaunch (`DailyReview.swift`; `DailyReviewView.swift`; `.audit/runs/daily-review-highlights/verifier/REPORT.md`).
- [x] See coaching prompts and responses. **Status: Fully implemented.** The signed review showed applied, effect-pending, and unanswered behavior-coaching states with action, surface, and policy-local time, excluded an unrelated planning prompt, exposed a factual empty state, and reproduced the filtered history after relaunch (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/daily-review-highlights/verifier/REPORT.md`).
- [ ] See intentional gaming overrides. **Status: Not implemented.** Overrides are not stored.
- [ ] See estimate-versus-actual comparisons. **Status: Touches remaining.** Daily Review now shows each recorded priority estimate beside its selected-day completion state, and estimate-learning aggregates retain estimate/actual samples, but the task-level actual duration and direct comparison are not yet presented together (`DailyReviewView.swift`; `.audit/runs/daily-review-plan-outcomes/verifier/REPORT.md`).
- [ ] Understand when incomplete data makes a total less precise. **Status: Touches remaining.** The signed review paired its limited-coverage warning with `about` values, source status, last-check evidence, unreliable idle, and explicit language that rounded categories are not a complete account of the day (`DailySourceCoverage.swift`; `DailySourceCoverageView.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] Receive a complete factual review without AI. **Status: Touches remaining.** With the persisted provider set to Disabled, Daily Review now identifies its local factual boundary, states the exact observed-session count, keeps missing time unobserved, and preserves correction, hypothesis rejection, and confirmation without model setup. Focused local-store and restart tests pass; the capped signed interaction timed out before completion (`RulesOnlyReviewState.swift`; `DailyReviewView.swift`; `.audit/runs/rules-only-review/verifier/REPORT.md`).

## 42. Facts, hypotheses, and review corrections

- [ ] See observed facts labeled separately from context and hypotheses. **Status: Not implemented.** No review model or facts/hypotheses UI exists.
- [ ] See evidence supporting a causal hypothesis. **Status: Touches remaining.** Weekly pattern cards now separate the hypothesis, observed samples, confidence, evidence, alternative explanation, and an explicit not-proven-cause boundary; the signed app kept a private task title out of the collapsed accessibility summary, revealed it only after keyboard expansion, and removed it again on collapse. Focused presentation and store coverage passed 13/13, while a signed app-and-helper relaunch against unchanged persisted rows remains outstanding (`WeeklyReviewView.swift`; `WeeklyReviewPatternPresentation.swift`; `WeeklyReviewPatternPresentationTests.swift`; `.audit/runs/weekly-causal-evidence/verifier/REPORT.md`).
- [ ] Reject a causal hypothesis. **Status: Touches remaining.** Daily Review exposes a labeled `REJECT` action, persists the rejected decision locally, preserves it through confirmation, and resets it to pending after a later correction so stale conclusions cannot survive changed evidence. Focused persistence and rules-only tests pass, while the installed signed app still needs a direct Reject click followed by app-and-helper relaunch proof (`DailyReviewView.swift`; `DailyReviewStore.swift`; `DailyReviewTests.swift`; `.audit/runs/daily-review/7b96623/REPORT.md`; `.audit/runs/rules-only-review/verifier/REPORT.md`).
- [x] Reclassify a behavior session from the review. **Status: Fully implemented.** The installed signed QA Reviews surface corrected a concrete Steam session from Unknown to Work, updated the visible category, and preserved the correction after relaunch (`DailyReviewView.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [ ] Split or merge a session from the review when needed. **Status: Partially implemented.** Midpoint splitting is implemented and restart-tested, but arbitrary splits, adjacent-session merge, and fresh signed acceptance remain incomplete (`DailyReviewStore.swift`; `DailyReviewTests.swift`).
- [x] Add away-from-Mac work. **Status: Fully implemented.** The installed signed Reviews flow added a labeled 30-minute away-from-Mac entry, updated Actual Time and Away from Mac while keeping Screenwatch-observed time at zero, edited the same entry to 45 minutes without duplication, and preserved the entry, task, note, and separated totals after relaunch. Validation, privacy-safe copy, accessibility exposure, and scoped removal confirmation were also verified; focused persistence tests prove deletion leaves Screenwatch observations unchanged (`DailyReviewView.swift`; `DailyReviewStore.swift`; `DailyReviewTests.swift`; `.audit/runs/offline-work/9cb54454/REPORT.md`).
- [ ] Correct a task's completion state. **Status: Not implemented.** Today can complete a task, but there is no review correction or reopen operation.
- [ ] Add a personal note. **Status: Partially implemented.** Daily Review now has a day-scoped, nullable, trimmed, 1,000-character personal note that reopens a confirmed review, preserves evidence rows, survives store reopen, clears to absent, and migrates existing reviews without rewriting them. A signed populated review visibly showed the local-only boundary, stable editor/save accessibility, unchanged two-minute actual and one-minute work totals, a live `38 / 1000` counter, and an enabled Save action after entry. The save could not be verified because the next Computer Use capture failed twice with ScreenCaptureKit `-3811`; the database still had no note, so signed save, confirm/edit reopen, day switching, relaunch, clear, and over-limit behavior remain unproven (`DailyReview.swift`; `DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/daily-review-personal-note/verifier/REPORT.md`).
- [ ] Change tomorrow's main task. **Status: Partially implemented.** A user can choose the main objective for the current daily plan, but there is no tomorrow/review-specific flow.
- [ ] See totals and conclusions update after corrections. **Status: Touches remaining.** Daily Review now recalculates factual category totals immediately and reopens a prior confirmation after edits, but every downstream conclusion and weekly statement has not been visibly rechecked for this correction journey.
- [ ] Confirm the corrected review. **Status: Touches remaining.** Confirmation state, invalidation after a later correction, and restart persistence are implemented and focused-tested, but this signed acceptance stopped after proving correction-to-rule adaptation rather than pressing the final confirmation control.
- [x] Prevent unconfirmed hypotheses from becoming learned facts. **Status: Fully implemented.** In the installed signed-QA Weekly Review, one deterministically derived pattern began as `NOT LEARNED`; activating `ACCEPT HYPOTHESIS` once changed it to `LEARNED FROM EXPLICIT ACCEPTANCE`, removed the acceptance action, and created exactly one durable `review_hypothesis_promotions` row. Relaunch restored the learned state with one row, focused production-service tests proved repeated acceptance is idempotent, and the signed Settings Records flow then confirmed review-and-learning deletion, reduced the table to zero rows, re-seeded the same factual inputs, and relaunched to the same candidate as `NOT LEARNED` with zero promotions. This proves that derivation alone, restart, and re-derivation never cross the explicit user-confirmation boundary (`Sources/ZoidCoachApp/HypothesisLearningBoundary.swift`; `Sources/ZoidCoachApp/ReviewHypothesisLearningService.swift`; `Sources/ZoidCoachInfrastructure/ReviewHypothesisPromotionStore.swift`; `.audit/runs/review-hypothesis-learning/84623d934a4eff76ef56eb4787de4ac6fc4640f5/REPORT.md`).

## 43. Weekly review

- [x] Receive a weekly review after at least three days with acceptable coverage. **Status: Fully implemented.** The signed QA Reviews surface visibly changed from a 0/7 limited state to a factual 7/7 prior-week review after seven confirmed days with at least 30 observed minutes, while focused tests enforce the three-day gate (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Receive a data-quality summary instead of strong conclusions when evidence is insufficient. **Status: Fully implemented.** The clean signed QA app visibly showed `DATA QUALITY SUMMARY 0/7 DAYS`, the exact missing-day guidance, no patterns, and no experiment before sufficient evidence was seeded (`WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See completed outcomes and planned-versus-completed work. **Status: Fully implemented.** The signed review visibly reported 14 planned items, 525 estimated minutes, 5 completed items, and a 36 percent outcome rate explicitly labelled as not being a productivity score (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See estimate accuracy patterns. **Status: Fully implemented.** The signed review derived estimate accuracy only from eligible prior-week learning samples and visibly expanded three dated estimated-versus-corrected examples (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See the best work windows. **Status: Fully implemented.** The signed review visibly reported the median corrected aligned start time and duration from five eligible prior-week work-window samples, with dated evidence available on demand (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See frequent drift triggers. **Status: Fully implemented.** The signed review visibly identified the strongest corrected distracting or gaming application pattern with sample count, confidence, date range, and expandable evidence (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See gaming timing and budget adherence. **Status: Fully implemented.** The signed review visibly reported six corrected gaming-day samples against the configured 60-minute budget, and each expandable example includes corrected minutes and first observed local time (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See recovery success after coaching prompts. **Status: Fully implemented.** The signed review visibly reported that 1 of 1 answered prompts was followed by correction-aware observed work within 30 minutes and preserved a non-causal alternative explanation (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See which prompts were useful or ineffective. **Status: Fully implemented.** The canonical response-effect relation now drives the signed prompt follow-through pattern, which visibly reported 1 of 1 applied actions without mistaking correlation for behavioral proof (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See repeated blocked or vague tasks. **Status: Fully implemented.** The signed review visibly reported a repeatedly blocked task from two prior-week events and expandable evidence uses the local task title rather than an opaque identifier (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] See sample size, date range, examples, confidence, and alternative explanations for every pattern. **Status: Fully implemented.** Every signed pattern visibly exposed sample count, stable prior-week date range, confidence, an evidence control, privacy-safe dated examples, and an alternative explanation; the estimate pattern was expanded in the installed app (`WeeklyReview.swift`; `WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Receive no more than one primary behavioral experiment for the next week. **Status: Fully implemented.** Migration 31 enforces one experiment per review week and repeated loads preserve the same proposal; the signed review visibly showed exactly one primary experiment (`AutonomousDatabaseMigrator.swift`; `WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Accept the experiment. **Status: Fully implemented.** The installed signed app accepted the edited proposal, visibly changed it to `ACCEPTED`, displayed 2/7 next-week tracking, and preserved that state after relaunch (`WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Edit the experiment. **Status: Fully implemented.** The installed signed app changed the proposal title to `Make the first blocked step concrete`, saved it, showed success feedback, and preserved the edited text through acceptance and relaunch (`WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Reject the experiment. **Status: Fully implemented.** The installed signed app changed the accepted experiment to `REJECTED`, removed active tracking, disabled the repeated reject action, and preserved rejection after another relaunch (`WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Track an accepted experiment during the following week. **Status: Fully implemented.** The installed signed app visibly showed `TRACKING 2/7 DAYS · START 2026-07-11` after acceptance and again after relaunch, proving durable next-week progress from the stable prior-week review (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).

## 44. General settings

- [ ] Enable or disable launch at login. **Status: Touches remaining.** The installed signed Background Agent window visibly separated Enabled registration from Healthy and stale Attention runtime states, kept Disable available during a stale heartbeat, and presented truthful impact and local-data preservation copy. Focused relaunch proof prevents a user-disabled helper from silently re-registering and preserves byte-identical local data, but the installed Disable and Enable actions were not activated because the required action-time confirmation was not available (`AgentLaunchService.swift`; `AgentLifecycleController.swift`; `.audit/runs/launch-at-login-control/verifier/REPORT.md`).
- [x] Set workday start and end times. **Status: Fully implemented.** The signed Settings flow visibly presented Work starts and Work ends alongside explicit weekday controls, saved Tuesday as the only working day through policy version 2, restored the exact selection after app and helper restart, and disabled the final selected day. The running agent immediately rescheduled Daily Review to Tuesday and the restarted Monday dashboard exposed zero configured work capacity, while 33 focused Settings tests cover time persistence, multi-window preservation, deliberate normalization, and conflict recovery (`SettingsPolicyDraft.swift`; `SettingsView.swift`; `.audit/runs/settings-workdays/verifier/REPORT.md`).
- [ ] Set planning and review times. **Status: Touches remaining.** Nightly planning, morning confirmation, and Daily review are now visible versioned policy controls, and the installed signed app persisted Daily review through multiple saves and restart with stable accessibility. A signed edit-and-relaunch pass for both planning-time controls remains (`SettingsPolicyDraft.swift`; `SettingsPolicyConflict.swift`; `.audit/runs/configurable-review-time/verifier/REPORT.md`).
- [ ] Use manual workday start and end without fixed hours. **Status: Not implemented.** No manual-workday mode or start/end controls exist.
- [x] Configure time-zone behavior. **Status: Fully implemented.** The installed signed-QA Settings flow exposed the full IANA picker, current UTC offset, Mac-zone comparison, and direct Mac-zone restore; `America/Los_Angeles` persisted through save, reopen, and app-plus-helper restart, while independent and overlapping concurrent policy edits preserved their correct values and required deliberate reapply only for the competing time-zone choice (`SettingsView.swift`; `SettingsPolicyDraft.swift`; `SettingsPolicyConflict.swift`; `.audit/runs/settings-time-zone-policy/aedc669a5cc5da5457684a670b690f8bcb060de4/evidence.json`).
- [ ] Configure keyboard shortcuts. **Status: Partially implemented.** Voice hotkey presets are configurable in the Voice section, but there is no general shortcut configuration for task and coaching actions.
- [ ] See current permission and connection states. **Status: Touches remaining.** The installed signed-QA Settings journey now proves passive Ready to Connect, Connected, Access Needed, Refresh Failed, retry recovery, and durable last-success states for Reminders without opening a permission prompt. Source health and macOS permission ledgers also expose Calendar, notifications, agent, Screenwatch, Accessibility, Automation, and screen recording, but several non-Reminders statuses remain coarse (`.audit/runs/reminders-recovery/verifier/REPORT.md`).
- [ ] Change included Reminder lists. **Status: Touches remaining.** Settings now exposes permission state, System Settings repair, in-place recheck, discovery retry, explicit local-only configuration, deleted-list notices, and stable-ID Include or Exclude controls. Saves use CAS rebasing, preserve concurrent unrelated settings, expose explicit Keep Current Values and Reapply My Changes decisions for overlapping edits, and refresh visible tasks exactly once. Deterministic signed-QA restart, rename, stale-writer rejection, and duplicate-free retry proof passes, while the Settings conflict click-through remains unperformed.
- [ ] Change the Screenwatch folder. **Status: Touches remaining.** Settings now visibly exposes Recheck, Choose Folder, and conditional Use Expected Folder controls backed by the canonical private bookmark, and the signed picker opened with correct days-folder guidance. Native alternate selection was not completed within the capped accessibility run, so this remains short of full end-to-end proof (`ScreenwatchConnectionView.swift`; `.audit/runs/screenwatch-recovery-ux/verifier/REPORT.md`).
- [ ] Test Screenwatch health. **Status: Touches remaining.** Source Check and Screenwatch Refresh inspect schema-valid, stale, and missing streams with passing reader tests. One post-recovery verifier run confirmed fresh ingestion, while the later recheck correctly failed when Screenwatch became stale; the UI test action itself was not exercised.
- [ ] See notification authorization and delivery state. **Status: Touches remaining.** Migration 36 now retains privacy-safe authorization fallback, macOS acceptance, deterministic QA delivery, scheduling failure, timestamp, attempt, and replacement outcomes for 30 days, and Settings Signals visibly exposes the new authorization/history card with refresh and privacy copy. The first signed run proved the card renders, but it also exposed and then fixed a QA composition bug before populated history could be visibly rechecked (`NotificationDeliveryLedger.swift`; `NotificationDeliveryHealthView.swift`; `.audit/runs/notification-delivery-health/verifier/REPORT.md`).
- [ ] Send a test notification. **Status: Touches remaining.** The installed signed-QA Settings flow reached Signals and activated Send Test through stable native accessibility controls, truthfully reported the existing unavailable authorization with the exact manual repair route and Today fallback, converted two immediate AX press requests into exactly one privacy-safe ledger attempt, and restored the readable `TODAY FALLBACK ... TRY 1` history after app and helper relaunch; seven focused tests and one release build pass, while an already-authorized identity must still prove real macOS acceptance and visible notification appearance (`OnboardingDeliveryTestService.swift`; `SettingsNotificationDeliveryTestController.swift`; `.audit/runs/settings-notification-delivery-test/independent-verifier/REPORT.md`).
- [ ] See notification authorization status. **Status: Touches remaining.** Production maps macOS authorization into Source Health, while signed QA now resolves the isolated fixture service instead of consulting the production notification center. The signed onboarding step visibly showed fixture Healthy, and focused tests prove denied and unavailable states, but the rebuilt Settings card was not rerun after the isolation fix (`NotificationService.swift`; `NotificationDeliveryHealthView.swift`; `.audit/runs/notification-delivery-health/verifier/REPORT.md`).

## 45. Coaching and classification settings

- [ ] Choose the coaching mode. **Status: Partially implemented.** Observe, Suggest, Approve actions, and Autonomous operating modes exist and now participate in conflict-safe Settings saves, but these control automation authority rather than behavior-coaching style.
- [ ] Choose the maximum intervention level. **Status: Touches remaining.** Settings now presents the persisted coaching level as an explicit hard maximum intervention ceiling with stable accessibility, Gentle and Accountability explanations, conflict-safe policy round-trip proof, and runtime tests that enforce the lighter 10-minute recovery without the accountability-only break action versus the stronger 20-minute recovery with the eligible break action while preserving prompt-cap and cooldown gates. The clean signed package installed with its QA agent, but an onboarding policy-store failure made Settings unreachable, so selecting Gentle, saving, relaunching, and proving its prompt boundary, then selecting Accountability, saving, relaunching, and proving its active-task prompt boundary remain before full end-to-end acceptance (`SettingsPolicyDraft.swift`; `SettingsView.swift`; `GamingDriftPromptService.swift`; `.audit/runs/maximum-intervention-level/candidate/REPORT.md`; `.audit/runs/maximum-intervention-level/verifier/REPORT.md`).
- [ ] Change the daily prompt cap. **Status: Touches remaining.** Settings now exposes a bounded 1-10 cap with a stable accessibility identifier, persists it through conflict-safe Gaming Policy mutation, and runtime tests prove cap 2 suppresses the third eligible session. Signed QA visibly changed the value from 1 to 2, but Save, relaunch, and seeded runtime enforcement were stopped before qualification (`SettingsPolicyDraft.swift`; `SettingsView.swift`; `GamingDriftPromptService.swift`; `.audit/runs/coaching-limits-settings/candidate/REPORT.md`).
- [ ] Change quiet hours. **Status: Touches remaining.** Quiet start/end controls persist in the versioned schedule policy, survive disjoint concurrent Settings edits, and the shared notification coordinator reads the latest policy for every scheduling decision. Deterministic tests prove that a changed policy affects the next decision without recreating the coordinator, but a signed Settings edit followed by a second prompt without restarting the helper remains (`PromptNotificationCoordinator.swift`; `QuietHoursDeliveryBoundaryTests.swift`; `.audit/runs/quiet-hours-delivery/7a216a88032234e5d7a442e46a8052a16b283b92/REPORT.md`).
- [ ] Change cooldowns. **Status: Touches remaining.** Settings now exposes a bounded 5-1,440 minute cooldown in five-minute steps, preserves legacy coaching-level defaults and concurrent edits, and runtime tests prove 35 minutes suppresses at 30 and permits at 36. Signed QA visibly changed 180 to 35 minutes, but Save, relaunch, and helper-restart persistence were stopped before qualification (`SettingsPolicyDraft.swift`; `SettingsView.swift`; `GamingDriftPromptService.swift`; `.audit/runs/coaching-limits-settings/candidate/REPORT.md`).
- [x] Change the task-start grace period. **Status: Fully implemented.** The installed signed Settings UI exposed explicit task-start and return-from-idle grace controls with stable accessibility descriptions, saved 12 and four minutes through the running agent as policy V2, restored both values after closing Settings and after restarting the app and helper, then saved a shorter 10-minute task-start grace as policy V3 without restarting the helper. An isolated signed-runtime seed inside the 12-minute task boundary created no prompt or quiet-drift record and preserved one active task, one open activity interval, and all ten behavior observations; focused policy, draft, conflict, legacy, bounds, and decision tests passed 67 of 67 (`SettingsView.swift`; `GamingDriftPromptService.swift`; `.audit/runs/behavior-grace-controls/signed-acceptance/da20828655cb53f5a7ee0e7093c57f985a183c80/evidence.json`).
- [ ] Change the default coaching-pause duration. **Status: Touches remaining.** Settings now exposes a persisted Quick Pause default of 1 Hour, Until Tomorrow, or Indefinitely, preserves the legacy indefinite behavior, and resolves Until Tomorrow at the next midnight in the policy time zone. The signed installed UI saved each default, observed an exact one-hour pause boundary, observed an exact Africa/Cairo midnight boundary, and preserved the selected midnight boundary across helper and app restart before saving back to Indefinitely. Focused tests prove the menu-bar controller reloads the latest policy, applies every duration, automatically resumes timed pauses, and preserves unrelated concurrent edits, but the signed run did not directly click the menu-bar Quick Pause action, wait through an automatic boundary, or perform the concurrent-edit journey (`MenuBarCoachingPauseController.swift`; `.audit/runs/default-coaching-pause/verifier/REPORT.md`).
- [x] Review application rules. **Status: Fully implemented.** The signed-QA Settings ledger loaded 131 installed, observed, and saved applications with search plus All, Auto, Work, Communication, and Gaming filters. The visible verifier searched Discord, changed its distinct category, saved through the agent, killed and relaunched the app, and saw policy V3 with Communication still selected (`AppClassificationLedger.swift`; `.audit/runs/app-classification-management/candidate/REPORT.md`).
- [x] Review domain rules. **Status: Fully implemented.** The signed-QA user expanded Domain Rules in Settings and reviewed the exact 14 ordered built-in domain rows with a clear privacy explanation. Accessibility output proved the complete first-to-last rule set without exposing any visited URL, browsing history, or private window title, and four focused domain-rule tests passed (`DomainRuleReviewState.swift`; `AppClassificationLedger.swift`; `ContextualDomainRuleReviewTests.swift`; `.audit/runs/settings-safety-6038/REPORT.md`).
- [ ] Review project mappings. **Status: Not implemented.** No project-mapping model or UI exists.
- [x] Review unknown sessions. **Status: Fully implemented.** The signed Reviews surface exposed two Unknown sessions in chronological order with an exact 2-session, 3-minute summary, neutral evidence language, privacy-safe app/time/count details, separate classified activity, and an explicit no-pending-confirmation empty state after classification (`UnknownSessionReviewState`; `DailyReviewView.swift`; `.audit/runs/unknown-session-review/verifier/REPORT.md`).
- [ ] Enable or disable screenshot analysis. **Status: Touches remaining.** Onboarding and Settings expose the persisted ambiguous-only toggle, focused tests cover bootstrap adoption and Screenwatch policy mutation, and signed onboarding visibly rendered both Off and On states. A fresh signed package still must prove save-and-relaunch persistence after the final allowlist fix, then verify the running agent stops creating analysis evidence after the user turns the setting Off (`OnboardingCoordinatorTests.swift`; `PolicyStoreTests.swift`; `.audit/runs/screenshot-analysis-consent/candidate/REPORT.md`).
- [x] Import classification rules. **Status: Fully implemented.** The signed-QA native chooser accepted a reviewed JSON replacement only after previewing one Work, one Communication, and one Gaming rule and requiring destructive confirmation. Focused tests reject wrong types, folders, symlinks, oversized data, malformed documents, unsupported schemas, blanks, duplicates, and cross-category conflicts before draft mutation (`AppClassificationRulesDocumentService.swift`; `AppClassificationRulesDocumentServiceTests.swift`; `.audit/runs/app-classification-management/candidate/REPORT.md`).
- [x] Export classification rules. **Status: Fully implemented.** The signed-QA native destination chooser exported the visible draft, and inspection confirmed a normalized schema-only JSON document containing no unrelated settings or secrets. Atomic writing and symlink-destination rejection are covered by focused tests (`AppClassificationRulesDocumentService.swift`; `AppClassificationRulesDocumentServiceTests.swift`; `.audit/runs/app-classification-management/candidate/REPORT.md`).
- [ ] Reset learned rules. **Status: Touches remaining.** Reviews now shows the exact active learned-rule count and a targeted destructive reset with cancel, historical-preservation copy, immediate count refresh, and exact removed-count feedback. The store appends all active-rule tombstones atomically, remains idempotent when empty, and preserves historical corrections and task attachments across reopen; the signed installed cancel/reset/relaunch journey remains (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/learned-rule-reset/verifier/REPORT.md`).

## 46. AI settings and behavior

- [ ] Use rules-only mode with all Release 1 functionality available. **Status: Partially implemented.** Disabled-AI/local-only mode now preserves deterministic planning, task controls, gaming coaching, weekly review, and factual Daily Review correction and confirmation. Several remaining Release 1 scenarios and the complete installed offline journey are still incomplete (`RulesOnlyReviewState.swift`; `.audit/runs/rules-only-review/verifier/REPORT.md`).
- [ ] See that remote AI is off until explicitly configured. **Status: Touches remaining.** Defaults select Disabled and Local only, remote evidence is disabled for local providers, and policy plus conflict tests preserve a newer remote-evidence winner. The installed settings state and an offline relaunch were not verified end to end.
- [ ] Choose a future approved provider and model. **Status: Touches remaining.** Settings exposes Disabled, local Ollama, and Codex CLI according to capability gates, plus model/reasoning selection. Provider, model, and reasoning choices now participate in field-level conflict recovery, but actual provider execution was not exercised end to end.
- [ ] Choose local or remote processing when supported. **Status: Touches remaining.** Provider and Remote Evidence controls distinguish local-only, redacted, and explicit private-content modes, preserve concurrent winners, and now preview the actual automatic-planning boundary before save. Installed provider selection and real request inspection remain (`RemoteEvidencePreview.swift`; `SettingsView.swift`; `.audit/runs/ai-boundary-controls/verifier/REPORT.md`).
- [ ] Preview a representative redacted payload before enabling remote AI. **Status: Touches remaining.** Settings now shows a fixed example matching the real encoded planning fields, anonymizes task and application labels in redacted mode, reveals the private-content difference, and scopes exclusions truthfully from separately requested voice images. The signed installed three-policy inspection remains (`RemoteEvidencePreview.swift`; `CodexCLIPlanningAdvisor.swift`; `.audit/runs/ai-boundary-controls/verifier/REPORT.md`).
- [ ] Set a daily or monthly request budget. **Status: Touches remaining.** The signed Intelligence Settings flow now exposes separate accessible daily and monthly limits plus canonical saved usage, states that one provider-independent and model-independent bucket cannot be bypassed by switching, discards the complete unsaved Settings draft, saves 110 daily and 3,100 monthly through the agent, and restores both values after the app and helper relaunch. Three isolated Ollama and Codex model rows visibly aggregate to one daily and monthly count, reset copy identifies UTC enforcement boundaries, and a saved zero limit visibly preserves local planning, tracking, coaching rules, and reviews while stating that no paid request is sent. The destructive warning truthfully discloses the immediate usage reset and unchanged Keychain credentials, and Cancel preserves all rows and counts. Forty-three focused tests and one release build pass, but the final destructive `DELETE AI METADATA` confirmation still requires fresh action-time approval and verification of zero usage, preserved limits and credentials, and relaunch persistence (`AIBudgetStatusService.swift`; `SettingsPolicyController.swift`; `SettingsView.swift`; `AIBudgetStatusServiceTests.swift`; `SettingsPolicyDraftTests.swift`; `.audit/runs/ai-budget-finish/candidate/REPORT.md`; `.audit/runs/ai-budget-finish/verifier/REPORT.md`; `.audit/runs/ai-budget-finish/independent-verifier/REPORT.md`).
- [ ] Clear the AI cache. **Status: Touches remaining.** Intelligence Settings now exposes Clear AI Cache and Request History with destructive confirmation through the authenticated agent command. The deletion is limited to model runs, Codex jobs, and transmission receipts while preserving Keychain credentials and non-AI data; seeded installed proof remains (`PrivacyDataService.swift`; `SettingsView.swift`; `.audit/runs/ai-boundary-controls/verifier/REPORT.md`).
- [ ] Disable AI instantly without disabling planning, tracking, coaching rules, or reviews. **Status: Partially implemented.** Selecting Disabled preserves deterministic planning and tracking and now saves through the same conflict-safe version boundary as other Settings fields. Behavior coaching and reviews are not available to preserve, and the complete settings-to-runtime transition was not exercised.
- [ ] Continue using Zoid 666 while offline. **Status: Touches remaining.** Calendar-unavailable planning is now usable end to end in the signed isolated app. Adding the known Reminder immediately replaced the stale helper snapshot with one visible planned block, a 45-minute estimate immediately produced truthful configured-work-window capacity, Close created no receipt, and `USE PLAN LOCALLY` produced the exact local plan with zero Calendar or Reminder commands. App and helper restart restored the exact main task, estimate, local-only receipt, and no approval replay. After isolated Calendar repair, the normal conflict-aware path returned and one confirmation produced one completed four-command operation, one owned Calendar commitment, one Reminder, and one daily-plan row without duplication. This remains Touches because denied notification delivery made the restored receipt incorrectly call the notification failure a Calendar change needing repair even though every Calendar and Reminder mutation succeeded (`TodayPlanPresentation.swift`; `TodayPlanPresentationTests.swift`; `.audit/runs/calendar-zero-block-refresh/verifier/REPORT.md`).
- [ ] See ambiguous activity remain unknown when AI fails. **Status: Touches remaining.** The signed queue kept an insufficient-evidence Preview session visibly Unknown without creating a correction or rule, and that state survived relaunch. A fresh provider-failure transition that creates the Unknown observation remains to connect the existing AI failure seam to this installed review surface (`UnknownSessionReviewState`; `.audit/runs/unknown-session-review/verifier/REPORT.md`).
- [ ] Receive a deterministic factual review when AI is unavailable. **Status: Not implemented.** No daily review generator exists.
- [ ] Avoid having AI directly complete, delete, reschedule, block, or override a corrected task. **Status: Partially implemented.** Structured generation is separated from explicit mutation/outbox commands and trust gates, but corrected-task locks and a complete adversarial E2E test are absent.

## 47. Privacy and data controls

- [ ] Understand which data stays local. **Status: Touches remaining.** Settings now shows a content-safe inventory covering plans, behavior evidence, prompts, meetings, learning, voice, AI request metadata, settings, database path, size, and schema version; the packaged Settings surface still needs a visible installed-app pass.
- [ ] Understand when a remote AI request could leave the Mac. **Status: Touches remaining.** Remote Evidence help text distinguishes local-only, redacted metadata, and explicitly private content; it still lacks a representative payload preview.
- [ ] See what data an export will contain before creating it. **Status: Touches remaining.** Settings shows the exact counts-only redacted manifest and exclusions before export, backed by payload tests; the preview still needs installed-app visual verification.
- [ ] Choose an explicit export destination. **Status: Touches remaining.** Settings uses a native JSON save panel and the service writes atomically only to the chosen JSON destination, with focused tests; the save-panel journey still needs installed-app verification.
- [ ] Open the local data folder. **Status: Touches remaining.** The button calls `NSWorkspace.open` on the canonical storage directory, but the audit did not click it in the installed app or verify the resulting Finder selection.
- [ ] Configure retention separately for raw records, sessions, prompts, reviews, and diagnostics. **Status: Touches remaining.** The signed installed Settings app now renders separate controls for screenshots, extracted text, diagnostics, behavior records, task sessions, prompts, and reviews plus learning in a responsive three-column default grid and two-column narrow grid, with readable single-line values, 44-point actions, and stable category-specific accessibility identifiers. The independent journey discarded a Behavior records change, saved Task sessions from 365 to 366 days through the running helper, verified the exact policy row, and restored both values after app and helper relaunch. Distinct saved-and-reloaded journeys for every remaining retention category still need end-to-end proof before this broader scenario is complete (`SettingsView.swift`; `SettingsRetentionControlsTests.swift`; `.audit/runs/retention-stepper-polish/verifier/REPORT.md`; `.audit/runs/retention-stepper-polish/integration/REPORT.md`).
- [ ] Delete one behavior session. **Status: Touches remaining.** Settings derives bounded application sessions, confirms the exact application and inclusive time range, and routes exact deletion through the agent with focused tests; the destructive installed-app journey remains unexercised.
- [ ] Delete one day. **Status: Touches remaining.** Settings provides a dedicated today-only confirmation and converts the local calendar day to a half-open service range that includes the full day across DST; installed-app deletion and refreshed totals remain to be witnessed.
- [ ] Delete a date range. **Status: Touches remaining.** Inclusive local-day selection now deletes behavior evidence plus every canonical and projected day-keyed plan record, preserves adjacent days, never deletes source screenshots, and refreshes Today and inventory; the destructive installed-app journey remains unexercised.
- [ ] Delete all raw behavior metadata. **Status: Touches remaining.** A confirmation-backed agent command deletes only local behavior, analysis, artifact-index, fact, and meeting-evidence rows while leaving source-owned files untouched; installed-app confirmation and result refresh remain to be witnessed.
- [ ] Delete AI request metadata. **Status: Touches remaining.** A confirmation-backed agent command independently deletes model runs, Codex jobs, and transmission receipts without touching Keychain credentials; installed-app confirmation and result refresh remain to be witnessed.
- [x] Delete reviews and learned rules. **Status: Fully implemented.** Signed Settings now names every deleted category and preserved raw fact in the seven-record inventory, confirmation, positive result, and repeat-zero result. Native Cancel preserved all seven targets plus raw behavior and task state; Confirm atomically removed all seven targets while preserving both raw facts; refreshed inventory showed zero; repeat remained idempotent; and signed relaunch retained the exact zero-one-one state. Focused disclosure tests and the full privacy service group pass (`Sources/ZoidCoachCore/ReviewLearningDeletionDisclosure.swift`; `Sources/ZoidCoachInfrastructure/PrivacyDataService.swift`; `Sources/ZoidCoachInfrastructure/AgentMutationRouter.swift`; `Sources/ZoidCoachApp/Views/SettingsView.swift`; `.audit/runs/delete-reviews-learned-rules/73d56ca7199fd873bfb7501595ad33080ddc7b81/evidence.json`).
- [ ] Delete all Zoid 666 data. **Status: Touches remaining.** A confirmation-backed agent command deletes all user tables while preserving schema migrations, source-owned files, and Keychain credentials, and an empty-store restart test passes; the destructive installed-app journey remains unexercised.
- [ ] See related totals and conclusions disappear when their evidence is deleted. **Status: Touches remaining.** Successful data mutations now refresh the Today snapshot and stored-data inventory, and range deletion removes both canonical and projected plan conclusions; visible installed-app before-and-after proof is still required.
- [ ] Export redacted diagnostics without exposing raw titles, URLs, notes, screenshots, prompts, or credentials. **Status: Touches remaining.** Export is counts-only, excludes content and paths, rejects non-JSON and symbolic-link targets, and has payload inspection tests plus a pre-export manifest; a live saved-file inspection from the packaged app remains.

## 48. Source health and diagnostics

- [x] See Reminders permission and last successful sync. **Status: Fully implemented.** Settings visibly shows Ready to Connect, Connected, Access Needed, and Refresh Failed beside the last confirmed sync timestamp. The installed signed-QA journey proved that a successful refresh persisted the timestamp across restart, a failed refresh retained it, and a recovered retry advanced it (`RemindersConnectionController.swift`; `RemindersConnectionView.swift`; `.audit/runs/reminders-recovery/verifier/REPORT.md`).
- [x] See Screenwatch path and time of the last valid record. **Status: Fully implemented.** Signed Settings visibly and accessibly showed the canonical selectable Screenwatch days-folder path and a locale-aware absolute latest schema-valid record time from a real isolated JSONL source. A later invalid line preserved the earlier valid timestamp under truthful incompatible-format health, relaunch recomputed the same evidence, and removing the log produced a truthful no-valid-record state that persisted across another relaunch (`Sources/ZoidCoachApp/Services/ScreenwatchSetupService.swift`; `Sources/ZoidCoachApp/Views/ScreenwatchConnectionView.swift`; `Sources/ZoidCoachApp/Views/ScreenwatchRecordEvidence.swift`; `.audit/runs/screenwatch-path-last-record/5866ba0627ae99e9307922e58da639dcb314c71c/evidence.json`).
- [ ] See whether Screenwatch is waiting, healthy, stale, missing, denied, incompatible, or failing to parse. **Status: Touches remaining.** The dedicated card now distinguishes all requested states with readable labels and focused tests cover each mapping. The signed installed run visibly proved Waiting, Stale, and Healthy, while denied, incompatible, and parse-failure states remain to be walked (`ScreenwatchConnectionView.swift`; `ScreenwatchConnectionControllerTests.swift`; `.audit/runs/screenwatch-recovery-ux/verifier/REPORT.md`).
- [ ] See notification authorization and the last delivery result. **Status: Touches remaining.** The local delivery ledger and Settings Signals card now present the newest truthful result as Today fallback, accepted by macOS, delivered in QA, or delivery failed, without copying notification content. Focused restart, retention, redaction, and ordering tests pass, and the signed card visibly rendered its empty state; a populated signed restart remained blocked before the delivery-test step (`NotificationDeliveryLedger.swift`; `NotificationDeliveryHealthView.swift`; `.audit/runs/notification-delivery-health/verifier/REPORT.md`).
- [x] See local database health, size, and last migration. **Status: Fully implemented.** Source Health performs a read-only integrity check and displays the canonical database filename, aggregate database/WAL/SHM size, exact current and expected schema versions, latest migration time, and a clear healthy, attention, or not-ready state. The signed-QA app visibly changed from Not Ready to Healthy after the runtime created storage, Refresh exposed a 660 KB schema 29-of-29 store, and restart preserved the healthy state with the same migration time (`.audit/runs/local-system-diagnostics/candidate/REPORT.md`).
- [x] See the current AI mode and recent provider failures. **Status: Fully implemented.** Source Health displays the effective rules-only, local Ollama, Codex CLI, Apple on-device, or remote OpenAI mode, explains the processing boundary, and lists only the five newest provider/state/time/redacted-diagnostic failures. The signed-QA app visibly reported Local Ollama with local processing, Refresh succeeded, and restart preserved the same truthful mode (`.audit/runs/local-system-diagnostics/candidate/REPORT.md`).
- [x] See whether the background helper is running. **Status: Fully implemented.** The installed signed Background Agent window distinguished current, missing, and five-minute-stale runtime heartbeats instead of trusting registration alone. Repair, disable, re-enable, and launchd restart recovery all returned truthful visible states (`AgentLaunchService.swift`; `AgentLifecycleView.swift`; `.audit/runs/agent-lifecycle-recovery/verifier/REPORT.md`).
- [ ] Understand the impact of each unhealthy source. **Status: Touches remaining.** Every unhealthy Today source row now names the affected capability and safe fallback for Reminders, notifications, agent, Calendar, and Screenwatch, with focused all-state proof. Signed representative-state inspection remained blocked by the capped computer-use timeout (`SourceRepairGuidance.swift`; `.audit/runs/source-repair-guidance/verifier/REPORT.md`).
- [ ] Open a direct repair action when one is available. **Status: Touches remaining.** Every Today source row now exposes a stable source-specific control and truthful accessibility hint, publishes Checking synchronously, and deduplicates rapid activation until the adapter finishes. Notification and Screenwatch rechecks still correctly direct the user to Settings for their actual repair paths, and signed click-through remains (`AppModel.swift`; `SourceRepairGuidance.swift`; `.audit/runs/source-repair-guidance/verifier/REPORT.md`).
- [ ] Export a safe diagnostic package after reviewing its contents. **Status: Partially implemented.** Settings now previews the exact redacted JSON manifest and exclusions before a user chooses its destination, but it remains a single JSON artifact rather than a multi-file diagnostic package and lacks installed-app proof.

## 49. Screenwatch outage and recovery

- [ ] See a warning when Screenwatch stops reporting during active use. **Status: Partially implemented.** Today Source Freshness and coverage text show `limited`/stale state, but there is no prominent active-use warning or proven automatic foreground refresh.
- [ ] Understand that behavior totals and drift detection are temporarily unreliable. **Status: Partially implemented.** The dashboard states that Screenwatch coverage is limited/stale, but does not explicitly mention drift-detection suspension.
- [ ] Avoid receiving behavior prompts while Screenwatch is stale. **Status: Touches remaining.** Gaming-drift production rejects missing, future-dated, and older-than-180-second evidence, withdraws unresolved stale decisions, and reconciles inbox truth so their pending or delivered notifications are removed while unrelated notifications remain. Resolution origin and reason are now durable: only the exact `system` plus `screenwatch_evidence_invalid` pair can bypass same-session deduplication, cooldown, and daily-cap gates after fresh evidence returns, while explicit user dismissal and conservative legacy null metadata continue to enforce every normal limit across restart. The focused recovery contract passed 5 of 5 tests, the relevant migration, inbox, gaming-drift, and adjacent prompt suite passed 79 of 79 tests, and the release build passed. An exact clean signed package at `d1bff474f7fe3451ee274b958bc734950ceab1a0` passed identity, signing, XPC, migration-45, and database checks; its fresh isolated rerun ingested 70 current gaming observations and showed a current signed Today snapshot with seven complete baseline days, one incomplete main objective, a 60-minute budget, 70 used minutes, and zero unlocked minutes, but no initial prompt appeared inside the bounded acceptance window. This stays at Touches because that inconclusive signed run could not prove the visible prompt, stale withdrawal, same-session fresh recovery, explicit user dismissal, cooldown and daily-cap preservation, or app and helper restart sequence end to end (`PromptInbox.swift`; `PromptInboxStore.swift`; `GamingDriftPromptService.swift`; `AutonomousDatabaseMigrator.swift`; `.audit/runs/screenwatch-resolution-origin/candidate/REPORT.md`; `.audit/runs/screenwatch-resolution-origin/verifier/REPORT.md`; `.audit/runs/stale-screenwatch-suppression/verifier/REPORT.md`).
- [ ] Continue planning and manually tracking tasks during the outage. **Status: Touches remaining.** Planning and task execution are independent of Screenwatch and tests pass with limited coverage, but a live controlled Screenwatch outage was not induced after runtime recovery.
- [ ] See missing time represented as missing rather than productive or distracting. **Status: Touches remaining.** The signed review displayed a dedicated Missing metric and stated that uncovered active-task time is not counted as work, gaming, or distraction, while focused fixtures prove a 40-minute gap remains separate from every category (`DailySourceCoverage.swift`; `DailySourceCoverageTests.swift`; `.audit/runs/daily-source-coverage/verifier/REPORT.md`).
- [ ] See Screenwatch return to healthy automatically when valid activity resumes. **Status: Touches remaining.** The signed Settings card visibly returned from Waiting and Stale to Healthy when valid current activity appeared and Recheck ran. Foreground activation also rechecks without reselection in passing controller tests, but the capped run did not visibly leave and return to the app for automatic foreground recovery (`ScreenwatchConnectionView.swift`; `.audit/runs/screenwatch-recovery-ux/verifier/REPORT.md`).
- [ ] Continue totals from the correct point without obvious duplicates after recovery. **Status: Touches remaining.** Archive/checkpoint tests cover incremental reads, partial lines, and deterministic replay without duplicate ingestion; live outage/recovery totals were not verified.
- [ ] See a clear schema-mismatch message when the source format changes. **Status: Touches remaining.** Complete JSON records that no longer match the expected Screenwatch schema now produce a privacy-safe Attention state, explicit unsupported-format guidance, aggregate parsed and mismatch counts, and a direct Repair action, while malformed or truncated JSON is not mislabeled and captured app, title, and URL content is never displayed. Four focused reader tests and one signed package plus exact-helper gate passed; the installed Source Health surface and Screenwatch repair control were visible, but repeated ScreenCaptureKit `-3811` failures stopped the healthy-to-changed-to-mixed-to-repaired visual sequence before promotion to Fully implemented (`ScreenwatchReader.swift`; `ScreenwatchReaderTests.swift`; `.audit/runs/screenwatch-schema-mismatch/verifier/REPORT.md`).
- [ ] Preserve prior activity and task history during the outage. **Status: Touches remaining.** Existing history is not deleted by missing or stale reads, the canonical database is healthy, and stores are independently tested, but a live outage and recovery comparison was not performed.

## 50. Notification failure

- [x] Continue using Zoid 666 when notification permission is unavailable. **Status: Fully implemented.** The fresh installed signed-QA denial journey continued through onboarding, opened the complete Today dashboard, exposed the paused-setup recovery strip, and resolved the canonical choice without touching the production notification center (`OnboardingRootView.swift`; `DashboardView.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`).
- [ ] See a permission repair path when notification access is revoked. **Status: Touches remaining.** Production Settings opens the notification pane, preserves the exact `System Settings > Notifications > Zoid 666 > Allow notifications` fallback, and rechecks authorization only after a real repair attempt. Signed QA visibly showed revoked access and a fixture-only Apply QA Repair action without opening production System Settings, while seven focused return tests passed. The actual signed foreground return then cleared the pending repair message instead of showing the still-denied breadcrumb and Today fallback, so denied, granted, unrelated-foreground, and relaunch acceptance remains incomplete (`NotificationDeliveryHealthView.swift`; `NotificationDeliveryHealthTests.swift`; `.audit/runs/notification-permission-return-ux/verifier/REPORT.md`).
- [x] Receive prompts through the Today dashboard when notification delivery fails. **Status: Fully implemented.** A fresh signed-QA run with notification permission denied created the canonical prompt, visibly reported Today Fallback, and displayed the same title, summary, and harmless actions in Today Decisions (`OnboardingTestPromptService.swift`; `DashboardView.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`).
- [ ] Continue seeing active-task state in the dashboard and menu bar. **Status: Partially implemented.** The dashboard state is independent of notifications, but the menu bar has only Zoid Voice and no active-task state.
- [ ] Avoid losing a prompt response when notification handling is interrupted. **Status: Touches remaining.** Prompt response plus pending effect is transactional, token-bound, idempotent, and concurrency tested. The denied-to-repaired deterministic journey now replays response processing after interruption and applies the response exactly once across the durable store, while a killed signed notification process remains unproven (`NotificationPermissionRecoveryTests.swift`; `.audit/runs/notification-permission-recovery/0d6ec2d/REPORT.md`).
- [x] Continue using in-app prompts when notification permission is denied. **Status: Fully implemented.** The installed denial journey resolved Continue Setup directly from Today, removed the decision from the dashboard, persisted response surface `dashboard`, and returned to onboarding with the same resolved state and enabled continuation (`PromptInboxStore.swift`; `TodayDashboardXPC.swift`; `.audit/runs/onboarding-test-prompt/signed-acceptance/REPORT.md`).
- [ ] Avoid receiving stacked duplicate notifications for one coaching decision. **Status: Touches remaining.** Stable request identifiers replace the existing request, the ledger explicitly marks the later attempt as a replacement, and the denied-to-repaired persistence journey proves repeated scheduling leaves one latest fixture notification before an exactly-once response. Behavior-coaching episodes and a real Notification Center stacking inspection remain unavailable (`PromptNotificationCoordinator.swift`; `NotificationPermissionRecoveryTests.swift`; `.audit/runs/notification-delivery-health/acceptance-mapping.json`; `.audit/runs/notification-permission-recovery/0d6ec2d/REPORT.md`).

## 51. Reminders failure and recovery

- [ ] Continue manual planning after Reminders access is denied or revoked. **Status: Touches remaining.** Today now adds a dedicated outage banner with exact retained plan facts plus direct New Local Task and Open Source Health actions, while existing signed proof covers local-only task creation and restart-safe planning without touching Apple Reminders. A strict installed revoke-to-restore journey remains (`RemindersContinuityState.swift`; `DashboardView.swift`; `.audit/runs/reminders-outage-continuity/verifier/REPORT.md`).
- [x] See when task data may be out of date. **Status: Fully implemented.** The installed signed-QA card visibly reported Refresh Failed, explained that task data was not returned, retained the prior successful-sync timestamp, and offered an explicit retry without discarding prior success (`.audit/runs/reminders-recovery/verifier/REPORT.md`).
- [x] Retry Reminders synchronization. **Status: Fully implemented.** The installed signed-QA card visibly recovered from an injected task-fetch failure through one Refresh Reminders action, changed back to Connected, and advanced the last-success timestamp only after tasks were returned (`RemindersConnectionController.swift`; `.audit/runs/reminders-recovery/verifier/REPORT.md`).
- [ ] Keep local estimates, active sessions, and plan state while sync is unavailable. **Status: Partially implemented.** The outage banner now reports canonical retained task count and estimated minutes and explicitly confirms that an active session continues tracking locally. Focused canonical snapshot and restart persistence tests pass, while the full installed permission-loss, continued-work, helper restart, and recovery journey remains (`RemindersContinuityState.swift`; `TodayDashboardAgentTests.swift`; `.audit/runs/reminders-outage-continuity/verifier/REPORT.md`).
- [x] See completion remain pending when Apple Reminders rejects it. **Status: Fully implemented.** The installed signed-QA journey revoked deterministic Reminders access before completion and proved that Today removed the task from active work without claiming source success, then exposed the terminal source-write failure and its single retry (`DashboardView.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).
- [x] Retry the pending completion after access returns. **Status: Fully implemented.** Restoring Reminders access and choosing the task-specific retry requeued the same durable command, preserved attempt-one evidence, confirmed the source task on attempt two, and survived app and agent restart (`ActionOutboxStore.swift`; `TodayDashboardAgent.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).
- [x] Avoid losing the local task when a Reminder write fails. **Status: Fully implemented.** The signed failure-and-recovery proof retained the local completion record, readable title, and exactly one history entry through failure, retry, confirmed source completion, and restart (`TaskHistoryStore.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).
- [x] Avoid seeing a task reported as synchronized before confirmation. **Status: Fully implemented.** The installed ledger reports pending, retrying, failed, unavailable, or unknown until the exact durable command succeeds, and the signed journey proved the confirmed label appeared only after the source task was completed (`ReminderCompletionSyncState.swift`; `.audit/runs/reminder-completion-sync/84da415/REPORT.md`).

## 52. Database or local-state failure

- [ ] See a read-only state when local changes cannot be saved safely. **Status: Touches remaining.** A prominent `READ-ONLY SAFETY MODE` banner exists with a reason and recheck action, but a current write failure was not safely induced to prove the complete transition and recovery.
- [ ] Understand which actions are temporarily unavailable. **Status: Partially implemented.** The read-only banner says external actions are blocked, but it does not enumerate affected controls or disable every unavailable action with a local explanation.
- [ ] Continue viewing already saved information when safe. **Status: Partially implemented.** The UI is designed to remain visible in safety mode and the canonical database is healthy now, but an actual read-only transition was not exercised.
- [ ] Avoid receiving coaching actions that cannot be recorded reliably. **Status: Partially implemented.** A database write circuit breaker and read-only agent mode exist and are tested, but a live write failure and prompt-suppression sequence was not verified.
- [ ] Retry after a temporary local database lock. **Status: Partially implemented.** Migration 44 adds a durable Calendar plan-operation ledger alongside the existing task-mutation ledger, freezes the complete command set before enqueue, and replays only an exact authoritative receipt. In signed QA, a real temporary exclusive lock still produced exactly one external Reminder completion operation, outbox command, and completed history row; a lost task reply reused the same client operation across helper relaunch without duplicate effects; and a lost Calendar reply restored the same complete committed command set after helper relaunch. Focused tests additionally reject incomplete and same-sized wrong command sets and return an authoritative refusal to review, while the preserved canonical prompt-block error still survives inbox refresh. The scenario remains partial because a persistent lock beyond the retry bound has not visibly proved zero partial effects, and native signed pixels and Accessibility have not proved the incomplete-set refusal, reconciling panel, or authoritative-refusal recovery UI (`CalendarPlanOperationStore.swift`; `AgentMutationRouter.swift`; `AgentPlanScheduler.swift`; `TodayDashboardXPC.swift`; `AppModel.swift`; `.audit/runs/full-mutation-lock-retry/operation-key/verifier/REPORT.md`; `.audit/runs/full-mutation-lock-retry/remaining/verifier/REPORT.md`).
- [ ] Restart after an unsuccessful upgrade and retain readable previous data. **Status: Blocked from verification.** Every pending upgrade to an existing database now starts from a consistent SQLite snapshot, including committed WAL rows. A real SQLite integration test applied one migration, failed before the next, restored the exact prior schema and readable source-health row with integrity `ok`, retained the recovery snapshot, and then completed a healthy restart without losing the row. A separate destructive test removed the snapshot before restore and proved the error fails closed without falsely claiming that prior data was recovered. The verifier also corrected the pre-existing schema-42 versus expected-schema-41 drift, passed all 24 migrator tests, and completed a release build. Full qualification remains blocked because the unsigned release agent correctly refused isolated fixture access before migration; a deliberately failing clean signed-QA package must still visibly show recovery guidance, preserve representative estimates, plans, prompts, and task sessions, then survive corrected-package app and helper relaunch (`AutonomousDatabaseMigrator.swift`; `AutonomousDatabaseMigratorTests.swift`; `.audit/runs/failed-upgrade-recovery/verifier/REPORT.md`).
- [ ] Avoid losing estimates, plans, prompt responses, or task sessions after a crash. **Status: Blocked from verification.** Durable stores and a healthy canonical database exist, but an intentional process crash and complete recovery check were not performed against the user's live state.

## 53. Sleep, wake, and restart

- [ ] Put the Mac to sleep during an active task. **Status: Blocked from verification.** Sleep recovery policies exist, but a real sleep cycle with a controlled test task was not performed during this audit.
- [ ] Wake after a short lock and see timing follow the configured policy. **Status: Touches remaining.** Real workspace and display sleep notifications now record one absence boundary, and a wake below five minutes keeps the active task running with neutral reconciliation copy naming the task, observed absence, and aligned-work evidence boundary. Six focused tests, release, and signed packaging pass, while physical sleep and wake required action-time confirmation that was not present (`WakeTaskReconfirmationController.swift`; `ZoidCoachApp.swift`; `.audit/runs/wake-task-reconfirmation/verifier/REPORT.md`).
- [ ] Wake after a long sleep and be asked whether the task is still active. **Status: Touches remaining.** A wake at or above five minutes now opens a non-dismissible, keyboard-accessible task check-in with the active task title, observed absence duration, Continue and Pause choices, stable identifiers, and no scene-focus trigger. Physical installed sleep and wake plus both persisted choices remain unverified because action-time confirmation was absent (`WakeTaskReconfirmationView.swift`; `ZoidCoachApp.swift`; `.audit/runs/wake-task-reconfirmation/verifier/REPORT.md`).
- [ ] Avoid accumulating aligned time while no telemetry exists. **Status: Touches remaining.** The wake notice and decision explicitly separate the task clock from observed activity and state that time without telemetry is never aligned work, while existing coverage logic suppresses unsafe conclusions. A physical sleep-to-wake comparison of visible task time against aligned totals remains (`WakeTaskReconfirmationView.swift`; `DailySourceCoverage.swift`; `.audit/runs/wake-task-reconfirmation/verifier/REPORT.md`).
- [ ] Sleep during a sprint and see an understandable reconciled result on wake. **Status: Touches remaining.** Wall-clock reconciliation computes expiry after a long absence without completing the task and presents the expired explanation on refresh; a physical installed sleep and wake cycle remains (`TaskExecutionStoreTests`; `TodayDashboardAgentTests`).
- [ ] Restart Zoid 666 with an active task and recover it without duplicated time. **Status: Blocked from verification.** Task sessions are durable and the canonical database recovered, but no controlled test task was started, restarted, timed, and reconciled end to end.
- [ ] Restart with an unresolved prompt and see its current valid state. **Status: Partially implemented.** Focused timeline tests reopen SQLite and recover the unresolved prompt's current state while expiry is recalculated on fetch. The signed installed helper could not load seeded prompts, so app-and-agent restart acceptance remains (`PromptInboxTimelineTests.swift`; `.audit/runs/today-prompt-inbox/verifier/REPORT.md`).
- [x] Restart with an unfinished review and resume it. **Status: Fully implemented.** The installed signed Daily Review reopened the most recently edited unconfirmed review after restart, preserved its corrected session, resumed it from another selected day, and retained the confirmed result without an unfinished banner after the final relaunch (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/unfinished-review-resume/verifier/REPORT.md`).
- [x] Change time zones and retain accurate historical event times. **Status: Fully implemented.** In the installed signed-QA app, a local task was created, started, completed, and retained visibly in Daily Review while the policy moved through Los Angeles, London, Tokyo, and back to the Mac's Cairo zone; the durable completion instant remained byte-identical at `2026-07-13T11:21:36Z` after the final app and helper restart (`SettingsPolicyDraftTests.swift`; `.audit/runs/settings-time-zone-policy/aedc669a5cc5da5457684a670b690f8bcb060de4/evidence.json`).
- [x] Confirm before a time-zone change moves a plan to another local day. **Status: Fully implemented.** The installed signed-QA Settings flow selected a cross-day zone through stable native accessibility controls, displayed a privacy-safe warning with the exact source and destination local days, preserved the unsaved draft and active policy after Cancel, repeated the same truthful warning, and saved only after explicit Confirm. SQLite proof showed policy versions 1 through 3 while the exact historical plan row remained unchanged, app and helper relaunch restored the confirmed zone, and a same-day change saved directly without an unnecessary warning. Focused signed-package gating, controller, real SQLite, DST-boundary, inspection-failure, and relaunch tests plus a release build passed (`Sources/ZoidCoachApp/Views/SignedQATimeZonePickerControl.swift`; `Sources/ZoidCoachInfrastructure/TimeZonePlanMoveInspector.swift`; `Sources/ZoidCoachApp/Views/SettingsPolicyController.swift`; `Tests/ZoidCoachAppTests/SignedQATimeZonePickerControlTests.swift`; `.audit/runs/time-zone-plan-move-confirmation/picker-finish-candidate/REPORT.md`; `.audit/runs/time-zone-plan-move-confirmation/independent-verifier/REPORT.md`).
- [ ] Avoid negative or impossible durations when the system clock changes. **Status: Touches remaining.** Task timing now clamps backward discontinuities to zero, caps one continuous forward discontinuity at 1,440 minutes, preserves later legitimate intervals additively, and survives reopen. Four focused tests pass, while a signed isolated runtime visibly reported 0 minutes for a future-started active interval, 1,440 minutes for a seven-day discontinuity, and 1,445 minutes after a separate five-minute interval and relaunch. The native Pause selection did not reach persisted state in that fixture, so a signed backward-close, pause, resume, and task-switch journey remains (`Sources/ZoidCoachInfrastructure/TaskExecutionStore.swift`; `Tests/ZoidCoachAppTests/TaskExecutionStoreTests.swift`; `.audit/runs/clock-safe-task-durations/verifier/REPORT.md`).

## 54. Quiet hours and scheduling

- [ ] Receive morning planning at the configured time. **Status: Partially implemented.** The agent runs scheduled and missed-run planning, but current settings center on nightly planning and no complete morning invitation flow was proven.
- [x] Receive planned task-start reminders. **Status: Fully implemented.** The signed app accepted a two-block plan and scheduled exactly one `TASK_START` request at each block start, while the optional task produced none. Replanning retained the stable day-and-task identifier, replaced the second request's 09:40 time with 10:10 and revised its task copy, and app/helper restart preserved exactly two requests (`AgentPlanScheduler.swift`; `ActionCommandExecutor.swift`; `.audit/runs/planned-task-start-reminders/0b857b9fd456b76ae69a2bc951d1fdac4f34d35d/REPORT.md`).
- [ ] Receive break-end reminders. **Status: Partially implemented.** The agent now schedules one durable, replacement-safe reminder for each accepted break, preserves stable identity across helper replay, delivers immediately after a missed boundary, cancels on early resume, and prevents a cleared delivery from being recreated through the persistent delivery ledger. Focused tests, release build, and signed packaging pass, but installed schedule, restart, cancellation, missed-boundary, and replay states remain unproven because the signed QA control seed was not consumed within the capped run (`AcceptedBreakReminderService.swift`; `PromptNotificationCoordinator.swift`; `.audit/runs/agent-break-end-reminders/verifier/REPORT.md`).
- [x] Receive the end-of-day review reminder. **Status: Fully implemented.** The signed observation-mode helper scheduled `daily-review:2026-07-13` for the configured 14:06 work end, deferred it through quiet hours to exactly 14:07 Cairo, retained one stable current occurrence across helper restart, and delivered it at the boundary. The reminder copy directs the user to Reviews, where the signed app exposed the complete Daily Review correction and confirmation workflow (`ReviewReminderService.swift`; `AgentMain.swift`; `.audit/runs/scheduled-review-reminders/680c8c8dc0eed1e45614188fa10b19f3a6f816ed/evidence.json`).
- [x] Receive the weekly review reminder. **Status: Fully implemented.** The signed Monday-only schedule treated 14:06 as the final configured workday boundary, deferred `weekly-review:2026-W29` to exactly 14:07 Cairo, retained one stable current occurrence across helper restart, and delivered it at the boundary. The next ISO-week identifier was scheduled separately, and the signed Reviews surface exposed the weekly evidence summary without inventing conclusions (`ReviewReminderService.swift`; `AgentMain.swift`; `.audit/runs/scheduled-review-reminders/680c8c8dc0eed1e45614188fa10b19f3a6f816ed/evidence.json`).
- [ ] Avoid behavioral prompts outside configured work hours. **Status: Partially implemented.** Quiet windows and wake eligibility are policy-controlled, but the required gaming-drift intervention path is not complete end to end.
- [ ] Keep source-health problems visible in the dashboard without waking the user. **Status: Touches remaining.** Source freshness is visible and health notifications are bounded, but a live quiet-hours source failure was not exercised.
- [x] See only one unresolved notification for the same prompt. **Status: Fully implemented.** Deterministic replacement integration retains one fixture notification when the same prompt is scheduled repeatedly, and the signed quiet-hours journey preserved exactly one queued prompt and one ledger delivery across app and helper restart. The stable request identifier prevents duplicate unresolved notifications while retaining updated delivery evidence (`PromptNotificationCoordinator.swift`; `NotificationDeliveryLedgerTests.swift`; `.audit/runs/quiet-hours-delivery/7a216a88032234e5d7a442e46a8052a16b283b92/REPORT.md`).
- [ ] See updated notification content replace obsolete content. **Status: Touches remaining.** A signed-QA-only Settings control now drives the real `PromptNotificationCoordinator` and `UNUserNotificationCenter` path while production and unpackaged QA fail closed. Deterministic proof replaces `PLAN_READY` with changed `PLAN_CHANGED` content and actions under one privacy-safe stable request identity, routes only the newest Undo action, persists that response, and prevents obsolete resurrection after reconstructed-store relaunch; focused tests and the release build pass. The signed app rendered the control and four distinct native accessibility identifiers, but the existing QA bundle notification permission was unavailable, so the safe original attempt reported that macOS did not accept it and the run stopped without requesting permission or changing System Settings. Actual Notification Center one-card replacement, newest-action routing, and relaunch non-resurrection therefore remain the final gate (`PromptNotificationCoordinator.swift`; `QANotificationReplacementProbe.swift`; `QANotificationReplacementProbeView.swift`; `QANotificationReplacementProbeTests.swift`; `.audit/runs/stable-notification-content-replacement/verifier/REPORT.md`; `.audit/runs/qa-notification-replacement-seam/verifier/REPORT.md`).

## 55. Accessibility

- [ ] Complete onboarding using only the keyboard. **Status: Touches remaining.** The complete onboarding flow now has explicit accessibility identifiers, native controls, focusable choices, and keyboard shortcuts for continuation and retries, but a full keyboard-only signed-QA pass has not yet been completed (`OnboardingRootView.swift`).
- [ ] Plan the day using only the keyboard. **Status: Partially implemented.** SwiftUI buttons are keyboard-addressable and expose labels, but full focus traversal and plan completion were not proven.
- [ ] Start, pause, switch, and complete tasks using only the keyboard. **Status: Partially implemented.** Start and complete buttons are accessible, but pause, explicit switch, and full lifecycle controls are incomplete.
- [ ] Respond to coaching using only the keyboard. **Status: Partially implemented.** Notification actions and dashboard prompt buttons exist, but the full gaming coaching flow is not complete.
- [ ] Correct and confirm reviews using only the keyboard. **Status: Touches remaining.** Reviews now exposes a visible `⌥⌘R` route, native `Navigate > Open Reviews` command, chronological previous/next session shortcuts, five classification shortcuts, correction-before-confirm gating, Apply, and Confirm, and all six focused routing and state tests pass. The exact signed canonical package exposed the shortcut hint and enabled native command with valid signing, the exact helper, and writable XPC, but the automation host could not deliver the navigation keystroke through three trusted injection paths, so the downstream correction, database truth, confirmation, and relaunch journey remain unclaimed (`DailyReviewKeyboardFlow.swift`; `DailyReviewView.swift`; `DashboardView.swift`; `ZoidCoachApp.swift`; `DailyReviewKeyboardFlowTests.swift`; `.audit/runs/keyboard-only-daily-review/verifier/REPORT.md`).
- [ ] Configure settings using only the keyboard. **Status: Partially implemented.** Settings controls use native SwiftUI semantics, but a complete keyboard-only pass has not been validated.
- [ ] Hear useful VoiceOver labels for every control and state. **Status: Partially implemented.** Many critical buttons have explicit accessibility labels and custom controls expose values, but no complete VoiceOver audit covers all 497 Today elements and settings states.
- [ ] Understand every status without relying on color alone. **Status: Touches remaining.** Major states use text labels such as `ACTIVE`, `READY`, and `READ-ONLY`, but no full state inventory and contrast-independent verification exists.
- [ ] Use larger text without clipped task titles or hidden actions. **Status: Partially implemented.** Flexible text appears throughout, but the installed UI contains very long Reminder titles and no large-text visual proof was completed.
- [ ] Follow a clear and logical focus order. **Status: Partially implemented.** Accessibility order broadly follows visual order, but the dense Today page has hundreds of elements and was not end-to-end keyboard audited.
- [x] Use reduced motion without losing state-change feedback. **Status: Fully implemented.** All 29 owned Today and shared-control motion sites now consume one environment-backed policy that removes animation, pressed scaling, offset, scale, asymmetric, and reorder motion when Reduce Motion is enabled while preserving immediate labels, selection, counts, ordering, focus, and actions. A deterministic `NSHostingView` fixture exercised pressed feedback, plan insertion, usage switching, estimate confirmation, and reminder reordering with reduced motion, a standard-mode control retained restrained motion, six focused tests passed, and the exact signed release QA package launched with a complete accessibility tree without changing the user's macOS setting (`SumiTheme.swift`; `DashboardView.swift`; `TodayDashboardCommandOverview.swift`; `SumiThemeTests.swift`; `.audit/runs/reduced-motion-feedback/verifier/REPORT.md`).
- [ ] Use an accessible dashboard alternative to every compact notification action. **Status: Partially implemented.** Prompts are stored in a dashboard inbox, but the full set of notification actions and resulting states is not proven equivalent.

## 56. Localization and visual clarity

- [ ] See dates, durations, numbers, calendars, and week starts formatted for the current locale. **Status: Partially implemented.** Native formatters are used in many surfaces, but user-facing strings are hard-coded in English and no locale matrix was tested.
- [ ] Change locale without breaking saved task and review meaning. **Status: Partially implemented.** Stored times retain time-zone context, but reviews are absent and a real locale change was not verified.
- [ ] Read localized text without clipped controls. **Status: Touches remaining.** The signed Arabic first-launch Welcome screen now renders its full card, progress rail, header, status, Continue action, Exit help, and accessibility labels in logical right-to-left layout, advertises both English and Arabic in the signed bundle, returns to the exact English left-to-right shell on step 2, and preserves that boundary across app and helper restart. The installed 519-point layout, keyboard Return, generic Arabic error, unsupported-locale fallback, and localization of the remaining product surfaces still require acceptance (`OnboardingWelcomeCopy.swift`; `OnboardingRootView.swift`; `App/Info.plist`; `.audit/runs/arabic-welcome/verifier/signed-runtime.md`).
- [ ] Use light appearance with sufficient contrast. **Status: Touches remaining.** The installed Sumi-Ink light interface is coherent and readable, but a formal contrast audit has not been completed.
- [ ] Use dark appearance with sufficient contrast. **Status: Touches remaining.** The forced Light override is removed, both semantic Sumi-Ink palettes pass focused text and control-boundary contrast requirements, and the installed signed Light appearance remained coherent across onboarding, Today, Source Health, Settings, Reviews, and Background Agent. A live macOS Dark-to-Light traversal remains because the required action-time confirmation to change the system appearance was not available (`SumiTheme.swift`; `SumiThemeTests.swift`; `.audit/runs/dark-appearance/verifier/REPORT.md`).
- [ ] Recognize active, paused, warning, missing, and completed states without ambiguity. **Status: Partially implemented.** Active, ready, warning, and completed labels are present, but paused workday and review states are incomplete.
- [ ] See the Sumi-Ink visual system consistently across dashboard, planning, review, settings, menu bar, and prompts. **Status: Partially implemented.** Today, source health, and settings use Sumi-Ink; functional review and general task menu-bar surfaces do not exist.
- [ ] See restrained emphasis for risk and drift rather than alarming visual treatment. **Status: Touches remaining.** Existing warnings use restrained red seal treatment, but gaming-drift escalation is not complete enough for full validation.
- [ ] Keep gaming information visible without letting it dominate the product. **Status: Touches remaining.** The live Today hero shows a compact 60-minute gaming budget and unlock rule without dominating the page.
- [x] Avoid distracting motion or celebratory feedback around interruptions. **Status: Fully implemented.** Shared actions, dropdowns, overlays, Today planning changes, usage filters, estimate confirmations, and reminder reorders now become immediate and non-spatial under Reduce Motion while retaining useful opacity, border, fill, label, selection, focus, and ordering feedback. The deterministic reduced-motion view host and standard-mode control passed, static inspection found no remaining unconditional spatial-motion pattern in the 29 owned sites, and the coherent signed release QA package launched successfully (`SumiTheme.swift`; `DashboardView.swift`; `TodayDashboardCommandOverview.swift`; `SumiThemeTests.swift`; `.audit/runs/reduced-motion-feedback/verifier/REPORT.md`).

## 57. Responsiveness and perceived reliability

- [ ] Open the Today view without a noticeable cold-launch delay. **Status: Touches remaining.** The installed app becomes usable quickly in live inspection, but no controlled cold-launch timing was captured.
- [ ] Reopen the app quickly after it has already launched once. **Status: Touches remaining.** The running app returns promptly, but no repeatable timing measurement was recorded.
- [ ] See new Screenwatch activity reflected within a believable short delay. **Status: Partially implemented.** The dashboard reported current activity and one post-recovery verifier run confirmed fresh ingestion, but end-to-end UI latency was not measured and the later recheck found the source stale.
- [ ] See local task actions respond immediately. **Status: Touches remaining.** Estimate controls and visible task actions update locally, but destructive actions were not used against the user's real Reminders.
- [ ] See the dashboard and menu bar stay synchronized. **Status: Partially implemented.** The menu bar is a voice host rather than a full task-status surface, so task lifecycle parity is incomplete.
- [ ] Receive a gaming prompt shortly after the configured threshold rather than much later. **Status: Barely started.** Gaming accounting exists, but the specified ten-minute gaming-drift prompt journey is not proven.
- [ ] Generate a rules-only daily review without a noticeable wait. **Status: Not implemented.** No functional daily review generator or review screen is present.
- [ ] Leave the background helper running without obvious battery, CPU, or memory impact. **Status: Partially implemented.** A bounded signed restart sample observed 0.0-2.5% startup CPU, 23,616 KB resident memory after initialization, 19 open handles, and no SQLite file growth. Sustained energy and long-duration handle or memory measurements remain (`.audit/runs/agent-lifecycle-recovery/verifier/REPORT.md`).
- [ ] Use the app for weeks without unexplained database growth or duplicated screenshot storage. **Status: Blocked from verification.** Retention services exist and the canonical database is healthy, but a controlled multi-week growth and screenshot-deduplication observation has not been completed.

## 58. Complete planning-to-completion journey

- [ ] Open Zoid 666 at the start of the day. **Status: Blocked from verification.** Build 8, signing, agent, database, and service checks pass, but the independent `a068d27` verifier did not launch the main app because it would refresh real EventKit and XPC state (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] Review eligible Reminders. **Status: Blocked from verification.** Source and live database evidence show Reminder ingestion, but the independent `a068d27` verifier could not run the current packaged UI against isolated Reminder fixtures (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] Choose a realistic top three and main objective. **Status: Partially implemented.** Users can add, remove, rank, and mark a main objective, but the live plan had five blocks and no strict top-three approval flow.
- [ ] Estimate every priority task. **Status: Touches remaining.** Visible 15, 30, 45, 60, and 90 minute controls work for planned tasks, but custom and unknown estimates are absent.
- [ ] Approve the plan. **Status: Touches remaining.** The signed app visibly exposed the exact review-before-write state and truthful atomic refusal with no queued commands. Durable receipts reconcile only exact command identities, and terminal failure now retries the same eligible outbox rows instead of issuing replacements; successful installed Calendar write, receipt relaunch, and terminal repair remain to be proven (`CalendarPlanApprovalSheet.swift`; `AgentMutationRouter.swift`; `.audit/runs/calendar-approval-receipt/verifier/REPORT.md`).
- [ ] Start the recommended main task. **Status: Partially implemented.** A `DO THIS NEXT` task can be started, but the live recommendation said to continue an already active commitment and main-objective semantics are not consistently enforced.
- [ ] Work in an aligned application without receiving a drift prompt. **Status: Partially implemented.** Screenwatch currentness and active commitment exist, but alignment classification and prompt suppression are not user-visible enough to prove this.
- [ ] Complete the task. **Status: Touches remaining.** The installed dashboard exposes `COMPLETE` actions, but the user's real Reminder was not mutated during audit.
- [ ] See the Apple Reminder completed. **Status: Blocked from verification.** Completing a real user task would be an external state change and was not necessary to identify the larger incomplete flow.
- [ ] See the dashboard recommend the next useful task. **Status: Touches remaining.** The dashboard displays `DO THIS NEXT` with a reason and updates from agent state, but post-completion recomputation was not safely exercised.
- [ ] End the workday and confirm an accurate review. **Status: Not implemented.** No end-workday control or functional daily review confirmation flow exists.

## 59. Complete gaming-recovery journey

- [ ] Approve a plan with at least one incomplete priority task. **Status: Partially implemented.** Planned blocks and incomplete tasks exist, but the exact approved priority-plan state is not exposed.
- [ ] Start gaming during the work window before unlock conditions are satisfied. **Status: Blocked from verification.** This requires a live behavioral session and cannot establish completion while downstream intervention behavior is missing.
- [ ] Receive one accountability prompt after the threshold. **Status: Barely started.** Prompt infrastructure exists, but no verified ten-minute gaming trigger delivered the specified prompt.
- [ ] Understand the observed gaming time and unfinished task. **Status: Touches remaining.** The signed Daily Review visibly decoded one behavior prompt into `12 MIN IN STEAM` and the unfinished proposal with stable accessibility evidence, and relaunch preserved it. A real gaming threshold was not triggered in the installed run, so the complete source-to-review journey remains (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/recovery-followthrough-review/verifier/REPORT.md`).
- [ ] Start a 20-minute sprint from the prompt. **Status: Not implemented.** No 20-minute sprint action appears in the installed prompt or task controls.
- [ ] See gaming stop accumulating when the context changes. **Status: Partially implemented.** Behavior sessions can close on context changes, but the installed UI did not show a full gaming-to-work transition.
- [ ] See the selected task become active everywhere. **Status: Touches remaining.** Today and the menu bar now derive the same named task, open-ended or bounded timing contract, tracked duration, and manual controls from the canonical snapshot, and focused tests prove the exact shared state after agent restart. The real macOS status item remains inaccessible to the verifier and the signed Reminder seed failed closed before the complete cross-surface journey, so installed parity is not yet proven (`ActiveCommitmentPresentation.swift`; `MenuBarCoachState.swift`; `MenuBarCoachView.swift`; `.audit/runs/active-commitment-visibility/verifier/REPORT.md`).
- [ ] Work in an aligned context for at least ten minutes. **Status: Partially implemented.** Aligned work can be inferred in domain logic, but the user cannot verify the recovery window end to end.
- [ ] See the intervention counted as a recovery. **Status: Touches remaining.** The signed review counted one observed follow-through and two recovery starts, required corrected selected-task work within 30 minutes, excluded unrelated work from the task-specific minutes, and truthfully kept an applied response with no later work at `RECOVERY STARTED`. The actual gaming prompt-to-work action was seeded rather than performed live (`DailyReviewStore.swift`; `.audit/runs/recovery-followthrough-review/verifier/REPORT.md`).
- [ ] See the journey described factually in the daily review. **Status: Touches remaining.** The signed review combined observed gaming, unfinished-task context, selected-task start evidence, no-work follow-through, action surface, and unanswered state without inferring success, and reproduced the result after relaunch. A complete naturally triggered gaming journey remains (`DailyReviewView.swift`; `.audit/runs/recovery-followthrough-review/verifier/REPORT.md`).

## 60. Complete intentional-gaming journey

- [ ] Start gaming while priority work remains incomplete. **Status: Blocked from verification.** A real gaming session was not required because the response and review path is incomplete.
- [ ] Receive one appropriate prompt. **Status: Barely started.** General prompt delivery exists, but the gaming-specific end-to-end trigger is not proven.
- [ ] Choose `Continue intentionally`. **Status: Not implemented.** This action is not present in current prompt actions.
- [ ] See the prompt close and the override cooldown begin. **Status: Not implemented.** No intentional-gaming override control or visible cooldown exists.
- [ ] Continue gaming without repeated equivalent prompts during the cooldown. **Status: Not implemented.** The required override state does not exist in the user flow.
- [ ] See gaming budget or debt update according to policy. **Status: Partially implemented.** A durable gaming ledger and live budget exist, but override/debt response integration is not complete.
- [ ] Return to work voluntarily. **Status: Partially implemented.** A user can start a task, but the transition is not connected to an intentional-gaming episode.
- [ ] See the choice recorded without shame in the review. **Status: Touches remaining.** The signed review visibly recorded Continue intentionally as `INTENTIONAL CHOICE RECORDED · NO JUDGMENT`, counted it separately from recovery, and preserved it after relaunch. Choosing the action from a naturally triggered gaming prompt and observing its cooldown remain outstanding (`DailyReviewView.swift`; `.audit/runs/recovery-followthrough-review/verifier/REPORT.md`).

## 61. Complete ambiguous-work journey

- [ ] Start a technical task. **Status: Touches remaining.** Real tasks can be started, but no task-type declaration or dedicated technical-task context is visible.
- [ ] Open a browser or YouTube tutorial related to that task. **Status: Partially implemented.** Screenwatch observes application and context, but this exact relationship was not safely staged.
- [ ] See the session classified as research when evidence is sufficient. **Status: Not implemented.** Current Today behavior exposes app percentages rather than a user-visible research-session classification.
- [x] See the session remain unknown or request confirmation when evidence is insufficient. **Status: Fully implemented.** The signed queue explained that evidence was insufficient, invited confirmation only when recognized, kept Preview Unknown with its classification action disabled until the user made a choice, and restored that pending session unchanged after relaunch (`DailyReviewView.swift`; `.audit/runs/unknown-session-review/verifier/REPORT.md`).
- [ ] Avoid receiving a strong drift prompt without sufficient evidence. **Status: Partially implemented.** Confidence policies exist, but the user-facing drift system is incomplete and cannot be validated end to end.
- [x] Correct the session if Zoid 666 is wrong. **Status: Fully implemented.** In the signed queue, Safari changed from Unknown to Work through Apply Classification, immediately left the pending queue, moved into Classified Activity Sessions, recalculated Work and Unknown totals, and remained Work after relaunch (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/unknown-session-review/verifier/REPORT.md`).
- [x] Save an appropriately scoped rule if the same context will recur. **Status: Fully implemented.** The signed Safari correction accepted the Research task attachment, previewed that only new Safari observations would become Work while history stayed unchanged, saved one active Safari-to-Work rule, and restored the task attachment and rule after relaunch. Idle and Unknown selections visibly disabled future-rule creation, while focused tests also rejected unsafe rules at the durable store boundary (`DailyReviewStore.swift`; `DailyReviewView.swift`; `.audit/runs/unknown-session-review/verifier/REPORT.md`).
- [ ] See future matching activity handled according to the correction. **Status: Touches remaining.** The scoped Safari-to-Work future rule was saved, visibly active, and persisted after relaunch, with focused store coverage for rule persistence and safety. A fresh later Safari observation was not ingested through the running agent before the signed UI cap, so live future-match application remains (`DailyReviewStore.swift`; `ScreenwatchArchive.swift`; `.audit/runs/unknown-session-review/verifier/REPORT.md`).

## 62. Complete degraded-mode journey

- [ ] Begin a planned workday with all sources healthy. **Status: Partially implemented.** One post-recovery run confirmed Reminders, Screenwatch, the background agent, canonical database, and ingestion verifier healthy, but the later Screenwatch freshness recheck failed and the exact approved-day start flow remains incomplete.
- [ ] Lose Screenwatch activity during an active task. **Status: Blocked from verification.** Disrupting the user's real capture service was not performed; this needs an isolated fixture or explicit controlled outage.
- [ ] See a source warning and prompt suppression. **Status: Partially implemented.** Freshness warnings and suppression policies exist, but a live outage was not induced.
- [ ] Continue manually tracking the active task. **Status: Touches remaining.** Active commitments now expose explicit Pause, Break, Complete, and open-ended continuation semantics independently of Screenwatch, while canonical-agent tests prove one durable active interval across restart. A controlled Screenwatch outage plus installed manual-task continuation was not completed because the signed Reminder fixture failed closed, so the degraded-mode journey remains (`ActiveCommitmentPresentation.swift`; `MenuBarCoachView.swift`; `TodayDashboardAgentTests`; `.audit/runs/active-commitment-visibility/verifier/REPORT.md`).
- [ ] Lose notification availability before a prompt. **Status: Blocked from verification.** Notification authorization was not changed during the audit.
- [ ] Receive the prompt through notification or dashboard instead. **Status: Partially implemented.** Prompt inbox fallback exists, but notification-loss routing was not exercised end to end.
- [ ] Resolve the prompt once from the available surface. **Status: Partially implemented.** Idempotent prompt response infrastructure is tested, but no live degraded-mode episode was resolved.
- [ ] Recover Screenwatch and notification delivery. **Status: Blocked from verification.** Safe live service disruption and recovery were not attempted.
- [ ] See source health return to normal without losing the plan or duplicating actions. **Status: Partially implemented.** The installed signed-QA agent moved visibly through healthy, repaired, not connected, and re-enabled states while a preservation marker survived and the production helper remained isolated; Screenwatch and notification outages, plan preservation with real populated data, and cross-surface duplicate-action checks remain unproven (`.audit/runs/agent-lifecycle/candidate/REPORT.md`).
- [ ] Confirm a review that clearly identifies the missing coverage period. **Status: Not implemented.** No functional review confirmation path exists.

## 63. Complete correction-and-learning journey

- [ ] Finish a day containing an incorrectly classified session. **Status: Touches remaining.** The Reviews surface now groups local behavior observations into privacy-safe application sessions and displays the stored classification, but a signed-QA day containing a known incorrect session could not be visibly exercised while the Mac was locked (`Sources/ZoidCoachCore/DailyReview.swift`; `Sources/ZoidCoachInfrastructure/DailyReviewStore.swift`; `.audit/runs/daily-review/7b96623/REPORT.md`).
- [ ] Open the daily review. **Status: Touches remaining.** Reviews navigation now renders the complete daily review surface with day selection, loading, empty, failure, retry, and populated states in the clean signed-QA package; visible navigation proof remains outstanding because Computer Use reported a locked Mac (`Sources/ZoidCoachApp/Views/DashboardView.swift`; `Sources/ZoidCoachApp/Views/DailyReviewView.swift`; `.audit/runs/daily-review/7b96623/REPORT.md`).
- [ ] Find the incorrect session and reclassify it. **Status: Touches remaining.** Every session exposes an accessible classification picker and durable Apply Correction action, and store tests prove reclassification survives reopening and recalculates totals; the packaged interaction still needs visible click-through proof (`DailyReviewView.swift`; `DailyReviewTests.swift`; `.audit/runs/daily-review/7b96623/REPORT.md`).
- [ ] Split or attach the session to a task if needed. **Status: Touches remaining.** The user can correct the second half of a session and attach a trimmed task identifier or title, with deterministic tests proving the first half remains unchanged and the task attachment persists; selecting a canonical Reminder task and visible signed-QA interaction remain as finishing touches (`DailyReviewStore.swift`; `DailyReviewTests.swift`).
- [ ] See totals and review language update. **Status: Touches remaining.** Applying a correction reloads the snapshot, recalculates category totals, and regenerates explicitly tentative review language without exposing titles, URLs, or screenshots; visible signed-QA before-and-after proof remains outstanding (`DailyReviewStore.swift`; `DailyReviewView.swift`; `DailyReviewTests.swift`).
- [ ] Reject an unsupported causal hypothesis. **Status: Touches remaining.** The review presents Accept Explanation and Reject Explanation actions, persists rejection locally, labels the explanation as a hypothesis rather than fact, and invalidates a prior confirmation when the decision changes; the signed-QA control was not visibly clicked while the Mac was locked (`DailyReviewView.swift`; `DailyReviewStore.swift`; `DailyReviewTests.swift`).
- [ ] Confirm the corrected review. **Status: Touches remaining.** Confirmation is durable and remains editable, while any later correction atomically returns the hypothesis to pending and clears confirmation so learning cannot use stale approval; visible relaunch confirmation proof remains outstanding (`DailyReviewStore.swift`; `DailyReviewTests.swift`; `.audit/runs/daily-review/7b96623/REPORT.md`).
- [x] Complete enough well-covered days for a weekly review. **Status: Fully implemented.** The signed QA journey visibly progressed from 0/7 limited evidence to a 7/7 review for 2026-07-04 through 2026-07-10, and the store tests enforce at least three confirmed days with 30 observed minutes (`WeeklyReviewStore.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Inspect the evidence behind a weekly pattern. **Status: Fully implemented.** The installed signed app expanded estimate evidence into three dated estimated-versus-corrected examples and an alternative explanation, then collapsed it through the same accessible control (`WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).
- [x] Accept, edit, or reject one proposed experiment. **Status: Fully implemented.** The installed signed app edited, accepted, relaunched with tracking, rejected, and relaunched with the rejected state preserved for the same single weekly experiment (`WeeklyReviewStore.swift`; `WeeklyReviewView.swift`; `.audit/runs/weekly-review/verifier/REPORT.md`).

## 64. Release 1 end-user acceptance

- [ ] Use real Apple Reminders for daily planning. **Status: Touches remaining.** The installed app displays real Reminders, builds plans, and now persists through the canonical store, but permission-denied and manual fallback remain incomplete.
- [ ] Use real Screenwatch activity for understandable behavior totals. **Status: Touches remaining.** Live behavior minutes and app-percentage disclosure are visible, and the new daily review derives privacy-safe category totals plus durable corrections from the same canonical behavior records. The later recheck found Screenwatch stale, and the complete signed-QA correction journey remains unverified (`DailyReviewStore.swift`; `.audit/runs/daily-review/7b96623/REPORT.md`).
- [ ] Use notifications or the Today dashboard for timely status and prompts. **Status: Partially implemented.** Both delivery surfaces exist, but the core gaming-drift coaching episode is not complete.
- [ ] Complete morning planning, task tracking, one gaming intervention, response, recovery, and review as one continuous flow. **Status: Not implemented.** Planning, task tracking, and daily review now exist as separate durable journeys, but the gaming intervention, response, recovery, and continuous cross-surface acceptance run remain missing.
- [ ] Use the app for seven consecutive days without losing plans, estimates, task sessions, corrections, or prompt responses. **Status: Blocked from verification.** Corrections, hypothesis decisions, and confirmations now persist through restart in deterministic tests, but no controlled seven-day acceptance run has been completed.
- [ ] Complete the baseline week without unwanted behavior interruptions. **Status: Partially implemented.** A durable seven-complete-day observation gate, missing/limited-day handling, accessible Today progress, correction-aware report, frozen completion, and restart-safe agent checkpoint now exist. This remains partial because no production behavior-prompt generator or configured coaching-level model consumes the gate yet, so actual prompt suppression and post-baseline progression are unproven (`BaselineObservationStore.swift`; `BaselineObservationView.swift`; `.audit/runs/baseline-observation/candidate/REPORT.md`).
- [ ] Understand every intervention and the evidence that caused it. **Status: Partially implemented.** Prompt context is stored and some UI explanations exist, but the core gaming intervention evidence surface is incomplete.
- [x] Correct wrong classifications and see the product adapt. **Status: Fully implemented.** In the isolated signed QA app, a user corrected Steam from Unknown to Work, explicitly enabled and previewed future adaptation, saw the active rule survive relaunch, and the signed agent classified a later Screenwatch Steam observation as Work without rewriting the historical observation (`DailyReviewStore.swift`; `ScreenwatchArchive.swift`; `.audit/runs/correction-to-rule/f6a281e/REPORT.md`).
- [ ] Pause or override coaching without punishment. **Status: Partially implemented.** Automation can be paused, but intentional gaming override and coaching-specific pause durations are incomplete.
- [ ] Continue using planning and task tracking when behavior sources fail. **Status: Partially implemented.** The architecture and canonical persistence separate planning from behavior sources, but a live source outage and complete manual-control flow were not verified.
- [ ] Export or delete personal data without hidden dependencies. **Status: Touches remaining.** Export, inventory, retention, and every deletion route run locally through the database and agent without network services, source-file ownership, or Keychain deletion; the complete packaged UI journey still needs isolated destructive verification.
- [ ] Understand the product under missing data, denied permissions, restarts, and integration failures. **Status: Partially implemented.** Health and safety UI exists, canonical persistence is healthy, and the latest verifier correctly exposed Screenwatch staleness, but several failure states still lack a complete explanation and repair journey.
- [ ] Finish the week feeling that the next responsible action is easier to see and begin. **Status: Blocked from verification.** This outcome requires a stable week-long product trial after the missing review and coaching flows are implemented.

## 65. Explicitly deferred end-user scenarios

- [x] Keep team workspaces, managers, shared permissions, and monitoring of other people out of Release 1. **Status: Fully implemented.** The installed app is single-user and local-first with no team or remote-admin surface (`.audit/runs/baseline/a068d27/REPORT.md`).
- [x] Keep a public web dashboard out of Release 1. **Status: Fully implemented.** The product is a native local macOS app and has no public web dashboard (`.audit/runs/baseline/a068d27/REPORT.md`).
- [x] Keep cloud screenshot synchronization off by default and outside the MVP. **Status: Fully implemented.** Screenwatch evidence remains local and no cloud screenshot-sync feature is exposed (`.audit/runs/baseline/a068d27/REPORT.md`).
- [x] Keep mobile and cross-device behavior correlation outside the MVP. **Status: Fully implemented.** No mobile companion or cross-device behavior surface exists (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] Keep calendar auto-scheduling outside the MVP. **Status: Not implemented.** The current product explicitly supports autonomous Calendar planning and exposes `ACCEPT BLOCKS`, so this deferred constraint has been superseded rather than satisfied.
- [x] Keep automatic multi-task Reminder decomposition outside the MVP. **Status: Fully implemented.** The app plans existing tasks and does not expose automatic decomposition into multiple new Reminders (`.audit/runs/baseline/a068d27/REPORT.md`).
- [x] Keep hard application or website blocking outside Release 1. **Status: Fully implemented.** No blocking controls or enforcement service are present (`.audit/runs/baseline/a068d27/REPORT.md`).
- [ ] Require any future blocking to be explicitly enabled, reversible, time-bounded, and protected by an escape hatch. **Status: Touches remaining.** The signed Background Agent surface now states `HARD BLOCKING OFF`, explains that this release does not block applications or websites, presents Explicit enablement, Reversible, Time-bounded, and Escape hatch in stable order at standard and narrow widths, exposes no activation control or focus trap, preserves the existing lifecycle actions, causes zero policy or history mutations, and restores identically after app and helper relaunch (`Sources/ZoidCoachApp/BlockingSafetyDisclosure.swift`; `Sources/ZoidCoachApp/Views/AgentLifecycleView.swift`; `Tests/ZoidCoachAppTests/BlockingSafetyDisclosureTests.swift`; `.audit/runs/hard-blocking-safety/verifier/REPORT.md`). This is a visible and mechanically tested release guardrail rather than a blocking engine, so enforcement before activation, during a future active block, and through a real emergency escape remains unproven.
