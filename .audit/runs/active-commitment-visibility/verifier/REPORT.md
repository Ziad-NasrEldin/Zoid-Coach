# Active Commitment Visibility Verification

## Decision

`ZC-059-007` and `ZC-062-004` advance from Partially implemented to Touches remaining.

`ZC-017-001` and `ZC-037-001` remain Touches remaining.

No owned scenario qualifies as Fully implemented because the installed task-start and cross-surface journey did not complete.

## Lineage and focused proof

The candidate was verified on provisional authoritative code root `d95ace7` after prompt-task-block integration at `3738e69`.

One focused invocation selected `ActiveCommitment`, `activeMenuTaskCompletes`, and `menuBarBreakAndEndWorkdayPersistThroughTheCanonicalAgent` and passed nine Swift Testing tests with exit code 0.

The proof covers open-ended, bounded, continued-open-ended, and sprint-complete presentation, inactive-row rejection, the exact menu Complete command, one canonical open activity interval, and reconstruction through a fresh agent instance.

## Signed package and installed boundary

One release package installed `/Users/ziadnasreldin/Applications/Zoid 666 QA E2E.app` against isolated root `/private/tmp/zoid-666-active-commitment-qa`.

Packaging, code signing, designated requirement, embedded helper identity, LaunchAgent registration, and exact helper startup passed.

The installed app visibly exited onboarding into Today through `EXIT FOR NOW`.

A one-task Reminder fixture request was prepared for `Verify active commitment`.

After the single permitted app and helper restart, Today failed closed into `READ-ONLY SAFETY MODE` with the exact message that the QA OS fixture control request was not valid JSON or used unsupported field values.

The fixture still showed Reminders as not connected and zero available tasks, so the verifier did not retry, seed the database directly, or substitute a local-only task for the required Reminder journey.

The real macOS status item was not exercised and remains outside the accessible app tree, matching the previously documented accessibility boundary.

## Cleanup

The installed app and helper were stopped, the QA LaunchAgent was unregistered, and the installed app plus isolated runtime root were removed.

`launchctl` confirmed that `qa.ziadnasreldin.ZoidCoach.agent` was absent after cleanup.

## Remaining acceptance work

Run one corrected supported Reminder fixture journey from ready task through Today Start, app and helper restart, and Today persistence.

Exercise the real status item or an accepted status-item automation boundary for the matching named task, timing, Pause, Break, Complete, and Open Today controls.

Complete from the menu and prove one history event plus one Reminder mutation after relaunch.

Repeat manual tracking while Screenwatch is intentionally unavailable before qualifying `ZC-062-004` as Fully implemented.
