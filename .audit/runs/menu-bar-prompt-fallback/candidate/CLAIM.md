# Menu bar prompt fallback claim

This isolated lane starts from authoritative commit `c6954d2`.

Higher ready backlog items are signed-runtime proof or already-delivered onboarding policy candidates.
This lane claims the first unimplemented end-user gap inside the ready notification fallback priority.

## Scenarios

- `ZC-038-002` - See a menu bar badge if notifications are unavailable.
- `ZC-038-008` - Continue using all task controls without notifications.

## Owned files

- `Sources/ZoidCoachApp/ZoidCoachApp.swift`.
- `Sources/ZoidCoachApp/MenuBarCoachState.swift`.
- `Sources/ZoidCoachApp/MenuBarCoachView.swift`.
- `Tests/ZoidCoachAppTests/MenuBarCoachTests.swift`.
- `.audit/runs/menu-bar-prompt-fallback/candidate/*`.

## Boundaries

This lane adds a notification-aware unresolved-decision badge and a direct Today fallback inside the existing menu bar surface while preserving every task control.
It does not touch notification delivery, prompt response routing, GamingDrift files, root, installed runtime, tracker, registry, backlog, or Lavish.
