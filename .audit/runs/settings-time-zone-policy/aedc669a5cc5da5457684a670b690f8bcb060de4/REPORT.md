# Signed settings time-zone acceptance

The signed QA application used build identity `zoid-coach-aedc669a5cc5da5457684a670b690f8bcb060de4-clean` and an isolated runtime root.

The installed Settings interface exposed the full IANA picker, selected-zone UTC offset, Mac-zone comparison, direct Mac-zone restore, and historical-instant explanation.

`America/Los_Angeles` saved through the live helper and survived Settings reopen plus application and helper restart.

A competing proper policy mutation produced a visible conflict naming `Time zone`, preserved the London winner, and saved Los Angeles only after deliberate reapply.

An independent proper mutation preserved a Tokyo draft together with the concurrent 55 percent planning capacity, and the next save persisted both values.

The user created, started, and completed `Historical instant proof`, and Daily Review visibly retained the completed local task.

The durable completion value remained exactly `2026-07-13T11:21:36Z` after further zone changes, Mac-zone restore, application restart, and helper restart.

Final Settings state visibly restored `Africa/Cairo`, `UTC+03:00`, the Mac match, and the independently merged 55 percent planning capacity.

The application, helper registration, runtime root, and install root were removed after acceptance.
