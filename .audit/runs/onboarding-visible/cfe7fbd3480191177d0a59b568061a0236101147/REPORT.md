# Installed Zoid 666 onboarding and first-plan verification

Date: 2026-07-12

Branch: `codex/full-system`

Verified commit: `cfe7fbd3480191177d0a59b568061a0236101147`

Build identity: `zoid-coach-cfe7fbd3480191177d0a59b568061a0236101147-clean`

Installed application: `/Users/ziadnasreldin/Applications/Zoid 666 QA E2E.app`

QA root: `/private/tmp/zoid-666-signed-qa`

## Package and runtime proof

The installed application passed deep strict code-sign verification.

The application and helper report Team ID `377QC32T9T` with QA signing identifiers `qa.ziadnasreldin.ZoidCoach` and `qa.ziadnasreldin.ZoidCoach.agent`.

The application bundle displays `Zoid 666 QA` and stamps the exact clean commit and build identity above.

SMAppService registered the dedicated QA LaunchAgent.

Launchd reported the service running, and `lsof` resolved its executable to `/Users/ziadnasreldin/Applications/Zoid 666 QA E2E.app/Contents/MacOS/ZoidCoachAgentQA`.

The helper opened the isolated database below `/private/tmp/zoid-666-signed-qa/Application Support/Zoid 666`.

The real QA XPC mutation probe passed durable receipt, same-request replay, and stale-write rejection.

## Visible onboarding proof

The signed application opened a clean `Zoid 666 QA` first launch.

Welcome and Local Truth explained intended Reminders work, observed behavior, local storage, optional AI, and no shame or blocking.

Quitting at step 3 and reopening restored step 3 exactly.

The granted QA Reminders path became healthy and discovered Work and Personal under exact stable identifiers.

The visible controls saved Include Work and Exclude Personal through the running helper.

Screenwatch was deliberately deferred and setup continued.

The granted QA notification path delivered the bounded onboarding notification and displayed `DELIVERY · DELIVERED`.

Application inventory scanned 116 installed applications.

Activity Monitor was saved as Work, Atoll was saved as Gaming, and all remaining reviewed applications retained Automatic.

The run visibly reviewed and saved the 09:00-18:00 work window and 23:00-07:00 quiet window.

Balanced gaming and Rules-only coaching were selected and saved through XPC.

The delivery-proof task was completed and the run advanced to step 12.

## First-plan and restart proof

Step 12 displayed a real local starter objective named `Choose today's main objective` with a 15-minute estimate.

Opening Today immediately displayed that objective as the active commitment and displayed one planned block.

The direct stale-snapshot blocker was fixed by refreshing Today when onboarding changes route.

The direct plan-deletion blocker was fixed by retaining incomplete local source identifiers during external Reminder reconciliation.

Replacing the installed signed application while preserving the QA root reopened Today with the same objective, estimate, selected time block, and action controls.

An additional quit and relaunch produced the same result.

The dashboard masthead now renders `ZOID 666 LOCAL COMMAND OPERATIONS` without the legacy product name.

## Remaining environment boundaries

The deterministic signed-QA run does not claim a real macOS EventKit permission reset or a real notification permission reset.

Those permission-specific scenarios remain `Touches remaining` in the tracker.

The first installer attempt exposed a stale SMAppService registration after bootout.

The built-in QA probe safely forced an unregister and re-register cycle, after which the clean installer and runtime proof passed.
