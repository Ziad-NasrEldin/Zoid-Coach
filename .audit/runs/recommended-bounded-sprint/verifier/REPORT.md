# Recommended Bounded Sprint Verification

## Decision

`ZC-015-004` is upgraded to Touches remaining.

The decisive oversized-task journey is usable end to end in the installed signed Zoid 666 QA application.

The fitting-task branch passed focused rule coverage but was not revisited in the signed interface before the strict ten-minute UI cap ended, so this verifier does not claim Fully implemented.

## Integrated lineage

The implementation commit is `6d14c78`.

The live-capacity correction is `df4a716`.

Both commits are rebased onto authoritative baseline `67257f4`.

The packaged application identified itself as `zoid-coach-df4a71659e6a97e0d604179ad79f2e02f839bf0c-clean`.

## Focused verification

The focused Today dashboard, legacy decoding, custom sprint bounds, expiry, and restart seams passed before the verifier correction.

The post-rebase `recommendationSprintPresentationUsesLiveAvailability` regression passed once.

That regression covers exact available-minute sizing, the 25-minute cap, zero capacity, fitting-task suppression, and truthful incomplete-at-expiry copy.

## Release verification

Exactly one release QA package was produced.

Packaging passed app and helper compilation, signing, designated-requirement validation, LaunchAgent validation, and Mach-service validation.

The normal signed-app registration handshake succeeded once and launched the exact installed app and helper from the isolated QA root `/private/tmp/zoid-666-bounded-recommendation-sprint-qa`.

## Signed end-to-end verification

A deterministic Calendar fixture left exactly 20 minutes of focus capacity.

The installed Today interface displayed `90 MIN PLANNED / 20 MIN AVAILABLE` for `Prepare migration plan`.

The recommendation displayed a 20-minute sprint, named the full task, stated the exact available time, and explicitly said the task would stay incomplete when the sprint ended.

The direct action exposed stable identifier `today.recommendation.start-sprint`, an exact task-specific accessibility label, and a truthful hint.

Activating the action once created one active 20-minute sprint and changed Today to the active commitment state.

After terminating and relaunching both the installed app and helper, the same sprint remained active with 19 minutes and 35 seconds remaining.

Advancing the isolated canonical sprint timestamps beyond the boundary and relaunching showed `SPRINT COMPLETE`, kept the task active and incomplete, and exposed `CONTINUE OPEN-ENDED`.

With the deterministic Calendar fixture consuming the full work window, Today displayed `0 MIN AVAILABLE` and exposed no recommendation sprint action.

## Remaining touch

The 15-minute task with 20 minutes available is covered by the focused regression and the existing normal Start path.

The strict signed-interface cap ended before that final fitting branch was reseeded and observed in the installed app.

No additional package, registration handshake, or UI session was started after the cap.
