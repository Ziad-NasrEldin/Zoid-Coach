# ZC-016-006 signed acceptance

The complete installed signed journey passed at commit `6659884174b31ab366c9bda83de7fd8bcd0d768f` with build identity `zoid-coach-6659884174b31ab366c9bda83de7fd8bcd0d768f-clean`.
The exact clean package, application identity, helper identity, LaunchAgent label, isolated QA root, and database binding passed the committed signed preflight.
The first task was made the visible main objective through Today and started from its bounded-sprint control.
Today and the compact menu exposed `Write the first focus brief` as the same single privacy-safe active task.
SQLite independently proved exactly one active execution state and one open activity interval for `qa-zc016006-first`.
The real second-task switch path used the exact title-bound `START PREPARE THE SECOND FOCUS REVIEW` control and the native `Switch and preserve time` confirmation.
Today and the compact menu then exposed `Prepare the second focus review` as the only active task without retaining the previous title in the compact summary.
SQLite independently proved that `qa-zc016006-second` was the only active task and that `qa-zc016006-first` was paused.
An ordinary LaunchServices relaunch retained the second task as the only active task across Today, the compact menu, and SQLite without restarting the helper.
The closed menu-bar item exposed the dynamic `A task is active` accessibility title before each compact-menu inspection.
The private Screenwatch sentinel never appeared in Today or the compact menu.
Cleanup restored all isolated QA files byte-for-byte and metadata-for-metadata, then removed the temporary QA root, installed package, helper, and LaunchAgent.
Production app PID `29929` and helper PID `53195` remained on their original executable paths with unchanged executable hashes throughout the signed run.
The sealed external evidence set, ordered logs, summary, and 25-entry SHA-256 manifest remain at `/private/tmp/zoid-zc016006-evidence/6659884174b31ab366c9bda83de7fd8bcd0d768f`.
